# Rust
/target
**/target
Cargo.lock.bak

# dfx / ICP
.dfx/
**/.dfx/
canister_ids.json
.vessel/
.mops/

# Generated Motoko (from .mview compilation)
**/src/Generated/
**/.motoview/
**/.mvbuild/

# WASM build artifacts that are rebuilt
client/pkg/*.tmp

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
*.log

# Local env
.env
.env.local

# Internet Identity dev build (downloaded; reproducible)
**/.ii/
# Native client shells build output
**/node_modules/
**/src-tauri/target/
**/gen/

# Native clients: SwiftPM + Gradle build dirs.
clients/ios/.build/
clients/ios/.swiftpm/
clients/android/.gradle/
clients/android/**/build/
clients/android/local.properties

# Cross-compiled Rust core static archives — build OUTPUTS, regenerated by
# `tools/native-build.sh ios`. Not committed: they are large, platform-specific,
# and would silently drift out of sync with client/src/. Run the build to
# produce them locally before `swift build`.
clients/ios/libs/
