# Cargo build artifacts for this standalone crate.
/target

# Packaging outputs from bundle.sh. The .app bundle and the .dmg are BUILD
# OUTPUTS — never committed. Only the packaging SCRIPT (bundle.sh), the
# Info.plist template, and this .gitignore are tracked. Re-create the artifacts
# any time with `bash bundle.sh`.
/dist
