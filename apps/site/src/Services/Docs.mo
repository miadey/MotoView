/// MotoView documentation content — GENERATED by
/// apps/site/tools/gen-docs.py from /docs/*.md. Do not edit by hand.
import Text "mo:base/Text";

module {
  public class Docs() {
    public type Doc = {
      slug : Text; title : Text; section : Text; html : Text;
      prevSlug : Text; prevTitle : Text; nextSlug : Text; nextTitle : Text;
    };

    public func firstSlug() : Text { "introduction" };

    public func get(slug : Text) : ?Doc {
      switch (slug) {
        case ("introduction") { ?{ slug = "introduction"; title = "Introduction"; section = "Prologue"; html = "<h1 id=\"introduction\">Introduction</h1>\n<p>MotoView is a Motoko-native, server-driven UI framework for Internet Computer (ICP) canisters. You write <code>.mview</code> files — template markup and Motoko code living together in a single file — and MotoView compiles them to Motoko and deploys them to ICP with <code>dfx</code>. There is no application JavaScript, no React, no agent-js glue, and no validation you have to write twice.</p>\n<p>If you have used Blazor, Laravel Blade, or Phoenix LiveView, the feel will be familiar. The difference is where it runs: your UI lives inside the canister, rendered by Motoko, served straight from the chain.</p>\n<blockquote><strong>Write Motoko. Ship interactive, SEO-friendly ICP apps. No frontend JavaScript.</strong></blockquote>\n<h2 id=\"the-core-promise\">The core promise</h2>\n<p>The mental model is small enough to fit in one sentence: <strong>rendering is a query, events are updates, and the browser synchronizes through versioned UI batches.</strong></p>\n<ul><li><strong>Render as query.</strong> When the page needs to show its current state, the server renders it. The first load is server-rendered HTML — real markup that search engines and link previews can read — with the page content living inside <code>&lt;div id=\"mv-root\"&gt;</code>.</li><li><strong>Event as update.</strong> When a user clicks a button or submits a form, the browser posts the event to the canister. An update call dispatches it to a typed Motoko handler, your state changes, and a freshly rendered batch comes back immediately.</li><li><strong>Sync through versioned batches.</strong> Every rendered state carries a <code>batchId</code> that hashes its content. The browser keeps polling on an adaptive cadence; if the <code>batchId</code> hasn't changed, the server answers <code>unchanged</code> and skips re-sending HTML. When it has changed, the client swaps in the new markup.</li></ul>\n<p>You never wire up fetch calls, diff state by hand, or re-implement your validation rules on a client. You write Motoko, and the protocol moves it to the screen.</p>\n<h2 id=\"the-one-browser-component\">The one browser component</h2>\n<p>MotoView ships exactly one piece of browser code, and you don't write it. The client is written in Rust and compiled to WebAssembly — served at <code>/motoview.wasm</code> — accompanied by a tiny hand-written JS glue (<code>/motoview.js</code>, no bundler, no npm). The WASM is the brain: the adaptive polling state machine, the <code>motoview/1</code> protocol, batch interpretation, and event sequencing. The JS glue is just the hands: the unavoidable DOM, fetch, and timer primitives, plus focus, scroll, and input preservation. There is no JS build step in your project, and that is on purpose.</p>\n<h2 id=\"who-it-s-for\">Who it's for</h2>\n<p>MotoView is for developers building real, interactive ICP applications who would rather stay in one language than stitch a frontend toolchain onto a canister. It suits dashboards, CRUD apps, forms, and content-driven pages that need to be SEO-friendly and stay fast — without a Node/npm pipeline anywhere in sight. If you already know Motoko, you already know most of MotoView.</p>\n<h2 id=\"one-file-the-mview-idea\">One file: the <code>.mview</code> idea</h2>\n<p>A <code>.mview</code> file is template markup plus Motoko in the same place. Markup describes the view; an <code>@code { ... }</code> block holds the state and handlers; directives like <code>@if</code>, <code>@for</code>, and inline output (<code>@count</code>) bind them together. Here is a complete, interactive counter — the example that has been deployed to a local replica and exercised in a real browser:</p>\n<pre class=\"lang-razor\"><code>@page \"/counter\"\n@title \"Counter\"\n\n@code {\n    var count : Nat = 0;\n\n    func increment() { count += 1 };\n    func decrement() { if (count &gt; 0) count -= 1 };\n}\n\n&lt;h1&gt;Counter&lt;/h1&gt;\n&lt;p&gt;Current value: @count&lt;/p&gt;\n\n&lt;Button kind=\"primary\" @click=\"increment\"&gt;+&lt;/Button&gt;\n&lt;Button @click=\"decrement\"&gt;-&lt;/Button&gt;</code></pre>\n<p>The <code>@click</code> handlers map to the <code>increment</code> and <code>decrement</code> functions you wrote right above the markup. Clicking sends an event, the update call runs the handler, the new state is rendered into a batch, and the DOM swaps. State persists across calls because it lives in the canister.</p>\n<p>To build and run it locally:</p>\n<pre class=\"lang-bash\"><code>motoview new my-app\nmotoview dev</code></pre>\n<p><code>motoview dev</code> compiles your <code>.mview</code> files to Motoko, deploys to the local replica with <code>dfx</code>, and watches for changes.</p>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li><a href=\"/docs/events\">Events</a> — how <code>@click</code>, <code>@submit</code>, and <code>@input</code> map to typed Motoko handlers.</li><li><a href=\"/docs/forms\">Forms &amp; Validation</a> — <code>secure</code> forms, <code>bind</code>, and <code>validate</code> blocks.</li><li><a href=\"/docs/components\">Components</a> — built-in semantic components and your own under <code>src/Components</code>.</li><li><a href=\"/docs/protocol\">Protocol</a> — the <code>motoview/1</code> batch format and adaptive polling cadence.</li></ul>\n<p>Write Motoko. Ship the page.</p>"; prevSlug = ""; prevTitle = ""; nextSlug = "why-motoview"; nextTitle = "Why MotoView" } };
        case ("why-motoview") { ?{ slug = "why-motoview"; title = "Why MotoView"; section = "Prologue"; html = "<h1 id=\"why-motoview\">Why MotoView</h1>\n<p>Building an interactive app on the Internet Computer usually means building two apps.</p>\n<p>You write a Motoko canister for your data, your authorization, and your business rules. Then you write a <em>second</em> application — a React or Svelte frontend — that talks to the canister through agent-js. You wire up Candid bindings, manage a build pipeline of npm packages and bundlers, and hand-write <code>fetch</code>-style calls into the replica. And because the browser cannot be trusted, you write your validation rules twice: once in TypeScript to keep the form snappy, and once in Motoko because the TypeScript copy is only a suggestion.</p>\n<p>The result is a split architecture with a seam running right down the middle:</p>\n<ul><li><strong>Two languages, two mental models.</strong> Motoko on one side, JavaScript on the other.</li><li><strong>agent-js glue</strong> to translate every interaction across the wire.</li><li><strong>Duplicated validation</strong> that drifts the moment someone forgets to update both copies.</li><li><strong>A JavaScript build chain</strong> — npm, a bundler, a dozen transitive dependencies — that has nothing to do with your product.</li></ul>\n<p>Every feature you ship has to cross that seam. That is where the bugs live.</p>\n<h2 id=\"how-motoview-collapses-it\">How MotoView collapses it</h2>\n<p>MotoView removes the seam. You write <code>.mview</code> files — template markup and Motoko in a single file — and the <code>motoview</code> compiler turns them into a Motoko actor that you deploy with <code>dfx</code>. There is no application JavaScript, no React, no agent-js, and no duplicated validation. Rendering is a query, events are updates, and the browser stays in sync through versioned UI batches.</p>\n<p>Here is a counter — the example we deployed to a local replica and clicked through in a real browser:</p>\n<pre class=\"lang-razor\"><code>@page \"/\"\n@title \"Counter\"\n\n&lt;Card title=\"Counter\"&gt;\n  &lt;p&gt;Count: @count&lt;/p&gt;\n  &lt;Button kind=\"primary\" @click=\"increment\"&gt;+1&lt;/Button&gt;\n&lt;/Card&gt;\n\n@code {\n  var count : Nat = 0;\n\n  func increment() {\n    count += 1;\n  };\n}</code></pre>\n<p>Clicking the button posts an event to the canister, which dispatches to your typed Motoko <code>increment</code> function, recomputes the page, and returns a new batch. The client swaps the DOM. No <code>useState</code>, no API route, no Candid binding written by hand. State lives in the canister and persists across calls. See <a href=\"/docs/events\">Events</a> for how handlers and arguments work.</p>\n<p>Validation happens once, in Motoko, where it is enforced:</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputText name=\"name\" label=\"Name\" bind=\"@model.name\" /&gt;\n  &lt;InputEmail name=\"email\" label=\"Email\" bind=\"@model.email\" /&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;Button kind=\"primary\"&gt;Send&lt;/Button&gt;\n&lt;/form&gt;\n\n@code {\n  func send() {\n    validate model {\n      name required \"Name is required\";\n      email required email;\n    };\n    // ...persist the message\n  };\n}</code></pre>\n<p>The <code>secure</code> attribute mints a signed token (HMAC-SHA256) binding the path, handler, caller principal, nonce, expiry, and field-schema hash; the server re-derives the MAC on submit and rejects tampering, expired tokens, and replays. There is no second validation layer to keep in sync because there is no second language. See <a href=\"/docs/forms\">Forms &amp; Validation</a> and <a href=\"/docs/security\">Security</a>.</p>\n<h2 id=\"design-philosophy\">Design philosophy</h2>\n<p><strong>Readable templates over class-name soup.</strong> A template should read like the page it produces. MotoView ships semantic components — <code>Button</code>, <code>Card</code>, <code>Alert</code>, <code>Table</code>, <code>InputText</code>, <code>ValidationSummary</code> — so your markup says what it <em>means</em>, not which forty utility classes happen to produce the look:</p>\n<pre class=\"lang-razor\"><code>&lt;Button kind=\"primary\" size=\"lg\"&gt;Save&lt;/Button&gt;</code></pre>\n<p>not a wall of utility classes you have to decode every time you read it.</p>\n<p><strong>No <code>.map()</code> JSX.</strong> Control flow is markup, not a JavaScript expression smuggled into your view. You loop and branch with directives that read top to bottom:</p>\n<pre class=\"lang-razor\"><code>@for product in products {\n  &lt;Card title=\"@product.name\"&gt;\n    &lt;p&gt;@product.price&lt;/p&gt;\n  &lt;/Card&gt;\n}</code></pre>\n<p>If you have written Blade, Razor, or LiveView, this will feel familiar — the difference is that the language underneath is Motoko, running in your canister.</p>\n<p><strong>No JavaScript build chain.</strong> The only browser code is a small Rust-to-WebAssembly client (the polling state machine, the protocol, batch interpretation) plus a hand-written JS glue for the unavoidable DOM, fetch, and timer primitives. No npm, no bundler. Your toolchain is <code>dfx</code>, <code>rustup</code>, and the <code>motoview</code> compiler — and nothing else gets between you and your app.</p>\n<p>One language. One source of truth for validation. One build. Ready to write some Motoko? Start with <a href=\"/docs/quickstart\">Getting Started</a>.</p>"; prevSlug = "introduction"; prevTitle = "Introduction"; nextSlug = "whats-new"; nextTitle = "What's New" } };
        case ("whats-new") { ?{ slug = "whats-new"; title = "What's New"; section = "Prologue"; html = "<h1 id=\"what-s-new\">What's New</h1>\n<p>A changelog of the major recent additions to MotoView. MotoView is a young framework, so this is a record of what landed and what it does — not a marketing sheet. Where a feature is opt-in or has a caveat, it says so.</p>\n<hr>\n<h2 id=\"zero-trust-end-to-end-encryption-in-browser-vetkeys\">Zero-trust end-to-end encryption (in-browser vetKeys)</h2>\n<p>The headline addition: a MotoView app can now hold <strong>only ciphertext</strong> on-chain and decrypt <strong>in the browser</strong>. The canister never sees plaintext and has no decrypt path. Encryption keys are vetKeys — threshold-derived across the subnet, so no single node ever holds the key — and the BLS unwrap + IBE encrypt/decrypt run client-side in a Rust→WASM crypto module. This closes the last pillar of the zero-trust story (see <a href=\"/docs/zero-trust\">Zero-Trust</a> for the full architecture).</p>\n<p>It is <strong>opt-in</strong>, and honestly so: the default brain is ~76 KB of WASM; bundling the <code>ic-vetkeys</code> BLS12-381 + IBE machinery adds ~300 KB. Apps that don't need client-side encryption keep the lean default and pay nothing.</p>\n<p>The moving parts:</p>\n<ul><li><strong><code>motoview-crypto</code> WASM module</strong> — the client crypto (<code>client-crypto/</code>,</li></ul>\n<p>built with <code>tools/build-client.sh</code>) compiled to <code>wasm32-unknown-unknown</code> and served by the canister at <code>/motoview-crypto.wasm</code>. All BLS/IBE logic lives here; there is no crypto in JavaScript.</p>\n<ul><li>**<code>/_motoview/vetkd/*</code> endpoints** — every generated actor auto-exposes</li></ul>\n<p><code>GET /_motoview/vetkd/public-key</code> (the master public key) and <code>POST /_motoview/vetkd/derive</code> (a vetKey for the session-authenticated caller, bound to their principal via the II session cookie). The canister mediates the threshold derivation but only forwards a blob already encrypted to the client's transport key.</p>\n<ul><li><strong><code>window.mvCrypto</code> glue</strong> — the dumb-hands bridge. It lazy-loads</li></ul>\n<p><code>/motoview-crypto.wasm</code> on first use, gets randomness from <code>window.crypto</code>, fetches the two endpoints, marshals bytes in and out of the crypto WASM, and caches the unwrapped vetKey for the session. No decision logic.</p>\n<ul><li><strong>Declarative <code>data-mv-encrypt</code> / <code>data-mv-decrypt</code></strong> — the entire client</li></ul>\n<p>surface is two attributes, no JavaScript. On submit, every <code>[data-mv-encrypt]</code> field is IBE-encrypted before the (secure) form is sent. On render, every <code>[data-mv-decrypt]</code> element is decrypted in place.</p>\n<pre class=\"lang-mview\"><code>@* encrypt a field in the browser before the secure form is sent *@\n&lt;form @submit=\"addNote\" secure&gt;\n  &lt;textarea name=\"note\" data-mv-encrypt required&gt;&lt;/textarea&gt;\n  &lt;Button appearance=\"primary\" type=\"submit\"&gt;Encrypt &amp;amp; save&lt;/Button&gt;\n&lt;/form&gt;\n\n@* render stored ciphertext; the browser decrypts it in place on load *@\n@for n in notes {\n  &lt;span data-mv-decrypt=\"@n.ciphertext\"&gt;🔒 decrypting…&lt;/span&gt;\n}</code></pre>\n<ul><li><strong><code>EncStore</code> + <code>Audit</code></strong> — the server-side half. <code>EncStore</code> is ciphertext-only</li></ul>\n<p>storage keyed by principal, with <strong>no read-plaintext method</strong> by design. <code>Audit</code> is an append-only, upgrade-stable log: every sensitive transition (store, fetch, key derive) records principal, action, timestamp, and a content hash — never the plaintext. The canister can't read your data but can still prove who did what, when.</p>\n<ul><li><strong><code>examples/vault</code></strong> — a worked per-user encrypted notes vault: the browser</li></ul>\n<p>encrypts, the canister stores ciphertext via the vault store, every access is audited, and the browser decrypts on read. Your Motoko handler reads <code>ctx.form</code> (already ciphertext) and never sees plaintext.</p>\n<blockquote><strong>Caveat:</strong> the local replica's <code>dfx_test_key</code> charges ~26.2 billion cycles per derive (deriving is an update call, not a query); mainnet uses <code>key_1</code>. Budget cycles and cache the unwrapped vetKey for the session. See <a href=\"/docs/zero-trust#the-dfx_test_key-vs-key_1--cycle-caveat\">Zero-Trust</a>.</blockquote>\n<hr>\n<h2 id=\"theming\">Theming</h2>\n<h3 id=\"themepicker-the-fluent-theme-menu\"><code>&lt;ThemePicker /&gt;</code> — the Fluent theme menu</h3>\n<p>Drop in <code>&lt;ThemePicker /&gt;</code> for a dropdown of the official Fluent themes, exactly like the Fluent UI docs: <strong>Web Light</strong>, <strong>Web Dark</strong>, <strong>Teams Light</strong>, <strong>Teams Dark</strong>, and <strong>High Contrast</strong>. The <code>teams-*</code> themes swap the brand to Teams purple; <code>hc</code> is a black/white/yellow high-contrast token set. It uses the same <code>mv_theme</code> cookie + no-flash mechanism as the toggle (a tiny inline head script re-applies the saved theme before first paint, so it works even on certified cacheable pages), and a user's picked theme overrides the app's <code>@theme brand</code>.</p>\n<pre class=\"lang-razor\"><code>&lt;ThemePicker /&gt;</code></pre>\n<h3 id=\"themetoggle-light-dark-switch\"><code>&lt;ThemeToggle /&gt;</code> — light/dark switch</h3>\n<p>A simpler primitive for just light vs dark. Clicking it flips <code>&lt;html data-theme=\"light|dark\"&gt;</code> instantly and saves the choice in the same <code>mv_theme</code> cookie; with no saved choice it follows <code>prefers-color-scheme</code>. Like the picker, it's a framework primitive — <strong>no application JavaScript</strong>.</p>\n<pre class=\"lang-razor\"><code>&lt;ThemeToggle /&gt;</code></pre>\n<p>See <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a> for the full theming model (<code>@theme brand=\"#hex\"</code>, the built-in theme packages, and the design tokens).</p>\n<hr>\n<h2 id=\"bzzz-discourse-style-forum-clone\">Bzzz: Discourse-style forum clone</h2>\n<p>The Bzzz forum (<code>apps/bzzz</code>) was rebuilt as a faithful clone of <strong>forum.dfinity.org</strong> (stock Discourse skinned with DFINITY branding). It reproduces the real layout: the left sidebar, the latest-topics table with Topic / Replies / Views / Activity columns, category badges with the actual forum.dfinity.org colors, pinned/closed/solved glyphs, the Latest / Categories / New / Unread / Popular / Top tabs, and the topic thread + post timeline. Whole rows are clickable links (Discourse's <code>/forum/t/{id}</code> URL scheme is mirrored), not just the title. All rows are real service results — no fabricated topics.</p>\n<hr>\n<h2 id=\"bzzz-complete-role-scope-system-admin-console\">Bzzz: complete role-scope system + Admin Console</h2>\n<p>Bzzz now has a full two-scope role model:</p>\n<ul><li><strong>Global (app-wide):</strong> <code>SuperAdmin &gt; Admin &gt; Moderator &gt; None</code></li></ul>\n<p>(<code>src/Services/Admin.mo</code>). Bootstrap-safe — the first caller to claim becomes the founding Super Admin, the last Super Admin can never be demoted.</p>\n<ul><li><strong>Per-server:</strong> <code>Owner &gt; Admin &gt; Moderator &gt; Member</code></li></ul>\n<p>(<code>src/Services/Servers.mo</code>), with moderation (pin / lock / mute) gated by rank.</p>\n<ul><li><strong>Cross-scope override:</strong> a global staffer can act in <strong>any</strong> server or forum</li></ul>\n<p>even without a server role — pages gate on <code>Servers.canModerate(sid, caller) or Admin.isMod(caller)</code>.</p>\n<p>Every role transition is enforced server-side in the canister (a client can't grant itself a role), and roles persist across upgrades. The <strong>Admin Console</strong> (<code>/admin</code>) gives Super Admins a console for global staff management plus app-wide stats, with bootstrap and no-access states.</p>\n<hr>\n<h2 id=\"vibrant-fluent-ui-2-redesign-left-aligned-layout\">Vibrant Fluent UI 2 redesign + left-aligned layout</h2>\n<p>Bzzz was redesigned around the Fluent UI 2 token foundation — a more vibrant color treatment, consistent spacing/radii/shadows from the real Fluent tokens (<code>--colorBrandBackground</code>, <code>--colorNeutralForeground1</code>, the type ramp, etc.), and a left-aligned layout that reads like a real app shell rather than a centered demo.</p>\n<hr>\n<h2 id=\"expanded-fluent-component-coverage\">Expanded Fluent component coverage</h2>\n<p>A large batch of built-in Fluent components landed this round. Capitalized tags in any <code>.mview</code> resolve to these — see <a href=\"/docs/components\">Components</a>. New/expanded this round:</p>\n<p><strong>Buttons</strong></p>\n<ul><li><strong><code>Button</code></strong> — <code>appearance</code> (<code>primary</code> | <code>secondary</code> | <code>outline</code> | <code>subtle</code> |</li></ul>\n<p><code>transparent</code> | <code>danger</code>), <code>kind</code> (alias of <code>appearance</code>), <code>size</code> (<code>small</code> | <code>large</code>), <code>shape</code> (<code>rounded</code> [default] | <code>circular</code> | <code>square</code>), <code>icon</code> (literal emoji/text), <code>iconPosition</code> (<code>before</code> [default] | <code>after</code>), <code>disabled</code>, <code>type</code> (<code>button</code> [default] | <code>submit</code> | <code>reset</code>), plus event handlers (<code>on:click</code> etc., unchanged).</p>\n<ul><li><strong><code>CompoundButton</code></strong> — <code>appearance</code>, <code>kind</code> (alias), <code>size</code>, <code>secondary</code> (the</li></ul>\n<p>description line — literal or <code>@expr</code>), <code>icon</code>, <code>disabled</code>, <code>type</code>, <code>on:click</code>.</p>\n<ul><li><strong><code>ToggleButton</code></strong> — <code>appearance</code>, <code>kind</code> (alias), <code>size</code>, <code>shape</code>, <code>name</code></li></ul>\n<p>(checkbox name, submitted with forms), <code>checked</code> (initial pressed state), <code>icon</code>, <code>disabled</code>.</p>\n<ul><li><strong><code>MenuButton</code></strong> — <code>appearance</code>, <code>kind</code> (alias), <code>size</code>, <code>label</code> (button text,</li></ul>\n<p>literal or <code>@expr</code>), <code>icon</code> (before the label), <code>children</code> (MenuItem nodes).</p>\n<ul><li><strong><code>SplitButton</code></strong> — <code>appearance</code> (default <code>primary</code>), <code>kind</code> (alias), <code>label</code></li></ul>\n<p>(primary action text), <code>icon</code> (on the action half), <code>on:click</code> (fires on the <strong>primary</strong> action button), <code>children</code> (MenuItem nodes for the chevron menu).</p>\n<pre class=\"lang-razor\"><code>&lt;Button appearance=\"primary\" icon=\"💾\" @click=\"save\"&gt;Save&lt;/Button&gt;\n&lt;CompoundButton icon=\"📥\" secondary=\"Sync from server\" @click=\"pull\"&gt;Import&lt;/CompoundButton&gt;\n&lt;SplitButton label=\"Publish\" @click=\"publish\"&gt;\n  &lt;MenuItem&gt;Save draft&lt;/MenuItem&gt;\n&lt;/SplitButton&gt;</code></pre>\n<p><strong>Forms &amp; labels</strong></p>\n<ul><li><strong><code>Field</code></strong> — <code>label</code>, <code>required</code>, <code>hint</code>, <code>validationMessage</code>,</li></ul>\n<p><code>validationState</code> (<code>success</code> | <code>warning</code> | <code>error</code> | <code>none</code>).</p>\n<ul><li><strong><code>Label</code></strong> — <code>size</code> (<code>small</code> | <code>medium</code> | <code>large</code>), <code>weight</code></li></ul>\n<p>(<code>regular</code> | <code>semibold</code>), <code>required</code>, <code>htmlFor</code>, <code>for</code>, <code>text</code>.</p>\n<ul><li><strong><code>InfoLabel</code></strong> — <code>label</code>, <code>info</code>, <code>weight</code> (<code>regular</code> | <code>semibold</code>).</li><li><strong><code>SpinButton</code></strong> — <code>name</code>, <code>label</code>, <code>value</code>, <code>min</code>, <code>max</code>, <code>step</code>, <code>disabled</code>.</li><li><strong><code>Slider</code></strong> — <code>name</code>, <code>value</code>, <code>min</code>, <code>max</code>, <code>step</code>, <code>vertical</code>, <code>disabled</code>.</li><li><strong><code>Input</code></strong> — <code>name</code>, <code>type</code>, <code>placeholder</code>, <code>value</code>, <code>size</code></li></ul>\n<p>(<code>small</code> | <code>large</code>), <code>appearance</code> (<code>outline</code> | <code>underline</code> | <code>filled-lighter</code> | <code>filled-darker</code>), <code>disabled</code>, <code>required</code>.</p>\n<p><strong>Badges &amp; status</strong></p>\n<ul><li><strong><code>CounterBadge</code></strong> — <code>count</code>, <code>appearance</code> (<code>filled</code> | <code>ghost</code> | <code>outline</code>),</li></ul>\n<p><code>color</code> (<code>brand</code> | <code>danger</code> | <code>important</code> | <code>informative</code> | <code>severe</code> | <code>subtle</code> | <code>success</code> | <code>warning</code>), <code>size</code> (<code>tiny</code> → <code>extra-large</code>), <code>showZero</code>, <code>dot</code>.</p>\n<ul><li><strong><code>PresenceBadge</code></strong> — <code>status</code> (<code>available</code> | <code>away</code> | <code>busy</code> | <code>dnd</code> |</li></ul>\n<p><code>offline</code> | <code>out-of-office</code>), <code>size</code>, <code>outOfOffice</code>.</p>\n<ul><li><strong><code>Avatar</code></strong> — <code>name</code>, <code>size</code>, <code>shape</code>, <code>presence</code>, <code>text</code>, <code>color</code>, <code>active</code>,</li></ul>\n<p><code>badge</code>.</p>\n<ul><li><strong><code>Badge</code></strong> — <code>title</code>, <code>type</code>, <code>appearance</code>, <code>shape</code>, <code>size</code>, <code>color</code>.</li><li><strong><code>Spinner</code></strong> — <code>size</code>, <code>label</code>, <code>labelPosition</code>.</li><li><strong><code>ProgressBar</code></strong> — <code>value</code>, <code>max</code>, <code>color</code>, <code>validationState</code>, <code>thickness</code>,</li></ul>\n<p><code>shape</code>.</p>\n<p><strong>Typography (Fluent type ramp)</strong></p>\n<ul><li><strong><code>Display</code></strong>, <strong><code>Title1</code></strong> / <strong><code>Title2</code></strong> / <strong><code>Title3</code></strong>,</li></ul>\n<p><strong><code>Subtitle1</code></strong> / <strong><code>Subtitle2</code></strong>, <strong><code>Body1</code></strong> / <strong><code>Body2</code></strong> (<code>strong</code>), <strong><code>Caption1</code></strong> / <strong><code>Caption2</code></strong> (<code>strong</code>).</p>\n<p><strong>Cards, navigation &amp; containers</strong></p>\n<ul><li><strong><code>CardHeader</code></strong> — <code>header</code>, <code>description</code>, children = leading image/avatar slot.</li><li><strong><code>CardPreview</code></strong> — <code>src</code>, <code>alt</code>, children = custom media.</li><li><strong><code>CardFooter</code></strong> — children = action buttons / controls.</li><li><strong><code>Tree</code></strong> / <strong><code>TreeItem</code></strong> (<code>label</code>, <code>open</code>).</li><li><strong><code>Toolbar</code></strong> (<code>size</code>), <strong><code>Carousel</code></strong>.</li><li><strong><code>TagGroup</code></strong> (<code>size</code>), <strong><code>Tag</code></strong> (<code>value</code>, <code>dismissible</code>, <code>interactive</code>,</li></ul>\n<p><code>appearance</code>, <code>size</code>, <code>shape</code>), <strong><code>InteractionTag</code></strong> (<code>value</code>, <code>dismissible</code>, <code>size</code>, <code>@click</code>).</p>\n<ul><li><strong><code>MenuItemCheckbox</code></strong> / <strong><code>MenuItemRadio</code></strong> (<code>name</code>, <code>value</code>, <code>checked</code>).</li></ul>\n<pre class=\"lang-razor\"><code>&lt;Card&gt;\n  &lt;CardHeader header=\"Release 0.4\" description=\"Zero-trust + Fluent batch\" /&gt;\n  &lt;CardPreview src=\"/shot.png\" alt=\"screenshot\" /&gt;\n  &lt;CardFooter&gt;\n    &lt;Button appearance=\"primary\"&gt;Open&lt;/Button&gt;\n  &lt;/CardFooter&gt;\n&lt;/Card&gt;</code></pre>\n<hr>\n<h2 id=\"compiler-robustness\">Compiler robustness</h2>\n<ul><li><strong>Keyword-safe component params.</strong> A component <code>param</code> named after a Motoko</li></ul>\n<p>reserved word (e.g. <code>type</code>, <code>label</code>, <code>for</code>, <code>class</code>) is now <strong>auto-mangled</strong> to a guaranteed-safe <code>mvP_&lt;name&gt;</code> identifier in the generated Motoko, while the prop name you write stays the same. Any param name works — you find out at build time that nothing breaks, instead of hitting an <code>moc</code> error. (See <code>compiler/src/codegen.rs</code>, <code>mangle_param</code>.)</p>\n<ul><li><strong>Arbitrary component nesting</strong> with props + <code>@children</code> is verified — deeply</li></ul>\n<p>nested capitalized components passing typed props and yielding child content compile and render correctly.</p>\n<hr>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li><a href=\"/docs/zero-trust\">Zero-Trust</a> — the full encrypted-storage architecture and</li></ul>\n<p>threat model.</p>\n<ul><li><a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a> — <code>@theme</code>, the theme packages,</li></ul>\n<p><code>&lt;ThemePicker /&gt;</code> / <code>&lt;ThemeToggle /&gt;</code>.</p>\n<ul><li><a href=\"/docs/components\">Components</a> — authoring components and the built-in catalog.</li></ul>"; prevSlug = "why-motoview"; prevTitle = "Why MotoView"; nextSlug = "installation"; nextTitle = "Installation" } };
        case ("installation") { ?{ slug = "installation"; title = "Installation"; section = "Getting Started"; html = "<h1 id=\"installation\">Installation</h1>\n<p>MotoView compiles your <code>.mview</code> files to Motoko and deploys them to an Internet Computer canister with <code>dfx</code>. There is no Node, no npm, and no JavaScript build step to set up — that absence is the whole point. The toolchain is just three things: the DFINITY SDK, a Rust toolchain, and the <code>motoview</code> compiler.</p>\n<p>This page walks you from an empty machine to a verified toolchain. When you're ready to build something, head to the <a href=\"/docs/quickstart\">Quickstart</a>.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>You need three tools on your <code>PATH</code> before installing MotoView.</p>\n<h3 id=\"dfx-dfinity-sdk\">dfx (DFINITY SDK)</h3>\n<p><code>dfx</code> builds and deploys your canister to a local replica or to mainnet.</p>\n<pre class=\"lang-bash\"><code>sh -ci \"$(curl -fsSL https://internetcomputer.org/install.sh)\"\ndfx --version</code></pre>\n<h3 id=\"rust-and-the-webassembly-target\">Rust and the WebAssembly target</h3>\n<p>MotoView's browser client is written in Rust and compiled to WebAssembly — it's the only browser-side code in the framework. You need <code>rustup</code> and the <code>wasm32-unknown-unknown</code> target.</p>\n<pre class=\"lang-bash\"><code>curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh\nrustup target add wasm32-unknown-unknown</code></pre>\n<h3 id=\"wasm-opt-binaryen\">wasm-opt (binaryen)</h3>\n<p>The WASM client is optimized with <code>wasm-opt</code> from the <a href=\"https://github.com/WebAssembly/binaryen\">binaryen</a> toolkit.</p>\n<pre class=\"lang-bash\"><code># macOS\nbrew install binaryen\n\n# Debian/Ubuntu\napt-get install binaryen</code></pre>\n<p>Confirm it resolves:</p>\n<pre class=\"lang-bash\"><code>wasm-opt --version</code></pre>\n<h2 id=\"installing-the-compiler\">Installing the compiler</h2>\n<p>The <code>motoview</code> binary is a Rust crate. Build and install it with <code>cargo</code>, which <code>rustup</code> provided above.</p>\n<pre class=\"lang-bash\"><code>cargo install --git https://github.com/your-org/motoview motoview</code></pre>\n<p>If you're working from a local checkout of the repository instead:</p>\n<pre class=\"lang-bash\"><code>cargo install --path compiler</code></pre>\n<p>Verify the install:</p>\n<pre class=\"lang-bash\"><code>motoview --version</code></pre>\n<p>You now have the four CLI commands available — <code>motoview new</code>, <code>motoview build</code>, <code>motoview dev</code>, and <code>motoview compile</code>. See the <a href=\"/docs/cli\">CLI reference</a> for details.</p>\n<h2 id=\"adding-the-runtime\">Adding the runtime</h2>\n<p>The compiler turns <code>.mview</code> files into Motoko, but the generated actor depends on the MotoView <strong>runtime</strong> — the Motoko library that serves HTTP from your canister, interprets events, and emits versioned UI batches.</p>\n<p>The recommended way to add it is through <a href=\"https://mops.one\">mops</a>, the Motoko package manager. In your <code>mops.toml</code>:</p>\n<pre class=\"lang-toml\"><code>[dependencies]\nmotoview = \"0.1.0\"</code></pre>\n<p>Then install:</p>\n<pre class=\"lang-bash\"><code>mops install</code></pre>\n<p>If you're developing against a local checkout of the runtime, point <code>dfx.json</code> at it directly with a package argument instead of using mops:</p>\n<pre class=\"lang-json\"><code>{\n  \"canisters\": {\n    \"app\": {\n      \"type\": \"motoko\",\n      \"main\": \".mvbuild/main.mo\",\n      \"args\": \"--package motoview ../../runtime/src\"\n    }\n  }\n}</code></pre>\n<p>Both approaches make <code>import MotoView \"mo:motoview\"</code> resolve when the generated actor is compiled.</p>\n<h2 id=\"verifying-the-toolchain\">Verifying the toolchain</h2>\n<p>Scaffold a project and run it once to confirm every piece is wired up correctly.</p>\n<pre class=\"lang-bash\"><code>motoview new hello\ncd hello\nmotoview dev</code></pre>\n<p><code>motoview dev</code> compiles your <code>.mview</code> files to Motoko, runs <code>dfx deploy</code> against the local replica, and watches for changes. When it finishes it prints the canister URL. Open it in a browser and you should see the rendered page served straight from the canister at <code>&lt;canister-url&gt;/</code>.</p>\n<p>The scaffold ships with a counter page. Clicking the button sends an event to the canister's update method and swaps in a new batch — this is the same <code>event -&gt; update -&gt; batch -&gt; DOM swap</code> cycle described in <a href=\"/docs/events\">Events</a>, and it's verified end to end. If the count increments in the browser, your install is good.</p>\n<p>To compile a single file without deploying — handy when debugging a template — use:</p>\n<pre class=\"lang-bash\"><code>motoview compile src/Pages/Index.mview</code></pre>\n<p>A clean compile and a working counter confirm all four moving parts agree: the <code>dfx</code> replica, the Rust-built WASM client served at <code>/motoview.wasm</code>, the runtime serving the <code>motoview/1</code> protocol, and the compiler that generated the actor.</p>\n<h2 id=\"next-steps\">Next steps</h2>\n<ul><li><a href=\"/docs/quickstart\">Quickstart</a> — build your first page from scratch.</li><li><a href=\"/docs/project-structure\">Project Layout</a> — where <code>.mview</code> files, services, and config live.</li><li><a href=\"/docs/events\">Events</a> — how <code>@click</code>, <code>@submit</code>, and <code>@input</code> flow to typed Motoko handlers.</li></ul>"; prevSlug = "whats-new"; prevTitle = "What's New"; nextSlug = "quickstart"; nextTitle = "Quickstart" } };
        case ("quickstart") { ?{ slug = "quickstart"; title = "Quickstart"; section = "Getting Started"; html = "<h1 id=\"quickstart\">Quickstart</h1>\n<p>This guide takes you from an empty folder to a running, interactive counter in a real Internet Computer canister. You will scaffold a project, tour what was created, deploy to a local replica, click a button in the browser, and then edit a handler and watch the change take effect.</p>\n<p>If you have not installed the prerequisites and the <code>motoview</code> compiler yet, start with <a href=\"/docs/installation\">Installation</a>. You need <code>dfx</code>, <code>rustup</code> with the <code>wasm32-unknown-unknown</code> target, <code>wasm-opt</code>, and the <code>motoview</code> binary on your <code>PATH</code>.</p>\n<h2 id=\"create-a-project\">Create a project</h2>\n<p>Scaffold a new app with <code>motoview new</code>:</p>\n<pre class=\"lang-bash\"><code>motoview new hello-counter\ncd hello-counter</code></pre>\n<p>This generates a complete project: pages, a layout, the runtime dependency, and the config files <code>dfx</code> needs.</p>\n<h2 id=\"project-tour\">Project tour</h2>\n<p>Here is what <code>motoview new</code> lays down:</p>\n<pre class=\"lang-text\"><code>hello-counter/\n├── src/\n│   ├── Pages/        # .mview pages — one file per route\n│   ├── Layouts/      # shared shells (@yield, @section)\n│   ├── Components/   # reusable .mview components\n│   ├── Services/     # plain Motoko (*.mo) business logic\n│   └── Models/       # plain Motoko (*.mo) record/variant types\n├── motoview.json     # MotoView project config\n├── dfx.json          # canister + replica config for dfx\n└── mops.toml         # Motoko package manifest (pulls in \"motoview\")</code></pre>\n<p>A page is a single <code>.mview</code> file holding template markup plus a <code>@code</code> block of Motoko. There is no separate JavaScript project, no React, and no <code>agent-js</code> glue to wire up — the only browser code is the MotoView WASM client, which the runtime serves for you.</p>\n<h2 id=\"run-it\">Run it</h2>\n<p>Start the dev loop:</p>\n<pre class=\"lang-bash\"><code>motoview dev</code></pre>\n<p><code>motoview dev</code> compiles your <code>.mview</code> files to Motoko, deploys to a local <code>dfx</code> replica, and watches for changes. When it finishes, it prints the canister URL. Open it in your browser:</p>\n<pre class=\"lang-text\"><code>http://&lt;canister-id&gt;.localhost:4955/</code></pre>\n<p>The first load is plain server-rendered HTML — the page content lives in a <code>&lt;div id=\"mv-root\"&gt;</code>. That is what makes MotoView pages SEO-friendly: the markup arrives complete, then the WASM client takes over and keeps it in sync.</p>\n<h2 id=\"the-counter-page\">The Counter page</h2>\n<p>Open <code>src/Pages/Counter.mview</code>. It is the whole counter — template and logic in one file:</p>\n<pre class=\"lang-razor\"><code>@page \"/\"\n@title \"Counter\"\n\n@code {\n    stable var count : Nat = 0;\n\n    func increment() {\n        count += 1;\n    };\n\n    func decrement() {\n        if (count &gt; 0) { count -= 1; };\n    };\n}\n\n&lt;Card title=\"Counter\"&gt;\n    &lt;PageHeader&gt;You clicked @count times.&lt;/PageHeader&gt;\n\n    &lt;Button kind=\"primary\" @click=\"increment\"&gt;Increment&lt;/Button&gt;\n    &lt;Button kind=\"secondary\" @click=\"decrement\"&gt;Decrement&lt;/Button&gt;\n&lt;/Card&gt;</code></pre>\n<p>A few things are worth pointing out:</p>\n<ul><li><code>@count</code> outputs the current value inline. Any <code>@expr</code> or <code>@(expr)</code> is evaluated server-side at render time.</li><li><code>@click=\"increment\"</code> binds the click to the typed Motoko function <code>increment</code>. You never write event-wiring code.</li><li><code>stable var count</code> persists across calls — refresh the page and your count is still there.</li></ul>\n<h2 id=\"see-it-update\">See it update</h2>\n<p>Click <strong>Increment</strong> in the browser. Under the hood, the WASM client posts the event to <code>/_motoview/event</code>; the canister runs <code>increment</code>, re-renders, and returns a new versioned batch; the client swaps the changed DOM. The click-to-update round trip happens immediately — no waiting for the next poll.</p>\n<p>Adaptive polling also keeps the page fresh on its own: it runs hot (~350ms) for a few seconds after you interact, settles to warm (~2.5s) while the tab is visible, and backs off to cold (~15s) when idle. So if <code>count</code> changes from another caller, the page picks it up. For the full state machine and protocol, see <a href=\"/docs/protocol\">Architecture</a>.</p>\n<h2 id=\"edit-a-handler-watch-it-change\">Edit a handler, watch it change</h2>\n<p>With <code>motoview dev</code> still running, change <code>increment</code> to step by two:</p>\n<pre class=\"lang-razor\"><code>    func increment() {\n        count += 2;\n    };</code></pre>\n<p>Save the file. <code>motoview dev</code> recompiles and redeploys automatically. Refresh the browser, click <strong>Increment</strong>, and the count now jumps by two.</p>\n<p>That is the core loop: write Motoko, the markup renders on the server, events dispatch to typed functions, and the browser stays in sync through versioned batches — all without a line of application JavaScript.</p>\n<h2 id=\"next-steps\">Next steps</h2>\n<ul><li><a href=\"/docs/pages-and-routing\">Pages and Routing</a> — <code>@page</code>, typed route params like <code>/orders/{id:Nat}</code>, and layouts.</li><li><a href=\"/docs/events\">Events</a> — handler arguments, <code>@input</code>, <code>@change</code>, and <code>@submit</code>.</li><li><a href=\"/docs/forms\">Forms and Validation</a> — <code>secure</code> forms, <code>bind=\"@model.field\"</code>, and <code>validate</code>.</li><li><a href=\"/docs/components\">Components</a> — the built-in semantic components and building your own.</li></ul>"; prevSlug = "installation"; prevTitle = "Installation"; nextSlug = "project-structure"; nextTitle = "Project Structure" } };
        case ("project-structure") { ?{ slug = "project-structure"; title = "Project Structure"; section = "Getting Started"; html = "<h1 id=\"project-structure\">Project Structure</h1>\n<p>When you scaffold an app with <code>motoview new</code>, you get a layout that maps cleanly onto how MotoView thinks about an application: templates that render, code that runs on the canister, and configuration that ties it all to ICP. Everything is Motoko-native — there is no Node, no npm, and no JavaScript build step to manage.</p>\n<pre class=\"lang-bash\"><code>motoview new shop</code></pre>\n<pre class=\"lang-text\"><code>shop/\n├── motoview.json\n├── dfx.json\n├── mops.toml\n└── src/\n    ├── Pages/          # routable .mview templates\n    ├── Layouts/        # shared page shells\n    ├── Components/      # reusable .mview components\n    ├── Services/        # plain Motoko (*.mo)\n    ├── Models/          # plain Motoko types (*.mo)\n    └── Generated/       # compiler output (do not edit)</code></pre>\n<h2 id=\"src-pages\">src/Pages</h2>\n<p>Pages are <code>.mview</code> files that own a route. A page declares its URL with <code>@page</code>, optionally an SEO title and description, and contains template markup plus a <code>@code</code> block of Motoko.</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id:Nat}\"\n@layout Main\n@title \"Product \" # Nat.toText(id)\n\n&lt;PageHeader title=@product.name /&gt;\n&lt;p&gt;@product.summary&lt;/p&gt;\n\n@code {\n  var product = Catalog.get(id);\n}</code></pre>\n<p>Routes can be static (<code>@page \"/about\"</code>), dynamic (<code>@page \"/products/{id}\"</code>), or typed (<code>@page \"/orders/{id:Nat}\"</code>). See <a href=\"/docs/routing\">Routing</a> for the full path syntax.</p>\n<h2 id=\"src-layouts\">src/Layouts</h2>\n<p>Layouts are the shared shell around your pages — the <code>&lt;html&gt;</code>, navigation, and footer that you do not want to repeat. A layout marks where the page drops in with <code>@yield</code>, and can expose named regions with <code>@section</code>.</p>\n<pre class=\"lang-razor\"><code>@code { }\n\n&lt;!doctype html&gt;\n&lt;html&gt;\n  &lt;head&gt;@head&lt;/head&gt;\n  &lt;body&gt;\n    &lt;nav&gt;...&lt;/nav&gt;\n    &lt;main&gt;@yield&lt;/main&gt;\n  &lt;/body&gt;\n&lt;/html&gt;</code></pre>\n<p>A page opts in with <code>@layout Main</code>. Pages without a <code>@layout</code> directive render standalone.</p>\n<h2 id=\"src-components\">src/Components</h2>\n<p>Capitalized tags in your templates are components. App components live in <code>src/Components/*.mview</code> and declare typed parameters with <code>param</code>.</p>\n<pre class=\"lang-razor\"><code>param title : Text\nparam featured : Bool = false\n\n&lt;Card title=@title&gt;\n  @if featured { &lt;Badge type=\"success\"&gt;Featured&lt;/Badge&gt; }\n&lt;/Card&gt;</code></pre>\n<pre class=\"lang-razor\"><code>&lt;ProductCard title=@product.name featured=true /&gt;</code></pre>\n<p>Alongside your own components, MotoView ships built-in semantic components — <code>Button</code>, <code>Card</code>, <code>Alert</code>, <code>Badge</code>, <code>InputText</code>, <code>InputEmail</code>, <code>InputNumber</code>, <code>TextArea</code>, <code>ValidationSummary</code>, <code>Table</code>, <code>PageHeader</code>, and <code>Grid</code> — so you can write <code>&lt;Button kind=\"primary\"&gt;Save&lt;/Button&gt;</code> instead of long class strings. See <a href=\"/docs/components\">Components</a>.</p>\n<h2 id=\"src-services-and-src-models\">src/Services and src/Models</h2>\n<p>Not everything belongs in a template. <code>src/Services/<em>.mo</code> and <code>src/Models/</em>.mo</code> are plain Motoko files that you import from your <code>@code</code> blocks.</p>\n<ul><li><strong>Models</strong> hold your types and records — the shapes your pages bind to and your forms validate against.</li><li><strong>Services</strong> hold business logic, stable state, and the functions your pages call.</li></ul>\n<pre class=\"lang-motoko\"><code>// src/Models/Product.mo\npublic type Product = {\n  id : Nat;\n  name : Text;\n  summary : Text;\n};</code></pre>\n<pre class=\"lang-motoko\"><code>// src/Services/Catalog.mo\nimport Product \"../Models/Product\";\n\nmodule {\n  public func get(id : Nat) : Product.Product { ... };\n}</code></pre>\n<h2 id=\"mvbuild-generated-never-edit-or-commit\">.mvbuild (generated — never edit or commit)</h2>\n<p><code>motoview build</code> parses every <code>.mview</code> file and emits a single Motoko actor to <code>.mvbuild/main.mo</code>. <strong>This is a build artifact, not source</strong> — exactly like Blazor's <code>obj/</code> or a JS bundler's <code>dist/</code>. It is gitignored, never edited, and regenerated on every build; <code>dfx.json</code> points its canister <code>main</code> at it. You never read it: you write <code>.mview</code> files, and <code>motoview check</code> maps any <code>moc</code> error back to the originating <code>.mview</code> (via the <code>// mv:src</code> markers in the generated file). Your hand-written Motoko lives in <code>Services/</code> and <code>Models/</code>, which the compiler imports into the actor.</p>\n<h2 id=\"motoview-json\">motoview.json</h2>\n<p>The project config. It declares your app metadata and where the compiler should look and write.</p>\n<pre class=\"lang-json\"><code>{\n  \"name\": \"shop\",\n  \"version\": \"0.1.0\",\n  \"src\": \"src\",\n  \"out\": \"src/Generated\"\n}</code></pre>\n<h2 id=\"dfx-json\">dfx.json</h2>\n<p>The standard DFINITY SDK manifest. It defines the canister, points dfx at the generated <code>main.mo</code>, and adds the runtime as a package argument.</p>\n<pre class=\"lang-json\"><code>{\n  \"canisters\": {\n    \"shop\": {\n      \"type\": \"motoko\",\n      \"main\": \".mvbuild/main.mo\",\n      \"args\": \"--package motoview ../../runtime/src\"\n    }\n  }\n}</code></pre>\n<h2 id=\"mops-toml\">mops.toml</h2>\n<p>MotoView ships its runtime as the <a href=\"https://mops.one\">mops</a> package <code>motoview</code>. Declare it here so the runtime that serves HTTP from your canister is on the dependency path.</p>\n<pre class=\"lang-toml\"><code>[dependencies]\nmotoview = \"...\"</code></pre>\n<p>With the structure in place, run <code>motoview dev</code> to build, deploy to a local replica, and watch for changes. Next, head to <a href=\"/docs/events\">Events</a> to wire up <code>@click</code> and <code>@submit</code>.</p>"; prevSlug = "quickstart"; prevTitle = "Quickstart"; nextSlug = "mview-files"; nextTitle = "The .mview File" } };
        case ("mview-files") { ?{ slug = "mview-files"; title = "The .mview File"; section = "The .mview Format"; html = "<h1 id=\"the-mview-file\">The .mview File</h1>\n<p>A <code>.mview</code> file is the heart of MotoView. It holds your <strong>template markup</strong> and your <strong>Motoko code</strong> side by side in a single file — the way a Blade or Razor view does, but the code is real, type-checked Motoko that runs inside your Internet Computer canister.</p>\n<p>If you have written Blazor, Blade, or Phoenix LiveView, this will feel immediately familiar: the markup describes what to render, the code says how to compute it, and MotoView's compiler stitches the two together into a Motoko actor.</p>\n<h2 id=\"anatomy-of-a-mview-file\">Anatomy of a .mview file</h2>\n<p>Here is a small but complete page. It declares a route, sets the page title, holds some state, renders markup, and handles a click — all in one file.</p>\n<pre class=\"lang-razor\"><code>@page \"/counter\"\n@title \"Counter\"\n\n@code {\n    var count : Nat = 0;\n\n    public func increment() {\n        count += 1;\n    };\n}\n\n&lt;PageHeader title=\"Counter\" /&gt;\n\n&lt;Card&gt;\n    &lt;p&gt;The count is @count.&lt;/p&gt;\n    &lt;Button kind=\"primary\" @click=\"increment\"&gt;Add one&lt;/Button&gt;\n&lt;/Card&gt;</code></pre>\n<p>Three things are happening:</p>\n<ul><li><strong>Directives</strong> (lines starting with <code>@</code>) configure the page — its route, metadata, layout, and embedded code.</li><li>The <strong><code>@code</code> block</strong> holds plain Motoko: state (<code>var count</code>) and handlers (<code>public func increment</code>).</li><li>The <strong>template</strong> below is HTML plus MotoView tags, components, and inline output like <code>@count</code>.</li></ul>\n<p>When the browser clicks the button, the WASM client forwards the event to the canister, <code>increment()</code> runs, the page re-renders, and the new state flows back as a versioned batch. See <a href=\"/docs/events\">Events</a> for how <code>@click</code> is wired end to end.</p>\n<h2 id=\"the-directive-list\">The directive list</h2>\n<p>Directives always begin with <code>@</code> and configure the page or its structure.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>What it does</th></tr></thead><tbody><tr><td><code>@page \"/path\"</code></td><td>Declares the route. Supports params: <code>\"/products/{id}\"</code> and typed <code>\"/orders/{id:Nat}\"</code>.</td></tr><tr><td><code>@layout NAME</code></td><td>Renders this page inside the named layout.</td></tr><tr><td><code>@title EXPR</code></td><td>Sets the document title (used for SEO).</td></tr><tr><td><code>@description EXPR</code></td><td>Sets the meta description.</td></tr><tr><td><code>@canonical EXPR</code></td><td>Sets the canonical URL.</td></tr><tr><td><code>@meta</code></td><td>Emits additional meta tags.</td></tr><tr><td><code>@authorize</code></td><td>Requires an authenticated caller; <code>@authorize role=\"Admin\"</code> requires a role.</td></tr><tr><td><code>@section \"name\" { ... }</code></td><td>Defines content a layout pulls in by name.</td></tr><tr><td><code>@yield</code></td><td>In a layout, the slot where page content is rendered.</td></tr><tr><td><code>@head</code></td><td>Injects markup into the document <code>&lt;head&gt;</code>.</td></tr><tr><td><code>@slot \"name\"</code></td><td>A named insertion point a component can fill.</td></tr><tr><td><code>@code { ... }</code></td><td>Embeds Motoko: state, handlers, and helpers.</td></tr><tr><td><code>@style { ... }</code></td><td>Scoped CSS for this view.</td></tr><tr><td><code>@theme { tokens }</code></td><td>Declares theme tokens.</td></tr><tr><td><code>@if EXPR { } else { }</code></td><td>Conditional rendering.</td></tr><tr><td><code>@for X in EXPR { }</code></td><td>Loops over a collection.</td></tr><tr><td><code>@switch EXPR { case #Variant { } }</code></td><td>Matches a Motoko variant.</td></tr><tr><td><code>@count</code> / <code>@user.name</code> / <code>@(expr)</code></td><td>Inline output of a value or expression.</td></tr><tr><td><code>@effect Focus(\"#x\")</code> / <code>ScrollTo</code> / <code>Toast</code></td><td>Requests a client-side effect after render.</td></tr><tr><td><code>@animate</code></td><td>Marks an element for animation.</td></tr></tbody></table>\n<p>For the full event set (<code>@click</code>, <code>@submit</code>, <code>@input</code>, <code>@change</code>) and how handler arguments are baked in, see <a href=\"/docs/events\">Events</a>.</p>\n<h2 id=\"how-template-and-code-combine\">How template and @code combine</h2>\n<p>The <code>@code</code> block and the template are two views of the same actor. Anything you declare in <code>@code</code> is in scope in the template.</p>\n<pre class=\"lang-razor\"><code>@code {\n    var products : [Product] = Store.all();\n\n    func total() : Nat {\n        Array.size(products);\n    };\n}\n\n&lt;PageHeader title=\"Products\" /&gt;\n&lt;p&gt;We have @total() items.&lt;/p&gt;\n\n@for p in products {\n    &lt;Card title=\"@p.name\"&gt;\n        &lt;p&gt;@(p.price) credits&lt;/p&gt;\n    &lt;/Card&gt;\n}</code></pre>\n<p><code>products</code> and <code>total()</code> are ordinary Motoko members, used directly as <code>@total()</code> and looped over with <code>@for</code>. The compiler generates a Motoko actor where the template becomes a render function and the <code>@code</code> members become actor state and methods — so rendering is a query over your state, and events are updates to it.</p>\n<p>Because everything is Motoko, your validation, types, and business logic live in one place. There is no JavaScript layer to keep in sync.</p>\n<h2 id=\"where-files-live\">Where files live</h2>\n<p>Pages, layouts, and components each have a home:</p>\n<pre class=\"lang-bash\"><code>src/Pages/Counter.mview      # @page routes\nsrc/Layouts/Main.mview       # @layout targets\nsrc/Components/Card.mview     # reusable components</code></pre>\n<p>Run the compiler to turn them into Motoko:</p>\n<pre class=\"lang-bash\"><code>motoview build</code></pre>\n<p>Next, read about <a href=\"/docs/events\">Events</a> to handle user input, or <a href=\"/docs/components\">Components</a> to build reusable pieces.</p>"; prevSlug = "project-structure"; prevTitle = "Project Structure"; nextSlug = "pages-and-routing"; nextTitle = "Pages & Routing" } };
        case ("pages-and-routing") { ?{ slug = "pages-and-routing"; title = "Pages & Routing"; section = "The .mview Format"; html = "<h1 id=\"pages-routing\">Pages &amp; Routing</h1>\n<p>Every screen in a MotoView app is a <code>.mview</code> file under <code>src/Pages/</code>. A page is template markup plus the Motoko that backs it, living side by side in one file. The first directive you reach for is <code>@page</code>, which maps a URL to that file.</p>\n<pre class=\"lang-razor\"><code>@page \"/about\"\n\n@title \"About us\"\n\n&lt;PageHeader&gt;About us&lt;/PageHeader&gt;\n&lt;p&gt;MotoView renders this from your canister. No frontend JavaScript.&lt;/p&gt;</code></pre>\n<p>When the browser requests <code>/about</code>, the canister server-renders the HTML into <code>&lt;div id=\"mv-root\"&gt;</code> and ships it. From there the WASM client keeps the page in sync. See <a href=\"/docs/events\">Events</a> for how interactions flow back to your Motoko handlers.</p>\n<h2 id=\"route-parameters\">Route Parameters</h2>\n<p>Routes can capture segments with curly braces. Use <code>{name}</code> for a string segment, or <code>{name:Nat}</code> to require and parse a typed value.</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id}\"</code></pre>\n<pre class=\"lang-razor\"><code>@page \"/orders/{id:Nat}\"</code></pre>\n<p>With <code>{id}</code> the captured segment arrives as <code>Text</code>. With <code>{id:Nat}</code> MotoView parses the segment for you; a request whose segment is not a valid <code>Nat</code> does not match the route.</p>\n<h2 id=\"reading-params-in-code\">Reading Params in @code</h2>\n<p>Route parameters are available to your page logic inside <code>@code</code>. Read them, load your data, and expose values to the template.</p>\n<pre class=\"lang-razor\"><code>@page \"/orders/{id:Nat}\"\n@title \"Order #\" # Nat.toText(id)\n\n@code {\n  import Nat \"mo:base/Nat\";\n  import Orders \"../Services/Orders\";\n\n  let order = Orders.find(id);\n}\n\n@switch order {\n  case (?o) {\n    &lt;PageHeader&gt;Order #@Nat.toText(o.id)&lt;/PageHeader&gt;\n    &lt;Card title=\"Summary\"&gt;\n      &lt;p&gt;Total: @(Nat.toText(o.total))&lt;/p&gt;\n    &lt;/Card&gt;\n  }\n  case null {\n    &lt;Alert type=\"warning\"&gt;Order not found.&lt;/Alert&gt;\n  }\n}</code></pre>\n<p>Because <code>id</code> was declared <code>{id:Nat}</code>, it is already a <code>Nat</code> here — no manual <code>Text</code> parsing, no failed conversions to guard against.</p>\n<h2 id=\"authorization\">Authorization</h2>\n<p>Add <code>@authorize</code> to require an authenticated caller before the page renders. Pass <code>role=\"...\"</code> to require a specific role.</p>\n<pre class=\"lang-razor\"><code>@page \"/admin\"\n@authorize role=\"Admin\"\n\n&lt;PageHeader&gt;Admin&lt;/PageHeader&gt;</code></pre>\n<blockquote>Full Internet Identity login over HTTP and role stores are <strong>Roadmap</strong>. <code>@authorize</code> is the directive you'll use; the backing identity and role infrastructure is still being built out.</blockquote>\n<h2 id=\"seo-directives\">SEO Directives</h2>\n<p>MotoView renders real HTML on the server, so pages are crawlable and shareable out of the box. The SEO directives let you fill in the document head. Each takes a Motoko expression, so titles and descriptions can be computed from your data.</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id:Nat}\"\n\n@code {\n  import Nat \"mo:base/Nat\";\n  import Catalog \"../Services/Catalog\";\n\n  let product = Catalog.get(id);\n}\n\n@title product.name # \" — Acme Store\"\n@description \"Buy \" # product.name # \" on Acme, deployed on the Internet Computer.\"\n@canonical \"https://acme.example/products/\" # Nat.toText(id)\n\n&lt;PageHeader&gt;@product.name&lt;/PageHeader&gt;</code></pre>\n<p>The available head directives are:</p>\n<ul><li><code>@title EXPR</code> — sets the document <code>&lt;title&gt;</code>.</li><li><code>@description EXPR</code> — the meta description.</li><li><code>@canonical EXPR</code> — the canonical URL.</li><li><code>@meta</code> — additional meta tags.</li></ul>\n<p>For anything else you need in <code>&lt;head&gt;</code> — Open Graph tags, structured data — define a <code>@section \"head\"</code> block on the page. Its markup is collected into the document head where the layout places <code>@head</code>.</p>\n<pre class=\"lang-razor\"><code>@section \"head\" {\n  &lt;meta property=\"og:type\" content=\"product\" /&gt;\n  &lt;meta property=\"og:title\" content=\"@product.name\" /&gt;\n}</code></pre>\n<p>(In a layout, <code>@head</code> is the placeholder that receives the page title, meta description, canonical link, and any <code>@section \"head\"</code> content.)</p>\n<h2 id=\"putting-it-together\">Putting It Together</h2>\n<p>A typical detail page combines a typed route, a data load in <code>@code</code>, computed SEO, and a template that handles the missing case.</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id:Nat}\"\n@layout Main\n\n@code {\n  import Nat \"mo:base/Nat\";\n  import Catalog \"../Services/Catalog\";\n\n  let product = Catalog.get(id);\n}\n\n@title @switch product { case (?p) p.name; case null \"Not found\" }\n@description \"Product details on Acme.\"\n\n@switch product {\n  case (?p) {\n    &lt;PageHeader&gt;@p.name&lt;/PageHeader&gt;\n    &lt;p&gt;@(Nat.toText(p.price))&lt;/p&gt;\n  }\n  case null {\n    &lt;Alert type=\"warning\"&gt;That product doesn't exist.&lt;/Alert&gt;\n  }\n}</code></pre>\n<h2 id=\"building-your-routes\">Building Your Routes</h2>\n<p>Compile your <code>.mview</code> files to Motoko, then deploy with <code>dfx</code>:</p>\n<pre class=\"lang-bash\"><code>motoview build\nmotoview dev</code></pre>\n<p><code>motoview dev</code> builds, deploys to your local replica, and watches for changes. Once it's running, open the route in your browser and the server-rendered HTML is there immediately.</p>\n<p>Next, wire up interactivity with <a href=\"/docs/events\">Events</a>, or compose your pages from reusable <a href=\"/docs/components\">Components</a>.</p>"; prevSlug = "mview-files"; prevTitle = "The .mview File"; nextSlug = "layouts"; nextTitle = "Layouts" } };
        case ("layouts") { ?{ slug = "layouts"; title = "Layouts"; section = "The .mview Format"; html = "<h1 id=\"layouts\">Layouts</h1>\n<p>Most pages share the same chrome: a <code>&lt;head&gt;</code> full of meta tags, a navigation bar, a footer, maybe an analytics include. Instead of repeating that markup in every page, you define it once in a <strong>layout</strong> and let your pages slot their content into it.</p>\n<p>A layout is just a <code>.mview</code> file in <code>src/Layouts/</code>. Pages opt in with the <code>@layout</code> directive, and the layout decides where page content lands with <code>@yield</code>.</p>\n<h2 id=\"a-page-that-uses-a-layout\">A page that uses a layout</h2>\n<pre class=\"lang-razor\"><code>@page \"/products\"\n@layout MainLayout\n@title \"Products\"\n@description \"Browse the catalog\"\n\n&lt;PageHeader title=\"Products\" /&gt;\n\n&lt;Grid columns=\"3\"&gt;\n  @for p in products {\n    &lt;Card title=@p.name&gt;@p.summary&lt;/Card&gt;\n  }\n&lt;/Grid&gt;</code></pre>\n<p>The page never mentions <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>, or <code>&lt;body&gt;</code>. It only describes its own content. The <code>@title</code> and <code>@description</code> values are passed up to the layout's head so the rendered HTML stays SEO-friendly. (For more on metadata directives, see <a href=\"/docs/pages\">Pages &amp; Routing</a>.)</p>\n<h2 id=\"the-mainlayout-example\">The MainLayout example</h2>\n<p>Here is a complete layout. Note the single <code>@yield</code>, which is where the page's content is rendered.</p>\n<pre class=\"lang-razor\"><code>@code {\n  // Layouts can run Motoko too — anything in scope is available below.\n}\n\n&lt;!DOCTYPE html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;\n  &lt;meta charset=\"utf-8\" /&gt;\n  &lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" /&gt;\n  &lt;title&gt;@title&lt;/title&gt;\n  &lt;meta name=\"description\" content=@description /&gt;\n  &lt;link rel=\"stylesheet\" href=\"/motoview.css\" /&gt;\n  @section \"head\"\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;header class=\"site-nav\"&gt;\n    &lt;a href=\"/\" class=\"brand\"&gt;MotoView Store&lt;/a&gt;\n    &lt;nav&gt;\n      &lt;a href=\"/products\"&gt;Products&lt;/a&gt;\n      &lt;a href=\"/cart\"&gt;Cart&lt;/a&gt;\n    &lt;/nav&gt;\n  &lt;/header&gt;\n\n  &lt;main id=\"mv-root\"&gt;\n    @yield\n  &lt;/main&gt;\n\n  &lt;footer class=\"site-footer\"&gt;\n    &lt;p&gt;Built with MotoView.&lt;/p&gt;\n  &lt;/footer&gt;\n\n  &lt;script type=\"module\" src=\"/motoview.js\"&gt;&lt;/script&gt;\n  @section \"scripts\"\n&lt;/body&gt;\n&lt;/html&gt;</code></pre>\n<p>A few things to call out:</p>\n<ul><li><code>@title</code> and <code>@description</code> resolve to whatever the page declared with <code>@title</code> / <code>@description</code>. They are plain expressions, so you can supply a fallback in the layout.</li><li><code>@yield</code> is the single insertion point for the page's body. The page content lives inside <code>&lt;div id=\"mv-root\"&gt;</code> — this is the element the WASM client swaps when a new batch arrives, so keep it on the element that wraps <code>@yield</code>.</li><li><code>/motoview.css</code>, <code>/motoview.js</code>, and <code>/motoview.wasm</code> are served by the runtime. Including the stylesheet and module script is what wires up the browser client.</li></ul>\n<h2 id=\"sections-section-and-head\">Sections: <code>@section</code> and <code>@head</code></h2>\n<p>A layout exposes named <strong>sections</strong> that pages can fill in. Two are conventional: <code>head</code> (for per-page <code>&lt;link&gt;</code>/<code>&lt;meta&gt;</code> tags) and <code>scripts</code> (for per-page script tags placed at the end of the body). You declare the insertion point in the layout with <code>@section \"name\"</code>:</p>\n<pre class=\"lang-razor\"><code>  @section \"head\"\n  ...\n  @section \"scripts\"</code></pre>\n<p>A page contributes content to a section by defining it:</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id:Nat}\"\n@layout MainLayout\n@title product.name\n\n@section \"head\" {\n  &lt;link rel=\"canonical\" href=@canonical /&gt;\n  &lt;meta property=\"og:title\" content=@product.name /&gt;\n}\n\n&lt;PageHeader title=@product.name /&gt;\n&lt;p&gt;@product.description&lt;/p&gt;\n\n@section \"scripts\" {\n  &lt;!-- per-page markup that belongs at the end of &lt;body&gt; --&gt;\n}</code></pre>\n<p>If a page does not define a given section, the layout simply renders nothing there.</p>\n<p>For the common case of adding to the document head from a page without naming a section, use <code>@head</code>:</p>\n<pre class=\"lang-razor\"><code>@page \"/about\"\n@layout MainLayout\n@title \"About\"\n\n@head {\n  &lt;meta name=\"robots\" content=\"index,follow\" /&gt;\n}\n\n&lt;p&gt;About us.&lt;/p&gt;</code></pre>\n<p><code>@head</code> is shorthand that targets the layout's head region — useful when you just want one or two extra tags and do not need a dedicated named slot.</p>\n<h2 id=\"layout-inheritance\">Layout inheritance</h2>\n<p>Layouts can nest. A layout may itself declare <code>@layout</code> to wrap inside a parent layout, with its own <code>@yield</code> providing the slot for the child. This lets you build, for example, a broad <code>MainLayout</code> (full HTML document) and a narrower <code>DashboardLayout</code> that adds a sidebar:</p>\n<pre class=\"lang-razor\"><code>@layout MainLayout\n@title \"Dashboard\"\n\n&lt;div class=\"dashboard\"&gt;\n  &lt;aside class=\"sidebar\"&gt;\n    &lt;a href=\"/dashboard\"&gt;Overview&lt;/a&gt;\n    &lt;a href=\"/dashboard/orders\"&gt;Orders&lt;/a&gt;\n  &lt;/aside&gt;\n  &lt;section class=\"dashboard-body\"&gt;\n    @yield\n  &lt;/section&gt;\n&lt;/div&gt;</code></pre>\n<p>A page then chooses <code>@layout DashboardLayout</code>, which renders inside <code>MainLayout</code>. Sections like <code>head</code> propagate up the chain, so a page can still contribute <code>&lt;head&gt;</code> tags even when wrapped two layouts deep.</p>\n<h2 id=\"building\">Building</h2>\n<p>Layouts compile alongside pages. The compiler resolves <code>@layout</code> references and the named sections at build time:</p>\n<pre class=\"lang-bash\"><code>motoview build</code></pre>\n<p>No layout is selected by default — a page without <code>@layout</code> renders its markup as the full response. From here, wire up interactivity with <a href=\"/docs/events\">Events</a> and reusable markup with <a href=\"/docs/components\">Components</a>.</p>"; prevSlug = "pages-and-routing"; prevTitle = "Pages & Routing"; nextSlug = "components"; nextTitle = "Components" } };
        case ("components") { ?{ slug = "components"; title = "Components"; section = "The .mview Format"; html = "<h1 id=\"components\">Components</h1>\n<p>Components let you carve a page into small, reusable pieces of markup and logic. In MotoView, a component is just another <code>.mview</code> file — same template syntax, same <code>@code</code> block, same Motoko underneath. If you can write a page, you can write a component.</p>\n<p>In your templates, <strong>any capitalized tag is a component</strong>. A lowercase tag like <code>&lt;div&gt;</code> is plain HTML; a capitalized tag like <code>&lt;Card&gt;</code> resolves to a component.</p>\n<h2 id=\"authoring-a-component\">Authoring a component</h2>\n<p>App components live in <code>src/Components/*.mview</code>. The file name is the tag name. A component called <code>Greeting</code> lives at <code>src/Components/Greeting.mview</code>:</p>\n<pre class=\"lang-razor\"><code>@code {\n  param name : Text;\n}\n\n&lt;p&gt;Hello, @name!&lt;/p&gt;</code></pre>\n<p>That's the whole contract: declare what comes in with <code>param</code>, then render markup that uses it.</p>\n<h2 id=\"declaring-params\">Declaring params</h2>\n<p>A component receives data through <code>param</code> declarations inside its <code>@code</code> block. Each <code>param</code> names a typed Motoko value the parent must — or may — provide:</p>\n<pre class=\"lang-razor\"><code>@code {\n  param title : Text;\n  param count : Nat;\n}\n\n&lt;section&gt;\n  &lt;h2&gt;@title&lt;/h2&gt;\n  &lt;p&gt;@count items&lt;/p&gt;\n&lt;/section&gt;</code></pre>\n<h3 id=\"defaults\">Defaults</h3>\n<p>Give a <code>param</code> a default with <code>=</code>. Params with a default are optional; the parent can omit them.</p>\n<pre class=\"lang-razor\"><code>@code {\n  param label : Text = \"Save\";\n  param disabled : Bool = false;\n}\n\n&lt;button disabled=@disabled&gt;@label&lt;/button&gt;</code></pre>\n<p>Because params are typed Motoko values, the compiler checks them. Pass the wrong type and the build fails — you find out at <code>motoview build</code>, not in the browser.</p>\n<h2 id=\"passing-props\">Passing props</h2>\n<p>Set props as attributes on the capitalized tag. The attribute name matches the <code>param</code> name:</p>\n<pre class=\"lang-razor\"><code>&lt;Greeting name=\"Ada\" /&gt;\n\n&lt;section title=\"Inbox\" count=@unread /&gt;</code></pre>\n<p>String literals go in quotes; to pass a Motoko expression, use the <code>@</code> prefix or <code>@(...)</code>:</p>\n<pre class=\"lang-razor\"><code>&lt;Greeting name=@user.firstName /&gt;\n&lt;section title=\"Orders\" count=@(orders.size()) /&gt;</code></pre>\n<p>Handler arguments and event wiring follow the same rules everywhere — see <a href=\"/docs/events\">Events</a> for how <code>@click</code> and friends behave inside components.</p>\n<h2 id=\"default-content-with-yield\">Default content with @yield</h2>\n<p>A component can wrap content the parent puts between its tags. Render that content with <code>@yield</code>:</p>\n<pre class=\"lang-razor\"><code>@code {\n  param title : Text;\n}\n\n&lt;article class=\"card\"&gt;\n  &lt;h3&gt;@title&lt;/h3&gt;\n  &lt;div class=\"card-body\"&gt;\n    @yield\n  &lt;/div&gt;\n&lt;/article&gt;</code></pre>\n<p>Now the parent nests freely:</p>\n<pre class=\"lang-razor\"><code>&lt;Panel title=\"Profile\"&gt;\n  &lt;p&gt;Signed in as @user.name&lt;/p&gt;\n  &lt;Button kind=\"primary\"&gt;Edit&lt;/Button&gt;\n&lt;/Panel&gt;</code></pre>\n<p>Everything between <code>&lt;Panel&gt;</code> and <code>&lt;/Panel&gt;</code> lands where <code>@yield</code> sits.</p>\n<h2 id=\"named-slots-with-slot\">Named slots with @slot</h2>\n<p>When a component has more than one region to fill — say a header and a footer — declare named slots. Inside the component, mark the regions with <code>@slot \"name\"</code>:</p>\n<pre class=\"lang-razor\"><code>&lt;div class=\"dialog\"&gt;\n  &lt;header&gt;@slot \"header\"&lt;/header&gt;\n  &lt;div class=\"dialog-body\"&gt;@yield&lt;/div&gt;\n  &lt;footer&gt;@slot \"footer\"&lt;/footer&gt;\n&lt;/div&gt;</code></pre>\n<p>The parent targets each slot with <code>@section</code>:</p>\n<pre class=\"lang-razor\"><code>&lt;Dialog&gt;\n  @section \"header\" {\n    &lt;h2&gt;Confirm delete&lt;/h2&gt;\n  }\n\n  &lt;p&gt;This action cannot be undone.&lt;/p&gt;\n\n  @section \"footer\" {\n    &lt;Button kind=\"danger\"&gt;Delete&lt;/Button&gt;\n    &lt;Button&gt;Cancel&lt;/Button&gt;\n  }\n&lt;/Dialog&gt;</code></pre>\n<p>Content not wrapped in a <code>@section</code> flows into the default <code>@yield</code>. The same <code>@section</code> / <code>@yield</code> mechanism powers layouts — see <a href=\"/docs/layouts\">Layouts</a>.</p>\n<h2 id=\"nesting-and-any-param-name\">Nesting and any param name</h2>\n<p>Components nest arbitrarily — a component can use another, which uses another — and both <strong>props and <code>@children</code> thread through every level</strong>, because each component compiles to its own render function (nesting is just function calls). You can also name a <code>param</code> anything: if the name collides with a Motoko keyword (<code>label</code>, <code>type</code>, <code>class</code>, …) the compiler <strong>auto-mangles</strong> it, so it just works.</p>\n<pre class=\"lang-razor\"><code>@* Inner.mview *@   param label : Text\n                    &lt;span&gt;@label&lt;/span&gt;\n@* Card.mview *@    &lt;div&gt;&lt;Inner label=\"@title\" /&gt; @children&lt;/div&gt;\n@* page *@          &lt;Card&gt;&lt;Inner label=\"hi\" /&gt;&lt;/Card&gt;   @* nests fine *@</code></pre>\n<h2 id=\"built-in-components\">Built-in components</h2>\n<p>MotoView ships ~50 server-rendered, <strong>CSS-only</strong> built-ins (no JavaScript) — an authentic port of the Microsoft <strong>Fluent UI 2</strong> design system. Reach for meaning, not utility-class soup: <code>&lt;Button appearance=\"primary\"&gt;Save&lt;/Button&gt;</code>. See them all live, with copy-paste snippets, in the <strong><a href=\"https://github.com/miadey/MotoView\">/components gallery</a></strong> (it's itself a MotoView page). Theme them with <a href=\"/docs/styling-and-themes\"><code>@theme</code> / <code>&lt;ThemePicker/&gt;</code></a>.</p>\n<blockquote>Coverage: every component carries its <strong>complete Fluent variant matrix</strong> — each appearance, size, shape, color and state (hover/checked/disabled/selected) is implemented in CSS and wired through the codegen. See them all, side by side, on the live <strong><code>/variants</code></strong> page. (What isn't replicated are React-only <em>behaviours</em> — controlled state, virtualization, keyboard-driven widgets — since MotoView is server-rendered + CSS-only.) The exact props for any one component are its <code>gen_builtin</code> arm in <code>compiler/src/codegen.rs</code>.</blockquote>\n<p><strong>Buttons &amp; actions</strong> — <code>Button</code> (<code>appearance</code> primary/secondary/outline/subtle/transparent/danger, <code>size</code>, <code>shape</code> rounded/circular/square, <code>icon</code>, <code>iconPosition</code>, <code>disabled</code>, <code>type</code>), <code>CompoundButton</code> (<code>appearance</code>, <code>icon</code>, <code>secondary</code>), <code>ToggleButton</code> (<code>appearance</code>, <code>name</code>, <code>checked</code>), <code>SplitButton</code>, <code>MenuButton</code>, <code>Menu</code> + <code>MenuItem</code> / <code>MenuItemCheckbox</code> / <code>MenuItemRadio</code>.</p>\n<p><strong>Inputs &amp; fields</strong> — <code>Input</code> (<code>type</code>, <code>appearance</code>, <code>placeholder</code>, <code>size</code>), <code>InputText</code> / <code>InputEmail</code> / <code>InputNumber</code> / <code>TextArea</code> (<code>name</code>, <code>label</code>, <code>bind</code>, <code>required</code>, <code>minLength</code>), <code>Field</code> (<code>label</code>, <code>required</code>, <code>hint</code>, <code>validationState</code>), <code>Label</code>, <code>InfoLabel</code> (<code>label</code>, <code>info</code>), <code>SpinButton</code> (<code>min</code>/<code>max</code>/<code>step</code>), <code>Slider</code>, <code>Select</code>, <code>Combobox</code>, <code>Searchbox</code>, <code>Switch</code>, <code>Checkbox</code>, <code>Radio</code>, <code>ValidationSummary</code>.</p>\n<p><strong>Data display</strong> — <code>Badge</code> (<code>appearance</code> filled/ghost/outline/tint, <code>shape</code>, <code>size</code>, <code>color</code>), <code>CounterBadge</code> (<code>count</code>, <code>color</code>, <code>dot</code>), <code>PresenceBadge</code> (<code>status</code>), <code>Tag</code> (<code>appearance</code>, <code>size</code>, <code>shape</code>, <code>dismissible</code>), <code>TagGroup</code>, <code>InteractionTag</code>, <code>Avatar</code> (<code>name</code>, <code>size</code>, <code>shape</code>, <code>presence</code>, <code>color</code>, <code>active</code>, <code>badge</code>), <code>AvatarGroup</code>, <code>Persona</code>, <code>Rating</code>, <code>Image</code>, <code>ProgressBar</code> (<code>value</code>, <code>color</code>, <code>thickness</code>), <code>Spinner</code> (<code>size</code>, <code>label</code>), <code>Skeleton</code>, <code>Table</code>.</p>\n<p><strong>Typography</strong> — <code>Title1</code> / <code>Title2</code> / <code>Title3</code>, <code>Subtitle1</code> / <code>Subtitle2</code>, <code>Body1</code> / <code>Body2</code>, <code>Caption1</code> / <code>Caption2</code>, <code>Display</code>, and <code>Text</code> (<code>variant</code>) — the Fluent type ramp as components.</p>\n<p><strong>Layout &amp; cards</strong> — <code>Card</code> (<code>title</code>) + <code>CardHeader</code> / <code>CardPreview</code> / <code>CardFooter</code>, <code>Grid</code> (<code>columns</code>), <code>Divider</code> (<code>vertical</code>, <code>appearance</code>), <code>PageHeader</code>, <code>Toolbar</code>, <code>Carousel</code>.</p>\n<p><strong>Navigation &amp; disclosure</strong> — <code>Nav</code> + <code>NavItem</code> (<code>match</code>), <code>AppBar</code>, <code>Breadcrumb</code> + <code>BreadcrumbItem</code>, <code>TabList</code> + <code>Tab</code> (<code>match</code>), <code>Accordion</code> + <code>AccordionItem</code>, <code>Tree</code> + <code>TreeItem</code>.</p>\n<p><strong>Feedback &amp; overlays</strong> (all CSS-only) — <code>MessageBar</code> / <code>Alert</code> (<code>intent</code>/<code>type</code>), <code>Tooltip</code>, <code>Popover</code>, <code>Dialog</code>, <code>Drawer</code>.</p>\n<p><strong>Theme</strong> — <code>&lt;ThemeToggle/&gt;</code> (light/dark) and <code>&lt;ThemePicker/&gt;</code> (Web Light/Dark, Teams Light/Dark, High Contrast). See <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a>.</p>\n<p>A small example:</p>\n<pre class=\"lang-razor\"><code>&lt;Card title=\"New product\"&gt;\n  &lt;Field label=\"Name\" required&gt;\n    &lt;InputText name=\"name\" bind=\"@model.name\" required /&gt;\n  &lt;/Field&gt;\n  &lt;Slider name=\"qty\" min=\"1\" max=\"10\" value=\"3\" /&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;Button appearance=\"primary\" icon=\"💾\"&gt;Create&lt;/Button&gt;\n&lt;/Card&gt;</code></pre>\n<p>The form-oriented inputs pair with <code>bind=</code> and handler-side validation — see <a href=\"/docs/forms\">Forms &amp; Validation</a> for the full flow.</p>\n<h2 id=\"theming-the-components\">Theming the components</h2>\n<p>Every built-in is drawn from the <strong>Fluent UI 2 design tokens</strong>, so you re-theme the entire component set by changing tokens — never by touching component markup.</p>\n<ul><li><strong>Brand from one color.</strong> <code>@theme brand=\"#107c41\"</code> generates the full 16-step brand ramp plus every brand alias, for light <em>and</em> dark, from a single hex. Buttons, tabs, links, focus rings — everything brand-colored follows.</li><li><strong>Light &amp; dark.</strong> Automatic from the OS (<code>prefers-color-scheme</code>), or force it with <code>&lt;html data-theme=\"dark\"&gt;</code>. Drop in <code>&lt;ThemeToggle/&gt;</code> for a one-click switch — it persists the choice and applies it <em>before first paint</em>, so there's no flash, even on certified pages.</li><li><strong>The full Fluent theme set.</strong> <code>&lt;ThemePicker/&gt;</code> is a dropdown of the official themes — <strong>Web Light, Web Dark, Teams Light, Teams Dark, High Contrast</strong> — exactly like the Fluent UI docs. The user's pick is remembered and overrides the app's brand.</li><li><strong>Complete variants, every theme.</strong> Each component carries its complete variant matrix — every appearance, size, shape, color and state — and reads tokens rather than hard-coded colors, so all of it adapts to whichever theme is active.</li></ul>\n<p>See it live: the <strong><code>/components</code></strong> gallery (every component with a copy-paste snippet) and the <strong><code>/variants</code></strong> gallery (every variant of every component, side by side). The full token reference, ramps, presets, and picker internals are in <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a>.</p>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li><a href=\"/docs/events\">Events</a> — wiring <code>@click</code>, <code>@submit</code>, and friends.</li><li><a href=\"/docs/layouts\">Layouts</a> — <code>@section</code> / <code>@yield</code> at the page level.</li><li><a href=\"/docs/forms\">Forms &amp; Validation</a> — secure forms and the built-in inputs.</li></ul>"; prevSlug = "layouts"; prevTitle = "Layouts"; nextSlug = "control-flow"; nextTitle = "Control Flow" } };
        case ("control-flow") { ?{ slug = "control-flow"; title = "Control Flow"; section = "The .mview Format"; html = "<h1 id=\"control-flow\">Control Flow</h1>\n<p>MotoView templates are Motoko-aware. Inside a <code>.mview</code> file you can branch, loop, and match over your data using a small set of <code>@</code>-directives that read like the markup they produce. Everything runs server-side during render, so the values you reach for are just plain Motoko expressions and variables declared in your <a href=\"/docs/code-block\">@code block</a>.</p>\n<p>There is no client-side templating: the server evaluates the control flow, produces HTML, and the WASM client swaps it in. That means you can branch on a <code>caller</code>, a query result, or anything else available to your canister without shipping a line of application JavaScript.</p>\n<h2 id=\"inline-output-expr-and-expr\">Inline output: <code>@expr</code> and <code>@(expr)</code></h2>\n<p>The simplest form of control flow is printing a value. Reference a variable or a dotted path directly with <code>@</code>, or wrap a larger expression in <code>@( ... )</code>.</p>\n<pre class=\"lang-razor\"><code>@code {\n  let user = { name = \"Ada\"; visits = 7 };\n  var count : Nat = 3;\n}\n\n&lt;p&gt;Welcome back, @user.name.&lt;/p&gt;\n&lt;p&gt;You have visited @count times.&lt;/p&gt;\n&lt;p&gt;Next milestone: @(count + 1)&lt;/p&gt;</code></pre>\n<p>Use the bare form (<code>@count</code>, <code>@user.name</code>) for identifiers and simple member access. Reach for the parenthesized form (<code>@(count + 1)</code>) whenever you need an operator, a function call, or anything the parser would otherwise read as markup. Output is HTML-escaped for you.</p>\n<h2 id=\"if-else-if-else\"><code>@if</code> / <code>@else if</code> / <code>@else</code></h2>\n<p><code>@if</code> takes a Motoko <code>Bool</code> expression and renders its block when the expression is true. Chain with <code>@else if</code> and finish with <code>@else</code>.</p>\n<pre class=\"lang-razor\"><code>@if count == 0 {\n  &lt;Alert type=\"info\"&gt;Nothing here yet.&lt;/Alert&gt;\n} else if count &lt; 5 {\n  &lt;p&gt;Getting started — @count so far.&lt;/p&gt;\n} else {\n  &lt;p&gt;You are on a roll: @count!&lt;/p&gt;\n}</code></pre>\n<p>Because the condition is real Motoko, you can call into your <a href=\"/docs/services\">services</a> or guard on a principal:</p>\n<pre class=\"lang-razor\"><code>@if Principal.isAnonymous(caller) {\n  &lt;Button kind=\"primary\" @click=\"signIn\"&gt;Sign in&lt;/Button&gt;\n} else {\n  &lt;Badge type=\"success\"&gt;Signed in&lt;/Badge&gt;\n}</code></pre>\n<h2 id=\"for-x-in-expr\"><code>@for X in EXPR</code></h2>\n<p>Loop over any iterable with <code>@for</code>. This is the idiomatic way to render lists — <strong>prefer <code>@for</code> over building HTML with <code>.map()</code></strong> in a code block. <code>@for</code> keeps the markup in the template where it is readable, and it composes cleanly with components and nested control flow.</p>\n<pre class=\"lang-razor\"><code>@code {\n  let products = [\n    { name = \"Helmet\"; price = 120 },\n    { name = \"Gloves\"; price = 45 },\n  ];\n}\n\n&lt;Grid columns=\"2\"&gt;\n  @for p in products.vals() {\n    &lt;Card title=\"@p.name\"&gt;\n      &lt;p&gt;$@p.price&lt;/p&gt;\n      &lt;Button kind=\"primary\" @click=\"addToCart(p.name)\"&gt;Add&lt;/Button&gt;\n    &lt;/Card&gt;\n  }\n&lt;/Grid&gt;</code></pre>\n<p>Iterate an array with <code>.vals()</code>, or loop over any other Motoko iterator the same way (for example <code>Iter.range(0, n)</code>). Handler arguments like <code>addToCart(p.name)</code> are evaluated server-side at render time and baked into the event payload — see <a href=\"/docs/events\">Events</a>.</p>\n<p>Combine loops with <code>@if</code> to handle empty collections:</p>\n<pre class=\"lang-razor\"><code>@if products.size() == 0 {\n  &lt;Alert type=\"info\"&gt;No products yet.&lt;/Alert&gt;\n} else {\n  @for p in products.vals() {\n    &lt;p&gt;@p.name&lt;/p&gt;\n  }\n}</code></pre>\n<h2 id=\"switch-over-variants\"><code>@switch</code> over variants</h2>\n<p>When you are working with Motoko variant types, <code>@switch</code> is clearer than a chain of <code>@if</code>s. Each <code>case</code> matches a <code>#Variant</code>, and pattern bindings are available inside the block.</p>\n<pre class=\"lang-razor\"><code>@code {\n  type Status = { #Draft; #Published; #Archived : { reason : Text } };\n  let status : Status = #Published;\n}\n\n@switch status {\n  case #Draft {\n    &lt;Badge type=\"warning\"&gt;Draft&lt;/Badge&gt;\n  }\n  case #Published {\n    &lt;Badge type=\"success\"&gt;Live&lt;/Badge&gt;\n  }\n  case #Archived a {\n    &lt;Alert type=\"info\"&gt;Archived: @a.reason&lt;/Alert&gt;\n  }\n}</code></pre>\n<p><code>@switch</code> is exhaustive in the same way Motoko's <code>switch</code> is — cover every variant your type can hold so the generated Motoko compiles cleanly.</p>\n<h2 id=\"putting-it-together\">Putting it together</h2>\n<p>Control flow nests freely. Loop over a collection, switch on each item's state, and print values inline — all in one readable template:</p>\n<pre class=\"lang-razor\"><code>@for order in orders.vals() {\n  &lt;Card title=\"@order.id\"&gt;\n    @switch order.state {\n      case #Paid { &lt;Badge type=\"success\"&gt;Paid&lt;/Badge&gt; }\n      case #Pending { &lt;Badge type=\"warning\"&gt;Pending&lt;/Badge&gt; }\n    }\n    &lt;p&gt;Total: $@(order.total)&lt;/p&gt;\n  &lt;/Card&gt;\n}</code></pre>\n<p>Build it and watch the rendered output update:</p>\n<pre class=\"lang-bash\"><code>motoview dev</code></pre>\n<p>Next, learn how the buttons above turn clicks into typed Motoko calls in <a href=\"/docs/events\">Events</a>.</p>"; prevSlug = "components"; prevTitle = "Components"; nextSlug = "events"; nextTitle = "Events" } };
        case ("events") { ?{ slug = "events"; title = "Events"; section = "Interactivity"; html = "<h1 id=\"events\">Events</h1>\n<p>In MotoView, interactivity is server-driven. You attach an event to an element in your <code>.mview</code> markup, write the matching Motoko function in <code>@code</code>, and the framework handles everything between the click and the updated DOM. There is no event listener to register, no fetch call to write, no JSON to parse, and no state to reconcile by hand. You write Motoko; the WASM client and the runtime do the wiring.</p>\n<h2 id=\"attaching-events\">Attaching events</h2>\n<p>Bind a handler to an element with one of the event directives. The value is the name of a function defined in your <code>@code</code> block.</p>\n<pre class=\"lang-razor\"><code>@page \"/counter\"\n\n@code {\n  var count : Nat = 0;\n\n  func increment() { count += 1; };\n  func reset() { count := 0; };\n}\n\n&lt;p&gt;Count: @count&lt;/p&gt;\n\n&lt;Button kind=\"primary\" @click=\"increment\"&gt;Increment&lt;/Button&gt;\n&lt;Button @click=\"reset\"&gt;Reset&lt;/Button&gt;</code></pre>\n<p>The supported directives are:</p>\n<ul><li><code>@click</code> — fires when the element is clicked.</li><li><code>@submit</code> — fires when a form is submitted (use this on <code>&lt;form&gt;</code>).</li><li><code>@input</code> — fires as the user types into an input.</li><li><code>@change</code> — fires when an input's value is committed (blur or selection change).</li></ul>\n<p>That is the whole surface. Every directive maps to a typed Motoko function, and the same dispatch path serves all of them.</p>\n<h2 id=\"passing-arguments\">Passing arguments</h2>\n<p>Handlers can take arguments. Arguments are written in the markup and evaluated <strong>server-side at render time</strong>, then baked into the element so the client can forward them back unchanged.</p>\n<pre class=\"lang-razor\"><code>@code {\n  var items : [Text] = [\"Helmet\", \"Gloves\", \"Jacket\"];\n  var selected : Text = \"\";\n\n  func select(name : Text) { selected := name; };\n  func removeAt(i : Nat) { /* drop item i */ };\n}\n\n&lt;ul&gt;\n  @for item in items {\n    &lt;li&gt;\n      &lt;Button @click=\"select(item)\"&gt;@item&lt;/Button&gt;\n    &lt;/li&gt;\n  }\n&lt;/ul&gt;\n\n&lt;p&gt;Selected: @selected&lt;/p&gt;</code></pre>\n<p>Because arguments are resolved during render, the value you pass is whatever the expression evaluates to on the server in that iteration. The developer never serializes anything manually — you write <code>@click=\"select(item)\"</code> and the typed <code>select(name : Text)</code> function receives the right value.</p>\n<h2 id=\"how-events-work-internally\">How events work internally</h2>\n<p>The flow is the same for every directive. Nothing here requires application JavaScript.</p>\n<ol><li><strong>Render.</strong> When the page renders, each event directive produces a <code>data-mv-handler</code> attribute identifying the handler, plus <code>data-mv-arg*</code> attributes carrying any arguments that were evaluated server-side.</li><li><strong>Capture.</strong> In the browser, the WASM client (the \"brain\") observes the interaction through the thin JS glue (the \"hands\"). It reads the handler id and args off the element.</li><li><strong>POST.</strong> The client sends a form-encoded <code>POST</code> to <code>/_motoview/event</code> with the handler id and arguments.</li><li><strong>Dispatch.</strong> On the canister, that request is served by <code>http_request_update</code>, which decodes the payload and dispatches to your typed Motoko function — <code>increment</code>, <code>select(name)</code>, and so on.</li><li><strong>Batch.</strong> The handler mutates state, the page re-renders, and the server returns a new batch immediately in the response. The batch carries a <code>batchId</code> (a hash of the rendered state) and the updated HTML.</li><li><strong>Swap.</strong> The client interprets the batch and swaps the content inside <code>&lt;div id=\"mv-root\"&gt;</code>, preserving focus and scroll where it can.</li></ol>\n<p>Because the event response returns the new batch right away, the UI updates without waiting for the next poll. Adaptive polling then continues in the background to pick up state changed by other callers.</p>\n<pre class=\"lang-bash\"><code># Illustrative: the event POST the WASM client makes for you\ncurl -X POST https://&lt;canister&gt;.localhost/_motoview/event \\\n  -d \"path=/counter&amp;handler=&lt;handlerId&gt;\"</code></pre>\n<p>You do not write this request — it is shown only to make the protocol concrete. See <a href=\"/docs/protocol\">Protocol</a> for the full batch shape and status values.</p>\n<h2 id=\"events-and-forms\">Events and forms</h2>\n<p>For forms, pair <code>@submit</code> with <code>bind</code> on your inputs and add the <code>secure</code> attribute so the submission is signed and verified server-side:</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputText name=\"name\" label=\"Name\" bind=\"@model.name\" required /&gt;\n  &lt;Button kind=\"primary\"&gt;Send&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<p>Validation runs inside the handler, and failures come back in the batch for re-render. See <a href=\"/docs/forms\">Forms &amp; Validation</a> for the full pattern and <a href=\"/docs/security\">Security</a> for how <code>secure</code> tokens work.</p>\n<h2 id=\"what-you-do-not-write\">What you do not write</h2>\n<ul><li>No <code>addEventListener</code>.</li><li>No <code>fetch</code> / agent-js call.</li><li>No argument serialization or DOM patching.</li></ul>\n<p>Define the function, attach the directive, and MotoView connects the click to your Motoko.</p>"; prevSlug = "control-flow"; prevTitle = "Control Flow"; nextSlug = "forms"; nextTitle = "Forms" } };
        case ("forms") { ?{ slug = "forms"; title = "Forms"; section = "Interactivity"; html = "<h1 id=\"forms\">Forms</h1>\n<p>Forms in MotoView are plain HTML wired straight to typed Motoko handlers. You bind inputs to a model, submit to a server-side function, and validate without writing a single line of frontend JavaScript. The browser synchronizes the result through the same batch protocol that powers every other <a href=\"/docs/events\">event</a>.</p>\n<h2 id=\"building-a-form\">Building a form</h2>\n<p>A form declares the handler that runs on submit and binds each input to a field on a <code>@code</code> model. Use <code>bind=\"@model.field\"</code> to connect an input to state — MotoView renders the current value into the input and routes changes back into your model.</p>\n<pre class=\"lang-razor\"><code>@page \"/contact\"\n@title \"Contact us\"\n\n@code {\n  type Message = { name : Text; email : Text; body : Text };\n  var model : Message = { name = \"\"; email = \"\"; body = \"\" };\n  var sent : Bool = false;\n\n  func send() : async () {\n    validate model {\n      name required \"Please tell us your name\";\n      email required email \"A valid email is required\";\n      body required \"Message can't be empty\";\n    };\n    // persist, notify, etc.\n    sent := true;\n  };\n}\n\n@if sent {\n  &lt;Alert type=\"success\"&gt;Thanks — we'll be in touch.&lt;/Alert&gt;\n} else {\n  &lt;form @submit=\"send\" secure&gt;\n    &lt;ValidationSummary /&gt;\n    &lt;InputText  name=\"name\"  label=\"Name\"  bind=\"@model.name\" required /&gt;\n    &lt;InputEmail name=\"email\" label=\"Email\" bind=\"@model.email\" required /&gt;\n    &lt;TextArea   name=\"body\"  label=\"Message\" bind=\"@model.body\" required minLength={10} /&gt;\n    &lt;Button kind=\"primary\"&gt;Send message&lt;/Button&gt;\n  &lt;/form&gt;\n}</code></pre>\n<p>That is the whole contact form: markup, model, and handler in one <code>.mview</code> file. On submit the client POSTs the field values to <code>/_motoview/event</code>, the server hydrates <code>model</code>, runs <code>send</code>, and returns a fresh batch. If everything passes, the success branch renders; the DOM swaps without a full page reload.</p>\n<h2 id=\"bind-model-x\">bind=\"@model.x\"</h2>\n<p><code>bind</code> is two-way. On render, MotoView writes <code>model.x</code> into the input's value. On submit, the posted form fields are decoded back onto the model before your handler runs, so inside <code>send</code> you read <code>model.name</code>, <code>model.email</code>, and so on directly. The built-in input components (<code>InputText</code>, <code>InputEmail</code>, <code>InputNumber</code>, <code>TextArea</code>) accept <code>bind</code> alongside <code>name</code>, <code>label</code>, <code>required</code>, and <code>minLength</code>, so you avoid hand-writing <code>&lt;input&gt;</code> markup and class strings. Prefer them over raw elements.</p>\n<h2 id=\"validation\">Validation</h2>\n<p>Call <code>validate</code> inside the handler with one rule line per field:</p>\n<pre class=\"lang-motoko\"><code>validate model {\n  name  required \"Name is required\";\n  email required email;\n  price min 1 \"Price must be at least 1\";\n};</code></pre>\n<p>When a rule fails, the errors are returned in the batch and the page re-renders with them in place — no exception, no lost input. Surface them with <code>&lt;ValidationSummary /&gt;</code> for a grouped list, and per-field messages appear next to the bound inputs automatically. Because validation lives in your Motoko handler, there is no duplicated client/server logic to keep in sync.</p>\n<h2 id=\"the-secure-attribute\">The <code>secure</code> attribute</h2>\n<p>Add <code>secure</code> to a form and MotoView mints a signed token when the form is rendered:</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;</code></pre>\n<p>The token is an <strong>HMAC-SHA256</strong> MAC binding the request path, the handler, the caller principal, a nonce, an expiry, and a hash of the field schema. On submit the server re-derives the MAC from the incoming request and compares. This protects against:</p>\n<ul><li><strong>Tampering</strong> — changing the handler, path, or submitted field set breaks the schema/handler binding, so the MAC won't match.</li><li><strong>Cross-context replay</strong> — the token is bound to the caller principal and the specific form, so a token minted for one user or page can't be reused elsewhere.</li><li><strong>Replay &amp; stale submits</strong> — the nonce and expiry cause repeated or late submissions to be rejected.</li></ul>\n<p>Mismatched, expired, and replayed tokens are all refused before your handler runs. SHA-256 and HMAC are implemented in Motoko and verified against the standard test vectors, so the guarantee is real, not decorative.</p>\n<p>Use <code>secure</code> on any form that mutates state or accepts untrusted input. For forms that only read or filter, you can omit it.</p>\n<h2 id=\"next-steps\">Next steps</h2>\n<ul><li>Learn how handler arguments are evaluated in <a href=\"/docs/events\">Events</a>.</li><li>Compose richer inputs with the built-in <a href=\"/docs/components\">Components</a>.</li></ul>"; prevSlug = "events"; prevTitle = "Events"; nextSlug = "validation"; nextTitle = "Validation" } };
        case ("validation") { ?{ slug = "validation"; title = "Validation"; section = "Interactivity"; html = "<h1 id=\"validation\">Validation</h1>\n<p>Validation in MotoView runs where your data lives: inside your Motoko handlers, on the canister. There is no client-side validation library to keep in sync, no duplicated rules, and no JavaScript. You declare your rules once, and when they fail the server re-renders the page with the errors attached to the very batch the browser is already polling for.</p>\n<p>If you have not read <a href=\"/docs/forms\">Forms</a> and <a href=\"/docs/events\">Events</a> yet, start there — validation is the natural next step once a <code>@submit</code> handler is receiving a bound model.</p>\n<h2 id=\"the-validate-block\">The <code>validate</code> block</h2>\n<p>Inside a handler, call <code>validate</code> against your model. Each line is a field name, one or more rules, and an optional custom message.</p>\n<pre class=\"lang-motoko\"><code>@code {\n  var model = { name = \"\"; email = \"\"; price = 0 };\n\n  public func send() : async () {\n    validate model {\n      name required \"Name is required\";\n      email required email;\n      price min 1 \"Price must be at least 1\";\n    };\n\n    // Reached only when every rule passes.\n    await Store.save(model);\n  };\n}</code></pre>\n<p>When any rule fails, MotoView stops the handler, collects the failures, and returns a batch with status <code>validation-error</code>. Your <code>await Store.save(...)</code> never runs. The browser receives the batch, swaps <code>#mv-root</code>, and the page is now showing your errors — same render path as any other update.</p>\n<h2 id=\"rule-syntax\">Rule syntax</h2>\n<p>Rules are written after the field name, separated by spaces. A trailing string literal is the custom message for that field.</p>\n<pre class=\"lang-motoko\"><code>validate model {\n  name     required \"Name is required\";\n  email    required email;\n  bio      minLength 10 \"Tell us a little more\";\n  price    min 1 \"Price must be at least 1\";\n}</code></pre>\n<p>Built-in rules:</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Rule</th><th>Checks</th></tr></thead><tbody><tr><td><code>required</code></td><td>The field is present and not empty.</td></tr><tr><td><code>email</code></td><td>The value is a syntactically valid email address.</td></tr><tr><td><code>min N</code></td><td>A numeric field is at least <code>N</code>.</td></tr><tr><td><code>minLength N</code></td><td>A text field has at least <code>N</code> characters.</td></tr></tbody></table>\n<p>When you omit the message, MotoView supplies a sensible default. When you provide one, it is used verbatim — so write it the way you want the user to read it.</p>\n<h2 id=\"showing-all-errors-with-validationsummary\">Showing all errors with <code>ValidationSummary</code></h2>\n<p>Drop <code>&lt;ValidationSummary /&gt;</code> anywhere in your template to render every current error as a list. It produces nothing when there are no errors, so it is safe to leave in place permanently.</p>\n<pre class=\"lang-razor\"><code>@page \"/products/new\"\n\n&lt;form @submit=\"send\" secure&gt;\n  &lt;ValidationSummary /&gt;\n\n  &lt;InputText  name=\"name\"  label=\"Name\"  bind=\"@model.name\" /&gt;\n  &lt;InputEmail name=\"email\" label=\"Email\" bind=\"@model.email\" /&gt;\n  &lt;InputNumber name=\"price\" label=\"Price\" bind=\"@model.price\" /&gt;\n\n  &lt;Button kind=\"primary\"&gt;Save&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<h2 id=\"per-field-errors\">Per-field errors</h2>\n<p>Errors are also keyed by field, so you can place messages next to the input that produced them. The <code>InputText</code>, <code>InputEmail</code>, <code>InputNumber</code>, and <code>TextArea</code> components render their own field error automatically when one exists for their <code>name</code>, so the markup above already shows inline messages without extra wiring.</p>\n<p>The bound values survive the round trip: when the batch re-renders, each input is repopulated from the model, so the user never loses what they typed while fixing the one field that failed.</p>\n<h2 id=\"the-re-render-with-errors-flow\">The re-render-with-errors flow</h2>\n<p>Validation rides the same protocol as every other interaction:</p>\n<ol><li>The user submits the form. The browser POSTs to <code>/_motoview/event</code>, handled by <code>http_request_update</code>.</li><li>Your handler runs <code>validate</code>. A rule fails.</li><li>MotoView halts the handler and builds a batch with status <code>validation-error</code>, carrying the error map and the freshly rendered HTML.</li><li>The response returns that batch immediately — no waiting for the next poll.</li><li>The WASM client swaps <code>#mv-root</code>. <code>&lt;ValidationSummary /&gt;</code> and per-field messages appear; inputs keep their values.</li></ol>\n<p>Because the errors travel inside an ordinary batch, focus, scroll position, and in-progress input are preserved exactly as they are for any other update. See the <a href=\"/docs/protocol\">Protocol</a> page for the full batch shape and status values.</p>\n<h2 id=\"validation-and-secure\">Validation and <code>secure</code></h2>\n<p>Use <code>validate</code> together with the <code>secure</code> form attribute. The signed token rejects tampered, expired, and replayed submissions before your handler runs; <code>validate</code> then enforces that the data itself is well-formed. The two are complementary — see <a href=\"/docs/forms\">Forms</a> for how <code>secure</code> mints and verifies its token.</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;InputText name=\"name\" label=\"Name\" bind=\"@model.name\" /&gt;\n  &lt;Button kind=\"primary\"&gt;Save&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<p>With that, every rule lives in one place, runs on the canister, and the browser stays in sync through the same versioned batches it already speaks.</p>"; prevSlug = "forms"; prevTitle = "Forms"; nextSlug = "state"; nextTitle = "State" } };
        case ("state") { ?{ slug = "state"; title = "State"; section = "Interactivity"; html = "<h1 id=\"state\">State</h1>\n<p>State is what makes a MotoView page feel alive. A counter that climbs, a form that remembers what you typed, a list that grows — all of it is ordinary Motoko state living inside your canister. There is no client store to sync, no JavaScript model to mirror. You declare state in <code>@code</code>, your handlers mutate it, and MotoView re-renders.</p>\n<p>Rendering is a query over your state; events are updates to it. When state changes, the new render is hashed into a versioned batch and the WASM client swaps the DOM. See <a href=\"/docs/events\">Events</a> for how an interaction flows from the browser back to your Motoko.</p>\n<h2 id=\"var-vs-stable-var\"><code>var</code> vs <code>stable var</code></h2>\n<p>State is declared in the <code>@code</code> block like any Motoko field.</p>\n<pre class=\"lang-razor\"><code>@page \"/counter\"\n\n@code {\n    var count : Nat = 0;\n\n    public func increment() {\n        count += 1;\n    };\n}\n\n&lt;p&gt;The count is @count.&lt;/p&gt;\n&lt;Button kind=\"primary\" @click=\"increment\"&gt;Add one&lt;/Button&gt;</code></pre>\n<p>A plain <code>var</code> is mutable canister state. It is part of the actor the compiler generates, so it persists between requests for as long as the canister is running — every render and every event sees the same <code>count</code>.</p>\n<p>A <code>stable var</code> is the same, but it also survives a canister <strong>upgrade</strong>. In Motoko, <code>stable</code> variables are preserved across <code>dfx canister install --mode upgrade</code>; plain <code>var</code> state is re-initialized.</p>\n<pre class=\"lang-razor\"><code>@code {\n    stable var count : Nat = 0;\n}</code></pre>\n<blockquote><strong>MVP vs Roadmap.</strong> Plain <code>var</code> state is <strong>verified</strong>: it persists across calls within a running canister, which is exactly what the counter example demonstrated on a local replica. Reliable upgrade-stable page state — the full <code>stable var</code> story for generated pages, including stable migration of richer types — is <strong>Roadmap</strong>. For now, treat <code>var</code> as your working model and keep durable data in a <a href=\"/docs/services\">Service</a> you control.</blockquote>\n<h2 id=\"the-page-state-lifecycle\">The page state lifecycle</h2>\n<p>A page goes through the same shape on every request:</p>\n<ol><li><strong>Resolve</strong> the route and any params (see <a href=\"/docs/pages-and-routing\">Pages &amp; Routing</a>).</li><li><strong>Initialize</strong> the <code>@code</code> block. <code>let</code> bindings and <code>var</code> initializers run, and <code>onLoad(ctx)</code> is called if you defined it.</li><li><strong>Render</strong> the template against that state, producing HTML and a <code>batchId</code>.</li><li>On an <strong>event</strong>, the matching handler runs, mutates state, and the page re-renders into a fresh batch.</li><li>The client compares <code>batchId</code>s — <code>unchanged</code> batches are skipped, <code>changed</code> batches swap the DOM.</li></ol>\n<p>Because step 3 is a pure query over your state, the same state always produces the same HTML and the same <code>batchId</code>.</p>\n<h2 id=\"loading-data-with-onload-ctx\">Loading data with <code>onLoad(ctx)</code></h2>\n<p>For anything that needs to run before the first render — fetching a record, reading the caller, preparing a model — define <code>onLoad</code>. It receives the request context and runs during initialization.</p>\n<pre class=\"lang-razor\"><code>@page \"/orders/{id:Nat}\"\n\n@code {\n    import Orders \"../Services/Orders\";\n\n    var order : ?Orders.Order = null;\n\n    public func onLoad(ctx : Context) {\n        order := Orders.find(id);\n    };\n}\n\n@switch order {\n    case (?o) { &lt;PageHeader&gt;Order #@Nat.toText(o.id)&lt;/PageHeader&gt; }\n    case null { &lt;Alert type=\"warning\"&gt;Order not found.&lt;/Alert&gt; }\n}</code></pre>\n<p>Keep <code>onLoad</code> focused on loading. Mutations belong in event handlers, so the page stays cheap to re-render on every poll.</p>\n<h2 id=\"reading-context-caller-params-query\">Reading context: caller, params, query</h2>\n<p><code>onLoad</code> (and your handlers) can read the request <strong>context</strong> — who is calling and what they asked for.</p>\n<ul><li><strong><code>ctx.caller</code></strong> — the calling <code>Principal</code>. Use it to scope state to a user or to gate behavior.</li><li><strong>Route params</strong> — declared on <code>@page</code> and available directly by name, already typed. With <code>{id:Nat}</code>, <code>id</code> is a <code>Nat</code>.</li><li><strong>Query string</strong> — read named query values from the context.</li></ul>\n<pre class=\"lang-razor\"><code>@page \"/dashboard\"\n@authorize\n\n@code {\n    import Notes \"../Services/Notes\";\n\n    var mine : [Notes.Note] = [];\n    var filter : Text = \"all\";\n\n    public func onLoad(ctx : Context) {\n        mine := Notes.forOwner(ctx.caller);\n        filter := ctx.query(\"filter\");\n    };\n}\n\n&lt;PageHeader&gt;Your notes (@filter)&lt;/PageHeader&gt;</code></pre>\n<p>Here <code>ctx.caller</code> scopes the data to the signed-in principal, and <code>ctx.query(\"filter\")</code> reads <code>?filter=...</code> from the URL. Route params are simply in scope, so a <code>{id:Nat}</code> segment is used as <code>id</code> with no parsing.</p>\n<blockquote>Authenticated callers depend on <code>@authorize</code>. The directive works today; full Internet Identity login over HTTP and role stores are <strong>Roadmap</strong>, as noted in <a href=\"/docs/pages-and-routing\">Pages &amp; Routing</a>.</blockquote>\n<h2 id=\"putting-it-together\">Putting it together</h2>\n<p>Declare state, load it in <code>onLoad</code>, mutate it in handlers, and let MotoView re-render:</p>\n<pre class=\"lang-bash\"><code>motoview build\nmotoview dev</code></pre>\n<p>Next, wire interactions to your state with <a href=\"/docs/events\">Events</a>, or move durable data into a <a href=\"/docs/services\">Service</a>.</p>"; prevSlug = "validation"; prevTitle = "Validation"; nextSlug = "persistence"; nextTitle = "Upgrade-Stable Persistence" } };
        case ("persistence") { ?{ slug = "persistence"; title = "Upgrade-Stable Persistence"; section = "Interactivity"; html = "<h1 id=\"upgrade-stable-persistence\">Upgrade-Stable Persistence</h1>\n<p>By default, the state in a MotoView <a href=\"/docs/services\">service</a> lives in ordinary Motoko <code>var</code>s and collections, which persist for as long as the canister is <em>running</em> but reset on a code <strong>upgrade</strong> (<code>dfx deploy --mode upgrade</code>) — only Motoko <code>stable</code> variables survive an upgrade. For real apps you want your data to outlive deploys. MotoView makes that opt-in with two methods on a service.</p>\n<h2 id=\"opt-in-mvstablesave-mvstableload\">Opt in: <code>mvStableSave</code> / <code>mvStableLoad</code></h2>\n<p>A stateful service is a <code>public class Name()</code> (see <a href=\"/docs/services\">Services</a>). To make it upgrade-stable, add a snapshot pair:</p>\n<pre class=\"lang-motoko\"><code>// src/Services/Notes.mo\nimport HashMap \"mo:base/HashMap\";\nimport Iter \"mo:base/Iter\";\nimport Nat \"mo:base/Nat\";\nimport Hash \"mo:base/Hash\";\n\nmodule {\n  public class Notes() {\n    public type Note = { id : Nat; owner : Principal; body : Text };\n\n    var nextId : Nat = 1;\n    let notes = HashMap.HashMap&lt;Nat, Note&gt;(64, Nat.equal, Hash.hash);\n\n    public func add(owner : Principal, body : Text) : Nat {\n      let id = nextId; nextId += 1;\n      notes.put(id, { id; owner; body }); id;\n    };\n    public func all() : [Note] { Iter.toArray(notes.vals()) };\n\n    // ---- upgrade-stable persistence ----\n    // Snapshot to a RECORD (named fields), never a tuple: Motoko's\n    // to_candid/from_candid does NOT round-trip tuples — from_candid returns\n    // null and your state is silently wiped on every upgrade.\n    public func mvStableSave() : Blob {\n      to_candid ({ nextId = nextId; notes = Iter.toArray(notes.entries()) });\n    };\n    public func mvStableLoad(b : Blob) {\n      switch (from_candid (b) : ?{ nextId : Nat; notes : [(Nat, Note)] }) {\n        case (?saved) {\n          nextId := saved.nextId;\n          for (k in Iter.toArray(notes.keys()).vals()) { notes.delete(k) };\n          for ((k, v) in saved.notes.vals()) { notes.put(k, v) };\n        };\n        case null {};\n      };\n    };\n  };\n}</code></pre>\n<p>That's it. When the compiler sees <code>public func mvStableSave</code> in a service it generates, in the actor, a <code>stable var Notes__state : Blob</code> plus the <code>preupgrade</code>/<code>postupgrade</code> hooks that call your methods:</p>\n<pre class=\"lang-motoko\"><code>stable var Notes__state : Blob = \"\" : Blob;\nsystem func preupgrade()  { Notes__state := Notes.mvStableSave() };\nsystem func postupgrade() { Notes.mvStableLoad(Notes__state) };</code></pre>\n<p><code>preupgrade</code> snapshots your live state to the stable Blob just before the upgrade; <code>postupgrade</code> restores it into the fresh instance just after.</p>\n<h2 id=\"the-three-rules\">The three rules</h2>\n<ol><li><strong>Snapshot every mutable field into a RECORD.</strong> Put each <code>var</code> scalar and each</li></ol>\n<p>collection into the <code>to_candid</code> <strong>record</strong> as a named field — never a tuple. <code>to_candid</code>/<code>from_candid</code> cannot round-trip a tuple (it decodes to <code>null</code>, silently wiping your state on upgrade); a named record round-trips correctly. Maps → <code>Iter.toArray(map.entries())</code>, Buffers → <code>Buffer.toArray(buf)</code>. Immutable <code>let</code>s never need saving.</p>\n<ol><li><strong>Save and load must match exactly.</strong> The <code>from_candid</code> record annotation must</li></ol>\n<p>have the same field names and types as the <code>to_candid</code> record. Everything must be a <em>shared</em> type (records, variants, arrays, primitives, <code>Principal</code> — no functions or objects). If a record holds a <code>var</code> field or a <code>Buffer</code>, define a flat <em>snapshot</em> record for it and convert on save/load.</p>\n<ol><li><strong>Restore by replacing, not appending.</strong> On upgrade the constructor runs</li></ol>\n<p>first (re-seeding any starter data), <em>then</em> <code>postupgrade</code> runs. So clear each collection before refilling (<code>delete</code> keys / <code>Buffer.clear()</code>) and assign scalars — otherwise seeded data would accumulate. Done right, persistence is idempotent across repeated upgrades.</p>\n<h2 id=\"verifying\">Verifying</h2>\n<pre class=\"lang-bash\"><code>dfx deploy                       # install\n# ... create some data through the app ...\ndfx deploy --mode upgrade        # runs preupgrade -&gt; postupgrade\n# ... your data is still there ...</code></pre>\n<blockquote><strong>Scope.</strong> This is per-service opt-in with a Candid round-trip — simple and robust. A future option will use Motoko's enhanced orthogonal persistence to skip the serialization entirely, plus a schema-migration story for evolving service types.</blockquote>"; prevSlug = "state"; prevTitle = "State"; nextSlug = "drag-drop-and-effects"; nextTitle = "Drag & Drop, Effects & Services" } };
        case ("drag-drop-and-effects") { ?{ slug = "drag-drop-and-effects"; title = "Drag & Drop, Effects & Services"; section = "Interactivity"; html = "<h1 id=\"drag-drop-effects-services\">Drag &amp; Drop, Effects &amp; Services</h1>\n<p>The <a href=\"https://github.com/miadey/MotoView/tree/main/examples/crm\">CRM example</a> — a drag-and-drop sales-pipeline Kanban — exercises three capabilities that make MotoView apps feel alive without a line of frontend JavaScript: <strong>drag-and-drop</strong>, <strong>handler-triggered effects</strong>, and <strong>services</strong>.</p>\n<h2 id=\"drag-drop\">Drag &amp; drop</h2>\n<p>Drag-and-drop is server-driven. Mark a draggable item with <code>data-mv-drag</code> (its value is the argument passed to your handler) and a drop zone with <code>data-mv-drop</code> (the handler name) plus <code>data-mv-dropval</code> (a second argument). When the user drops, the WASM client dispatches <code>handler(dragValue, dropValue)</code> as a normal event.</p>\n<pre class=\"lang-razor\"><code>&lt;section class=\"kanban-col\" id=\"col-@stage\" data-mv-drop=\"dropDeal\" data-mv-dropval=\"@stage\"&gt;\n    @for deal in dealsIn(stage) {\n        &lt;article class=\"deal-card\" draggable=\"true\" data-mv-drag=\"@(deal.id)\"&gt;\n            &lt;span class=\"deal-title\"&gt;@deal.title&lt;/span&gt;\n        &lt;/article&gt;\n    }\n&lt;/section&gt;</code></pre>\n<pre class=\"lang-motoko\"><code>@code {\n    func dropDeal(id : Nat, stage : Text) : async () {\n        deals := Crm.move(deals, id, stage);\n        toast(\"Moved to \" # stage);\n        animate(\"#col-\" # stage, \"pulse\");\n    };\n}</code></pre>\n<p>The glue handles <code>dragstart</code>/<code>dragover</code>/<code>drop</code>, highlights the active drop zone (<code>.mv-drop-over</code>), and tilts the dragged card (<code>.mv-dragging</code>). You only write the handler.</p>\n<h2 id=\"effects-from-handlers\">Effects from handlers</h2>\n<p>Inside any <code>@code</code> function you can queue declarative effects that run on the client after the batch is applied:</p>\n<ul><li><code>toast(\"Deal added\")</code> — a transient notification.</li><li><code>animate(\"#panel\", \"shake\")</code> — play one of the built-in animations on a selector (see below).</li><li><code>focusOn(\"#email\")</code> — move focus to a selector.</li><li><code>scrollTo(\"#errors\")</code> — smooth-scroll an element into view.</li></ul>\n<p>Effects ride back in the render batch's <code>effects</code> array, so they fire exactly once per event — never on a poll.</p>\n<h2 id=\"animations\">Animations</h2>\n<p>MotoView ships a CSS animation library (transform/opacity only, GPU-friendly, with a <code>prefers-reduced-motion</code> guard). Play any of them from a handler with <code>animate(\"#sel\", \"name\")</code>, or declaratively on <strong>keyed list items</strong> as they're inserted and removed:</p>\n<pre class=\"lang-mview\"><code>&lt;ul&gt;\n@for item in items {\n    &lt;li key=\"@item.id\" enter=\"fade-up\" exit=\"slide-out-right\"&gt;@item.title&lt;/li&gt;\n}\n&lt;/ul&gt;</code></pre>\n<p>When the <a href=\"/docs/directives-reference\">keyed diff</a> inserts an item it plays its <code>enter</code> animation; when it removes one it plays <code>exit</code> and removes the node only after the animation ends. All of this is the WASM client toggling CSS classes — no application JavaScript.</p>\n<p>The library:</p>\n<ul><li><strong>Entrances</strong> — <code>fade-in</code>, <code>fade-up</code>, <code>fade-down</code>, <code>fade-left</code>, <code>fade-right</code>, <code>scale-in</code>, <code>zoom-in</code>, <code>pop</code>, <code>slide-in-left</code>, <code>slide-in-right</code>.</li><li><strong>Exits</strong> — <code>fade-out</code>, <code>fade-up-out</code>, <code>fade-down-out</code>, <code>scale-out</code>, <code>zoom-out</code>, <code>slide-out-left</code>, <code>slide-out-right</code>, <code>collapse</code>.</li><li><strong>Attention</strong> — <code>pulse</code>, <code>shake</code>, <code>bounce</code>, <code>wobble</code>, <code>flash</code>, <code>tada</code>, <code>spin</code> (loops), <code>pop</code>.</li></ul>\n<h2 id=\"services-models\">Services &amp; models</h2>\n<p>A <code>.mview</code> file is markup plus Motoko, but heavier logic belongs in plain Motoko modules. Drop them in <code>src/Services/<em>.mo</code> or <code>src/Models/</em>.mo</code> and the compiler imports them into the generated actor automatically — page <code>@code</code> can then call them.</p>\n<pre class=\"lang-motoko\"><code>// src/Services/Crm.mo  — a stateless service (a Motoko module is a library)\nmodule {\n    public type Deal = { id : Nat; title : Text; company : Text; value : Nat; stage : Text };\n    public func byStage(deals : [Deal], s : Text) : [Deal] {\n        Array.filter&lt;Deal&gt;(deals, func(d) { d.stage == s });\n    };\n    public func move(deals : [Deal], id : Nat, stage : Text) : [Deal] { /* ... */ };\n}</code></pre>\n<pre class=\"lang-razor\"><code>@code {\n    var deals : [Crm.Deal] = Crm.seed();           // state lives in the page\n    func dealsIn(s : Text) : [Crm.Deal] { Crm.byStage(deals, s) };\n}</code></pre>\n<blockquote>A Motoko <code>module</code> is stateless, so keep mutable state (the <code>var deals</code>) in the page <code>@code</code> and let the service provide pure transforms. See <a href=\"/docs/state\">State</a>.</blockquote>\n<h2 id=\"run-the-crm\">Run the CRM</h2>\n<pre class=\"lang-bash\"><code>cd examples/crm\ndfx start --background &amp;&amp; dfx deploy\n# open http://&lt;canister-id&gt;.localhost:4955/  and drag a card between columns</code></pre>"; prevSlug = "persistence"; prevTitle = "Upgrade-Stable Persistence"; nextSlug = "styling-and-themes"; nextTitle = "Styling & Themes" } };
        case ("styling-and-themes") { ?{ slug = "styling-and-themes"; title = "Styling & Themes"; section = "Styling"; html = "<h1 id=\"styling-themes\">Styling &amp; Themes</h1>\n<p>MotoView gives you three levels of styling, and the trick to a clean codebase is knowing which one to reach for. Start with <strong>semantic components</strong> for everyday UI, drop into <strong><code>@style</code></strong> when a page needs something specific, and use <strong><code>@theme</code> tokens</strong> to set the colors, spacing, and fonts that everything else inherits.</p>\n<p>The goal is simple: write expressive markup, not utility-class soup.</p>\n<p>Under the hood, the design foundation is the <strong>authentic Microsoft Fluent UI 2</strong> design-token system — the real <code>--colorNeutral*</code> / <code>--colorBrand*</code> ramps, type ramp, spacing scale, radii, shadows, and motion curves. Components read those tokens, so changing one token (or one brand color) re-themes the whole app, in light and dark, with no JavaScript.</p>\n<h2 id=\"level-1-semantic-components\">Level 1: Semantic components</h2>\n<p>Most of your UI is buttons, cards, alerts, inputs, and tables. MotoView ships these as built-in components so you describe <em>what</em> a thing is, not <em>how</em> to draw it.</p>\n<pre class=\"lang-razor\"><code>@page \"/dashboard\"\n\n&lt;PageHeader&gt;Dashboard&lt;/PageHeader&gt;\n\n&lt;Card title=\"Account\"&gt;\n  &lt;Badge type=\"success\"&gt;Active&lt;/Badge&gt;\n  &lt;p&gt;You have @invoices.size() open invoices.&lt;/p&gt;\n  &lt;Button kind=\"primary\" size=\"lg\" @click=\"renew\"&gt;Renew plan&lt;/Button&gt;\n&lt;/Card&gt;\n\n&lt;Alert type=\"warning\"&gt;Your trial ends in 3 days.&lt;/Alert&gt;</code></pre>\n<p>Compare that to a wall of class names. The component carries the styling, and the styling stays consistent everywhere it's used. Built-in components include <code>Button</code> (<code>kind</code>, <code>size</code>), <code>Card</code> (<code>title</code>), <code>Alert</code> and <code>Badge</code> (<code>type</code>), the inputs <code>InputText</code> / <code>InputEmail</code> / <code>InputNumber</code> / <code>TextArea</code>, plus <code>ValidationSummary</code>, <code>Table</code>, <code>PageHeader</code>, and <code>Grid</code> (<code>columns</code>).</p>\n<p>When you need your own reusable piece, build a component in <code>src/Components/</code> — see <a href=\"/docs/components\">Components</a>.</p>\n<h2 id=\"level-2-style-local-css\">Level 2: <code>@style</code> local CSS</h2>\n<p>When a page needs styling that doesn't belong in a shared component, use a <code>@style</code> block. The CSS is scoped to that page, so you can write plain selectors without worrying about collisions.</p>\n<pre class=\"lang-razor\"><code>@page \"/pricing\"\n\n&lt;div class=\"plans\"&gt;\n  @for plan in plans {\n    &lt;Card title=\"@plan.name\"&gt;\n      &lt;span class=\"price\"&gt;@plan.price&lt;/span&gt;\n    &lt;/Card&gt;\n  }\n&lt;/div&gt;\n\n@style {\n  .plans {\n    display: grid;\n    grid-template-columns: repeat(3, 1fr);\n    gap: var(--spacingHorizontalL);\n  }\n  .price {\n    font-size: var(--fontSizeBase600);\n    font-weight: var(--fontWeightBold);\n    color: var(--colorBrandForeground1);\n  }\n}</code></pre>\n<p>Notice the <code>var(--colorBrandForeground1)</code> and <code>var(--spacingHorizontalL)</code> references — local CSS should still read from <strong>theme tokens</strong> so a single theme change updates the whole app, and so dark mode and the theme picker keep working automatically.</p>\n<h2 id=\"the-fluent-2-token-foundation\">The Fluent 2 token foundation</h2>\n<p>The base stylesheet (<code>/motoview.css</code>, served by the canister) opens with the full Fluent 2 token set, resolved to literal values. <strong>Light</strong> is <code>createLightTheme(brandWeb)</code>; <strong>dark</strong> is <code>createDarkTheme(brandWeb)</code>; the brand is Fluent's <em>Communication blue</em> (<code>brandWeb</code>, ramp anchor <code>#0f6cbd</code> at shade 80).</p>\n<p>These are real CSS custom properties — read them anywhere with <code>var(--token)</code>.</p>\n<h3 id=\"color-tokens\">Color tokens</h3>\n<p>The brand ramp is sixteen shades, <code>--colorBrandBackground10</code> … <code>160</code>:</p>\n<pre class=\"lang-css\"><code>--colorBrandBackground10:  #061724;   /* darkest  */\n--colorBrandBackground80:  #0f6cbd;   /* the anchor / primary brand */\n--colorBrandBackground160: #ebf3fc;   /* lightest */</code></pre>\n<p>On top of the ramp sit the <strong>semantic color aliases</strong>. A representative slice:</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Group</th><th>Example tokens</th></tr></thead><tbody><tr><td>Neutral foreground</td><td><code>--colorNeutralForeground1</code> … <code>4</code>, <code>…OnBrand</code>, <code>…Disabled</code>, <code>…Inverted</code></td></tr><tr><td>Neutral background</td><td><code>--colorNeutralBackground1</code> … <code>6</code>, <code>…1Hover/Pressed/Selected</code>, <code>…Disabled</code></td></tr><tr><td>Subtle background</td><td><code>--colorSubtleBackground</code>, <code>…Hover</code>, <code>…Pressed</code>, <code>…Selected</code></td></tr><tr><td>Neutral stroke</td><td><code>--colorNeutralStroke1</code> … <code>3</code>, <code>…Accessible</code>, <code>…StrokeSubtle</code>, <code>…Disabled</code></td></tr><tr><td>Brand foreground</td><td><code>--colorBrandForeground1</code> / <code>2</code>, <code>--colorBrandForegroundLink</code>, <code>…Hover/Pressed</code></td></tr><tr><td>Brand background</td><td><code>--colorBrandBackground</code>, <code>…Hover</code>, <code>…Pressed</code>, <code>…Selected</code>, <code>--colorBrandBackground2</code></td></tr><tr><td>Compound brand</td><td><code>--colorCompoundBrandForeground1</code>, <code>…Background</code>, <code>…Stroke</code> (+ hover/pressed)</td></tr><tr><td>Brand stroke</td><td><code>--colorBrandStroke1</code> / <code>2</code>, <code>…2Hover</code>, <code>…2Pressed</code></td></tr><tr><td>Focus</td><td><code>--colorStrokeFocus1</code> (inner), <code>--colorStrokeFocus2</code> (outer)</td></tr><tr><td>Status: danger</td><td><code>--colorStatusDangerForeground1..3</code>, <code>…Background1..3</code>, <code>…Border1..2</code></td></tr><tr><td>Status: success</td><td><code>--colorStatusSuccessForeground1..3</code>, <code>…Background1..3</code>, <code>…Border1..2</code></td></tr><tr><td>Status: warning</td><td><code>--colorStatusWarningForeground1..3</code>, <code>…Background1..3</code>, <code>…Border1..2</code></td></tr><tr><td>Status: info</td><td><code>--colorStatusInfoForeground1..2</code>, <code>…Background1..2</code>, <code>…Border1</code></td></tr><tr><td>Palettes</td><td><code>--colorPaletteRed*</code>, <code>…Green*</code>, <code>…DarkOrange*</code>, <code>…Yellow*</code></td></tr></tbody></table>\n<h3 id=\"type-ramp\">Type ramp</h3>\n<pre class=\"lang-css\"><code>--fontFamilyBase:      'Segoe UI', -apple-system, BlinkMacSystemFont, Roboto, sans-serif;\n--fontFamilyMonospace: Consolas, 'Courier New', Courier, monospace;\n--fontFamilyNumeric:   Bahnschrift, 'Segoe UI', …;\n\n--fontSizeBase100..600:  10/12/14/16/20/24px\n--fontSizeHero700..1000: 28/32/40/68px\n--lineHeightBase100..600 + --lineHeightHero700..1000   /* matched line heights */\n\n--fontWeightRegular: 400;  --fontWeightMedium: 500;\n--fontWeightSemibold: 600; --fontWeightBold: 700;</code></pre>\n<p>Ready-made type styles are exposed as classes: <code>.fui-text-caption1</code>, <code>.fui-text-body1</code>, <code>.fui-text-body1-strong</code>, <code>.fui-text-subtitle2/1</code>, <code>.fui-text-title3/2/1</code>, <code>.fui-text-largetitle</code>.</p>\n<h3 id=\"spacing-radii-strokes\">Spacing, radii, strokes</h3>\n<pre class=\"lang-css\"><code>--spacingHorizontalNone/XXS/XS/SNudge/S/MNudge/M/L/XL/XXL/XXXL   /* 0,2,4,6,8,10,12,16,20,24,32px */\n--spacingVerticalNone/XXS/…/XXXL                                  /* same ladder, vertical */\n--borderRadiusNone/Small/Medium/Large/XLarge/Circular            /* 0,2,4,6,8,10000px */\n--strokeWidthThin/Thick/Thicker/Thickest                         /* 1,2,3,4px */</code></pre>\n<h3 id=\"shadows-elevation\">Shadows (elevation)</h3>\n<pre class=\"lang-css\"><code>--shadow2 / 4 / 8 / 16 / 28 / 64           /* ambient + key, depth ladder */\n--shadow2Brand / …64Brand                  /* brand-tinted variants */</code></pre>\n<p>Shadows are the one set of <em>colored</em> tokens that differs by theme — they deepen in dark mode for the same perceived elevation.</p>\n<h3 id=\"motion\">Motion</h3>\n<pre class=\"lang-css\"><code>--durationUltraFast/Faster/Fast/Normal/Gentle/Slow/Slower/UltraSlow  /* 50…500ms */\n--curveAccelerate(Max/Mid/Min)  --curveDecelerate(Max/Mid/Min)\n--curveEasyEase  --curveEasyEaseMax  --curveLinear</code></pre>\n<p>The type ramp, spacing, radii, stroke widths, durations and curves are theme-independent — only the <em>color</em> tokens (and shadows) change between light and dark.</p>\n<h3 id=\"legacy-mv-aliases\">Legacy <code>--mv-*</code> aliases</h3>\n<p>Earlier MotoView used a small <code>--mv-*</code> palette. Those tokens still exist as <strong>thin aliases onto Fluent</strong>, so older code and <code>@theme { … }</code> overrides keep working:</p>\n<pre class=\"lang-css\"><code>--mv-primary:    var(--colorBrandBackground);\n--mv-primary-600:var(--colorBrandBackgroundHover);\n--mv-primary-fg: var(--colorNeutralForegroundOnBrand);\n--mv-bg:         var(--colorNeutralBackground2);\n--mv-surface:    var(--colorNeutralBackground1);\n--mv-muted:      var(--colorNeutralBackground3);\n--mv-border:     var(--colorNeutralStroke2);\n--mv-text:       var(--colorNeutralForeground1);\n--mv-text-soft:  var(--colorNeutralForeground3);\n--mv-success:    var(--colorStatusSuccessForeground3);\n--mv-danger:     var(--colorStatusDangerForeground3);\n--mv-warning:    var(--colorStatusWarningForeground3);\n--mv-radius:     var(--borderRadiusXLarge);\n--mv-radius-sm:  var(--borderRadiusMedium);\n--mv-font:       var(--fontFamilyBase);\n--mv-shadow:     var(--shadow4);</code></pre>\n<p>Prefer the Fluent tokens in new code; reach for <code>--mv-*</code> only when you're matching existing markup. Both flip correctly in dark mode and under the theme picker.</p>\n<h2 id=\"level-3-theme-design-tokens-theme-packages\">Level 3: <code>@theme</code> — design tokens &amp; theme packages</h2>\n<p><code>@theme</code> sets the CSS variables (design tokens) that power semantic components, the base stylesheet, and your own <code>@style</code> blocks. Put it in a <a href=\"/docs/layouts\">layout</a> once and every page using that layout inherits it. It compiles to a <code>&lt;style&gt;:root{ … }&lt;/style&gt;</code> injected into <code>&lt;head&gt;</code> <em>after</em> the base stylesheet, so your values win.</p>\n<p>The three forms can be combined on one directive, and they apply in this order (later wins): <strong>brand ramp → preset → token overrides</strong>.</p>\n<pre class=\"lang-razor\"><code>@theme brand=\"#d13438\" \"ocean\" { --borderRadiusMedium: 8px; }</code></pre>\n<h3 id=\"brand-your-whole-app-from-one-color\">Brand your whole app from one color</h3>\n<p>Give <code>@theme</code> a single brand color and the compiler generates the full Fluent brand ramp (16 shades) and wires every brand alias token — for <strong>light AND dark</strong> — the same way Fluent's <code>createLightTheme</code> / <code>createDarkTheme(brandRamp)</code> do:</p>\n<pre class=\"lang-razor\"><code>@theme brand=\"#d13438\"</code></pre>\n<p>That one line themes every component (buttons, links, accents, focus rings) to your brand, in both light and dark mode.</p>\n<p><strong>How it works</strong> (<code>compiler/src/color.rs</code>): the input color is converted to HSL and anchored at <strong>shade 80</strong>; the other 15 shades follow the reference ramp's lightness ladder with brand-matched saturation, so any color yields a cohesive, Fluent-shaped ramp. The compiler then emits:</p>\n<ul><li>the 16 <code>--colorBrandBackground10..160</code> variables, plus</li><li>every brand <strong>alias</strong> (<code>--colorBrandBackground</code>, <code>…Hover</code>, <code>--colorBrandForeground1</code>,</li></ul>\n<p><code>--colorBrandForegroundLink</code>, <code>--colorBrandStroke1</code>, the compound-brand tokens, …) picked from the ramp at the <em>light</em> shade in <code>:root</code> and the <em>dark</em> shade under <code>[data-theme=\"dark\"]</code> and <code>@media (prefers-color-scheme: dark)</code>.</p>\n<p>The light/dark shade mapping for each alias lives in <code>compiler/src/brand_aliases.rs</code> (derived from Fluent's <code>createLightTheme</code> / <code>createDarkTheme(brandWeb)</code>). For example <code>--colorBrandBackground</code> uses ramp shade <strong>80</strong> in light and <strong>70</strong> in dark; <code>--colorBrandForeground1</code> uses <strong>80</strong> in light and <strong>100</strong> in dark.</p>\n<pre class=\"lang-css\"><code>/* what @theme brand=\"#d13438\" compiles to (abridged) */\n:root{\n  --colorBrandBackground10:#…; … --colorBrandBackground160:#…;\n  --colorBrandBackground:#d13438; --colorBrandForeground1:#d13438; …\n}\n[data-theme=\"dark\"]{ --colorBrandBackground:#…; --colorBrandForeground1:#…; … }\n@media (prefers-color-scheme: dark){\n  :root:not([data-theme=\"light\"]){ /* same dark brand aliases */ }\n}</code></pre>\n<h3 id=\"apply-a-built-in-theme-package\">Apply a built-in theme package</h3>\n<p>The framework ships five ready-made, accessibility-checked presets (WCAG-AA contrast). Apply one by name:</p>\n<pre class=\"lang-razor\"><code>@theme \"midnight\"</code></pre>\n<table class=\"mv-doc-table\"><thead><tr><th>Name</th><th>Look</th></tr></thead><tbody><tr><td><code>midnight</code></td><td>dark — deep indigo-black, violet primary</td></tr><tr><td><code>ocean</code></td><td>light — cool blue/teal</td></tr><tr><td><code>forest</code></td><td>light — natural green, warm neutrals</td></tr><tr><td><code>sunset</code></td><td>light — warm coral on cream</td></tr><tr><td><code>slate</code></td><td>light — minimal near-grayscale, slate primary</td></tr></tbody></table>\n<p>Presets are sets of <code>--mv-*</code> overrides (see <code>theme_preset</code> in <code>compiler/src/codegen.rs</code>), so they layer cleanly on top of the Fluent base.</p>\n<h3 id=\"override-individual-tokens\">Override individual tokens</h3>\n<p>Set any tokens directly — Fluent or <code>--mv-*</code> — with or without a preset. Overrides win over the preset:</p>\n<pre class=\"lang-razor\"><code>@theme \"ocean\" {\n  --colorBrandBackground: #0d9488;\n  --borderRadiusMedium: 4px;\n}</code></pre>\n<p>Because tokens are ordinary CSS custom properties, your own <code>@style</code> and components read them with <code>var(--colorBrandBackground)</code> — so changing a brand color is a one-line edit, not a search-and-replace through your markup.</p>\n<h2 id=\"light-dark\">Light &amp; dark</h2>\n<p>Dark mode is built in and requires <strong>no JavaScript and no extra wiring</strong>:</p>\n<ul><li><strong>Automatic</strong> — <code>@media (prefers-color-scheme: dark)</code> flips every color token to</li></ul>\n<p>the dark theme, following the user's OS setting.</p>\n<ul><li><strong>Forced</strong> — set <code>data-theme</code> on the root to override the OS:</li></ul>\n<p><code>&lt;html data-theme=\"dark\"&gt;</code> or <code>&lt;html data-theme=\"light\"&gt;</code>. The dark media query is scoped <code>:root:not([data-theme])</code>, so an explicit choice always wins.</p>\n<p>Every semantic component and any <code>@style</code> that reads tokens flips automatically. The base stylesheet is served by the canister at <code>/motoview.css</code>, alongside <code>/motoview.wasm</code> and <code>/motoview.js</code>.</p>\n<h3 id=\"let-the-user-choose-themetoggle\">Let the user choose: <code>&lt;ThemeToggle /&gt;</code></h3>\n<p>Drop the built-in toggle anywhere (an app bar, a nav, a settings page):</p>\n<pre class=\"lang-razor\"><code>&lt;ThemeToggle /&gt;</code></pre>\n<p>It's a framework primitive, so there's still <strong>no application JavaScript</strong>. It compiles to a single button with <code>data-mv-theme-toggle</code>. Clicking it:</p>\n<ol><li>flips <code>&lt;html data-theme=\"light|dark\"&gt;</code> instantly, and</li><li>saves the choice in an <code>mv_theme</code> cookie (<code>path=/; max-age=1y; samesite=lax</code>).</li></ol>\n<p>On every subsequent load the runtime injects a tiny <strong>inline head script</strong> that re-applies the saved theme <strong>before first paint</strong> — so there's no flash of the wrong theme, and it works on certified, cacheable pages too (whose certificate can't vary by cookie, so the theme has to be applied client-side from the cookie):</p>\n<pre class=\"lang-html\"><code>&lt;script&gt;(function(){try{var m=document.cookie.match(\n  /(?:^|; )mv_theme=(web-light|web-dark|teams-light|teams-dark|hc|light|dark)/);\n  if(m)document.documentElement.setAttribute('data-theme',m[1]);}catch(e){}})();&lt;/script&gt;</code></pre>\n<p>With no saved choice it follows <code>prefers-color-scheme</code>. The sun/moon icon is <strong>pure CSS</strong> driven by <code>[data-theme]</code> — it shows the theme you'll switch <em>to</em> (moon in light, sun in dark).</p>\n<h3 id=\"the-full-fluent-theme-set-themepicker\">The full Fluent theme set: <code>&lt;ThemePicker /&gt;</code></h3>\n<p>For the complete official Fluent theme lineup, drop in the picker — a dropdown just like the Fluent UI docs:</p>\n<pre class=\"lang-razor\"><code>&lt;ThemePicker /&gt;</code></pre>\n<p>It offers the five official themes:</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Picker option</th><th><code>data-theme</code> key</th><th>Brand</th></tr></thead><tbody><tr><td><strong>Web Light</strong></td><td><code>web-light</code></td><td>Communication blue (the <code>:root</code> base)</td></tr><tr><td><strong>Web Dark</strong></td><td><code>web-dark</code></td><td>Communication blue on the dark base</td></tr><tr><td><strong>Teams Light</strong></td><td><code>teams-light</code></td><td>Teams purple (<code>#5b5fc7</code>) on the light base</td></tr><tr><td><strong>Teams Dark</strong></td><td><code>teams-dark</code></td><td>Teams purple on the dark base</td></tr><tr><td><strong>High Contrast</strong></td><td><code>hc</code></td><td>black / white / yellow (cyan hover)</td></tr></tbody></table>\n<p><strong>How it works end to end:</strong></p>\n<ol><li><strong>The markup</strong> — <code>ThemePicker</code> compiles to a CSS-only <code>&lt;details&gt;</code> dropdown</li></ol>\n<p>(<code>.mv-theme-picker</code>). Each option is a <code>&lt;button data-mv-theme-set=\"&lt;key&gt;\"&gt;</code> with a color swatch and a label. No app JS in the markup at all.</p>\n<ol><li><strong>The CSS layers</strong> (<code>client/glue/motoview.css</code>) — the named themes are *layered</li></ol>\n<p>on the light/dark neutral bases*, not redefined from scratch:</p>\n<ul><li><code>web-light</code> is the plain <code>:root</code> light base; <code>web-dark</code> shares the same block as</li></ul>\n<p><code>[data-theme=\"dark\"]</code> and <code>[data-theme=\"teams-dark\"]</code>, so it inherits the full dark neutral palette.</p>\n<ul><li><code>[data-theme=\"teams-light\"]</code> / <code>[data-theme=\"teams-dark\"]</code> override only the</li></ul>\n<p><strong>brand</strong> tokens to Teams purple, sitting on the light/dark neutrals respectively.</p>\n<ul><li><code>[data-theme=\"hc\"]</code> is a complete high-contrast token set: black backgrounds,</li></ul>\n<p>white text/strokes, yellow brand, cyan hover.</p>\n<ol><li><strong><code>!important</code> on named-theme brand tokens</strong> — the Teams and HC brand overrides</li></ol>\n<p>use <code>!important</code>. This is deliberate: it lets a user's picked theme <strong>override the app's own <code>@theme brand</code></strong>. An app may set its brand in a layout; if the user then picks Teams or High Contrast from the picker, their choice wins.</p>\n<p>``<code>css [data-theme=\"teams-light\"] { --colorBrandBackground: #5b5fc7 !important; --colorBrandForeground1: #5b5fc7 !important; /<em> … </em>/ } </code>``</p>\n<ol><li><strong>The glue</strong> (<code>client/glue/motoview.js</code>) — a single delegated click handler:</li><li><code>[data-mv-theme-set]</code> → <code>mvSetTheme(key)</code> sets <code>&lt;html data-theme=key&gt;</code> and</li></ol>\n<p>writes the <code>mv_theme</code> cookie, closes the dropdown, then calls <code>mvPaintThemePicker()</code>.</p>\n<ul><li><code>mvPaintThemePicker()</code> reads the current <code>data-theme</code>, marks the matching option</li></ul>\n<p>with <code>aria-current=\"true\"</code> (the CSS shows a check + semibold label), and updates the summary's <code>.mv-theme-picker-label</code> to the active theme's name. It runs on load too, so the picker always reflects the saved choice. (It normalizes the toggle's <code>light</code>/<code>dark</code> keys to <code>web-light</code>/<code>web-dark</code>, and falls back to <code>prefers-color-scheme</code> when no <code>data-theme</code> is set.)</p>\n<ol><li><strong>No-flash + persistence</strong> — exactly the same <code>mv_theme</code> cookie and inline head</li></ol>\n<p>script as the toggle. The head regex explicitly allows the picker's keys (<code>web-light|web-dark|teams-light|teams-dark|hc|light|dark</code>), so the picked theme is applied before first paint on every load.</p>\n<ol><li><strong>Works on certified/cacheable pages</strong> — because the theme is applied **entirely</li></ol>\n<p>client-side from the cookie** (head script + glue), it never changes the server's HTML or its certificate. A <code>@cacheable</code> certified page and a dynamic page behave identically: the cached HTML ships neutral, then the head script paints the user's theme instantly.</p>\n<p><code>ThemeToggle</code> and <code>ThemePicker</code> can coexist — they share the cookie, and <code>mvPaintThemePicker</code> understands both key spaces.</p>\n<h2 id=\"the-vibrant-per-surface-accent-system-bzzz\">The vibrant per-surface accent system (Bzzz)</h2>\n<p>Fluent's neutral palette is the right default for <em>most</em> of an app, but a multi-surface product often wants each area to carry its own hue (Discord blurple, WhatsApp green, X blue, the forum's violet). Bzzz layers a small <strong>per-surface accent system</strong> on top of the Fluent tokens — vivid where it counts, neutral everywhere else.</p>\n<p>A surface declares two custom properties and the shared accent classes do the rest:</p>\n<pre class=\"lang-css\"><code>/* the layout sets sensible defaults… */\n.bz-content { --bz-accent: var(--colorBrandBackground); --bz-accent-2: #7c3aed; }</code></pre>\n<pre class=\"lang-razor\"><code>@* …and a page overrides them for its own hue *@\n&lt;div class=\"fd-wrap\" style=\"--bz-accent: #1d9bf0; --bz-accent-2: #0ea5e9;\"&gt;\n  &lt;div class=\"bz-banner bz-banner-row\"&gt;\n    &lt;span class=\"bz-banner-ico\"&gt;📰&lt;/span&gt;\n    &lt;div&gt;&lt;h1&gt;Feed&lt;/h1&gt;&lt;p&gt;What everyone's buzzing about.&lt;/p&gt;&lt;/div&gt;\n  &lt;/div&gt;\n\n  &lt;h2 class=\"bz-sec-title\"&gt;Trending&lt;/h2&gt;\n  &lt;span class=\"bz-accent-chip\"&gt;#motoview&lt;/span&gt;\n\n  &lt;article class=\"bz-card bz-card-accent\"&gt; … &lt;/article&gt;\n&lt;/div&gt;</code></pre>\n<table class=\"mv-doc-table\"><thead><tr><th>Class / token</th><th>What it does</th></tr></thead><tbody><tr><td><code>--bz-accent</code> / <code>--bz-accent-2</code></td><td>the surface's two-stop accent (used as a gradient). Set per surface; inherits to children</td></tr><tr><td><code>.bz-banner</code></td><td>a full-bleed gradient hero (<code>linear-gradient(120deg, accent → accent-2)</code>), white text, soft accent-tinted shadow, a radial sheen via <code>::after</code></td></tr><tr><td><code>.bz-banner-row</code> / <code>.bz-banner-ico</code> / <code>.bz-banner-actions</code></td><td>banner layout helpers (icon + heading row, action buttons)</td></tr><tr><td><code>.bz-sec-title</code></td><td>a section heading with a small gradient bar before it (<code>::before</code>)</td></tr><tr><td><code>.bz-accent-chip</code></td><td>a pill in the accent color over a <code>color-mix</code> accent tint of the surface background</td></tr><tr><td><code>.bz-card-accent</code></td><td>a <code>.bz-card</code> with a 3px gradient accent edge across the top (<code>::before</code>)</td></tr></tbody></table>\n<p>Because the gradients and tints are built from <code>--bz-accent</code> / <code>color-mix(... var(--bz-accent) ...)</code>, a single surface can re-theme its banners, chips, titles and card edges by setting just those two variables — and because the neutrals underneath are still Fluent tokens, the surface remains correct in dark mode.</p>\n<p><strong>High Contrast still wins.</strong> When the user picks High Contrast, dedicated <code>[data-theme=\"hc\"]</code> rules flatten the vibrant gradients to solid HC colors — banners become solid black with a yellow border, accent bars and chips become yellow on black — so accessibility is never sacrificed for flair:</p>\n<pre class=\"lang-css\"><code>[data-theme=\"hc\"] .bz-banner { background:#000 !important; border:2px solid #ff0;\n  box-shadow:none; color:#fff; }\n[data-theme=\"hc\"] .bz-card-accent::before,\n[data-theme=\"hc\"] .bz-sec-title::before { background:#ff0 !important; }\n[data-theme=\"hc\"] .bz-accent-chip { background:#000 !important; color:#ff0 !important;\n  box-shadow: inset 0 0 0 1px #ff0; }</code></pre>\n<h2 id=\"avoiding-utility-class-soup\">Avoiding utility-class soup</h2>\n<p>The pattern across all three levels is the same: push styling decisions <em>down</em> into tokens and components, and keep your markup describing intent.</p>\n<pre class=\"lang-razor\"><code>@* Reach for this *@\n&lt;Button kind=\"primary\" size=\"lg\"&gt;Save&lt;/Button&gt;\n\n@* Not this *@\n&lt;button class=\"inline-flex items-center px-4 py-2 rounded-md\n  bg-blue-600 text-white font-medium hover:bg-blue-700\"&gt;Save&lt;/button&gt;</code></pre>\n<p>A rule of thumb:</p>\n<ul><li><strong>Semantic component</strong> if it's a common UI element — let the component own the look.</li><li><strong><code>@style</code></strong> if it's genuinely page-specific layout or one-off styling — and read</li></ul>\n<p>Fluent tokens from it so dark mode and the theme picker keep working.</p>\n<ul><li><strong><code>@theme</code></strong> for anything visual you'd want to change in one place: brand color,</li></ul>\n<p>spacing scale, radius, fonts.</p>\n<p>Build and deploy as usual:</p>\n<pre class=\"lang-bash\"><code>motoview build\nmotoview dev</code></pre>\n<p>When you're ready to make pages interactive, head to <a href=\"/docs/events\">Events</a> and <a href=\"/docs/forms\">Forms &amp; Validation</a>.</p>"; prevSlug = "drag-drop-and-effects"; prevTitle = "Drag & Drop, Effects & Services"; nextSlug = "svg"; nextTitle = "SVG Interfaces" } };
        case ("svg") { ?{ slug = "svg"; title = "SVG Interfaces"; section = "Styling"; html = "<h1 id=\"svg-interfaces\">SVG Interfaces</h1>\n<p>SVG is just markup, and MotoView treats it that way. Because a <code>.mview</code> template is rendered server-side into HTML, you can write <code>&lt;svg&gt;</code> directly in your page, loop over Motoko data to draw nodes and edges, and wire <code>@click</code> onto individual shapes. The result is a fully server-driven diagram: no charting library, no client JavaScript, and the same event-update-batch cycle that powers the rest of the framework.</p>\n<p>This makes MotoView a natural fit for dashboards, flow diagrams, and network maps — anything where the picture <em>is</em> the data living in your canister.</p>\n<h2 id=\"svg-is-first-class-markup\">SVG is first-class markup</h2>\n<p>There is nothing special to enable. Drop an <code>&lt;svg&gt;</code> element into a template and use the directives you already know — <a href=\"/docs/control-flow\"><code>@for</code></a>, <code>@if</code>, inline output <code>@(expr)</code>, and <a href=\"/docs/events\">event handlers</a> — on its children.</p>\n<pre class=\"lang-razor\"><code>@page \"/dashboard\"\n@title \"Live metrics\"\n\n@code {\n  type Bar = { label : Text; value : Nat };\n  var bars : [Bar] = [\n    { label = \"Reads\"; value = 80 },\n    { label = \"Writes\"; value = 45 },\n    { label = \"Errors\"; value = 12 },\n  ];\n}\n\n&lt;svg viewBox=\"0 0 320 160\" role=\"img\" aria-label=\"Request volume\"&gt;\n  @for bar in bars {\n    &lt;rect x=\"@(bar.value)\" y=\"10\" width=\"@(bar.value)\" height=\"24\" rx=\"3\" /&gt;\n    &lt;text x=\"6\" y=\"28\"&gt;@bar.label — @bar.value&lt;/text&gt;\n  }\n&lt;/svg&gt;</code></pre>\n<p>Each value is evaluated on the server at render time and baked into the attribute. Because rendering is a query, the browser sees finished SVG — which means search engines and screen readers do too. Add <code>role</code> and <code>aria-label</code> so your diagram stays accessible.</p>\n<h2 id=\"looping-over-nodes-and-links\">Looping over nodes and links</h2>\n<p>A diagram is usually two collections: the things, and the connections between them. Model both in your <a href=\"/docs/code-blocks\"><code>@code</code></a> block and draw them with two loops — links first so they sit behind the nodes.</p>\n<pre class=\"lang-razor\"><code>@code {\n  type Node = { id : Text; x : Nat; y : Nat; label : Text };\n  type Link = { x1 : Nat; y1 : Nat; x2 : Nat; y2 : Nat };\n\n  var nodes : [Node] = [\n    { id = \"api\"; x = 40;  y = 60; label = \"API\" },\n    { id = \"db\";  x = 200; y = 60; label = \"DB\" },\n  ];\n  var links : [Link] = [\n    { x1 = 40; y1 = 60; x2 = 200; y2 = 60 },\n  ];\n\n  var selected : ?Text = null;\n\n  public func select(id : Text) : async () {\n    selected := ?id;\n  };\n}\n\n&lt;svg viewBox=\"0 0 280 120\" class=\"map\"&gt;\n  @for link in links {\n    &lt;line x1=\"@link.x1\" y1=\"@link.y1\" x2=\"@link.x2\" y2=\"@link.y2\" class=\"edge\" /&gt;\n  }\n\n  @for node in nodes {\n    &lt;g class=\"node\" @click=\"select(node.id)\"&gt;\n      &lt;circle cx=\"@node.x\" cy=\"@node.y\" r=\"18\" /&gt;\n      &lt;text x=\"@node.x\" y=\"@node.y\" text-anchor=\"middle\"&gt;@node.label&lt;/text&gt;\n    &lt;/g&gt;\n  }\n&lt;/svg&gt;</code></pre>\n<h2 id=\"clicking-shapes\">Clicking shapes</h2>\n<p><code>@click</code> works on any SVG element — a <code>&lt;circle&gt;</code>, a <code>&lt;rect&gt;</code>, or a wrapping <code>&lt;g&gt;</code>. Handler arguments are evaluated server-side and baked into <code>data-mv-arg*</code> attributes, then forwarded to your typed Motoko function. So <code>@click=\"select(node.id)\"</code> dispatches to <code>select</code> with the node's id; you never touch the wire format.</p>\n<p>Use the resulting server state to drive appearance with <code>@if</code> or <a href=\"/docs/control-flow\"><code>@switch</code></a>:</p>\n<pre class=\"lang-razor\"><code>@for node in nodes {\n  &lt;g @click=\"select(node.id)\"\n     class=\"@(selected == ?node.id ? \"node selected\" else \"node\")\"&gt;\n    &lt;circle cx=\"@node.x\" cy=\"@node.y\" r=\"18\" /&gt;\n  &lt;/g&gt;\n}</code></pre>\n<p>After a click the server returns a new batch immediately, the WASM client swaps <code>#mv-root</code>, and the selected node redraws. No round-trip you have to manage by hand.</p>\n<h2 id=\"styling-the-diagram\">Styling the diagram</h2>\n<p>Co-locate styles with the page using a <a href=\"/docs/styling\"><code>@style</code></a> block, or pull from your <a href=\"/docs/theming\">theme tokens</a>:</p>\n<pre class=\"lang-razor\"><code>@style {\n  .map .edge   { stroke: var(--mv-border); stroke-width: 2; }\n  .map .node   { cursor: pointer; }\n  .map circle  { fill: var(--mv-surface); stroke: var(--mv-primary); }\n  .map .selected circle { fill: var(--mv-primary); }\n}</code></pre>\n<h2 id=\"live-network-maps\">Live network maps</h2>\n<p>Combine SVG with <a href=\"/docs/protocol\">adaptive polling</a>: when a node's status changes in your canister, the next sync produces a different <code>batchId</code>, the client fetches the new batch, and the map re-renders. Recolor by status with <code>@switch</code>:</p>\n<pre class=\"lang-razor\"><code>@for node in nodes {\n  @switch node.status {\n    case (#healthy) { &lt;circle class=\"ok\"   cx=\"@node.x\" cy=\"@node.y\" r=\"18\" /&gt; }\n    case (#degraded){ &lt;circle class=\"warn\" cx=\"@node.x\" cy=\"@node.y\" r=\"18\" /&gt; }\n    case (#down)    { &lt;circle class=\"down\" cx=\"@node.x\" cy=\"@node.y\" r=\"18\" /&gt; }\n  }\n}</code></pre>\n<p>That is the whole model: state lives in Motoko, the shapes are a projection of that state, and clicks are <a href=\"/docs/events\">updates</a> that change it.</p>\n<h2 id=\"see-also\">See also</h2>\n<ul><li><a href=\"/docs/control-flow\">Control Flow</a> — <code>@for</code>, <code>@if</code>, and <code>@switch</code></li><li><a href=\"/docs/events\">Events</a> — how <code>@click</code> arguments reach your handlers</li><li><a href=\"/docs/styling\">Styling</a> and <a href=\"/docs/theming\">Theming</a> — <code>@style</code> blocks and tokens</li><li><a href=\"/docs/protocol\">Protocol</a> — adaptive polling that keeps live maps current</li></ul>"; prevSlug = "styling-and-themes"; prevTitle = "Styling & Themes"; nextSlug = "charts"; nextTitle = "Charts" } };
        case ("charts") { ?{ slug = "charts"; title = "Charts"; section = "Styling"; html = "<h1 id=\"charts\">Charts</h1>\n<p>MotoView ships a <strong>complete set of chart components</strong> — 66 of them — rendered as <strong>server-side SVG</strong>. There is <strong>no charting JavaScript</strong>: the canister computes the geometry in Motoko and returns <code>&lt;svg&gt;</code>, so charts are SEO-readable, work without scripts, and theme automatically. Tooltips are native SVG <code>&lt;title&gt;</code> (hover any shape); a CSS <code>:hover</code> highlights it. Everything uses the Fluent design tokens, so charts follow the active theme (try the picker on the live <strong><a href=\"/charts\">/charts</a></strong> gallery).</p>\n<h2 id=\"data-is-just-strings\">Data is just strings</h2>\n<p>You don't pass typed arrays — you pass <strong>CSV-style strings</strong>, which means a chart works with a literal <em>or</em> a dynamic <code>@(expr)</code>:</p>\n<pre class=\"lang-razor\"><code>&lt;!-- literal --&gt;\n&lt;ColumnChart values=\"120,98,145,160,132\" labels=\"Jan,Feb,Mar,Apr,May\" title=\"Signups\" /&gt;\n\n&lt;!-- dynamic: any Text expression from @code --&gt;\n&lt;LineChart series=@(model.chartData) labels=@(model.months) title=\"Revenue\" /&gt;</code></pre>\n<p>The conventions:</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Prop</th><th>Format</th><th>Used by</th></tr></thead><tbody><tr><td><code>values</code></td><td><code>\"42,30,55,20\"</code></td><td>single-series charts</td></tr><tr><td><code>labels</code></td><td><code>\"Q1,Q2,Q3,Q4\"</code></td><td>category axis</td></tr><tr><td><code>series</code></td><td><code>\"Sales:10,20,30;Costs:5,8,12\"</code></td><td>multi-series (<code>name:v,v;…</code>)</td></tr><tr><td><code>points</code></td><td><code>\"1,2;3,5;4,4\"</code></td><td>scatter (<code>x,y;…</code>)</td></tr><tr><td><code>points</code> (bubble)</td><td><code>\"x,y,size;…\"</code></td><td>bubble</td></tr><tr><td><code>ohlc</code></td><td><code>\"Mon:100,110,95,108;…\"</code></td><td>candlestick / OHLC</td></tr><tr><td><code>links</code></td><td><code>\"Source&gt;Target:value;…\"</code></td><td>sankey</td></tr><tr><td><code>edges</code></td><td><code>\"A&gt;B;A&gt;C;B&gt;D\"</code></td><td>arc / dendrogram</td></tr></tbody></table>\n<blockquote>Data containing <code>&gt;</code> (sankey/arc/dendrogram) can't sit in a literal attribute — pass it via <code>@(expr)</code> from <code>@code</code>.</blockquote>\n<h2 id=\"options\">Options</h2>\n<p>Any chart accepts: <code>title</code>, <code>width</code>, <code>height</code> (viewBox px), <code>unit</code> (suffix on values, e.g. <code>\"%\"</code>/<code>\"$\"</code>), and the booleans <code>hideAxes</code> / <code>hideGrid</code> / <code>hideLegend</code>. Numeric charts also take <code>yMin</code> / <code>yMax</code>.</p>\n<pre class=\"lang-razor\"><code>&lt;BarChart values=\"42,30,55\" labels=\"Q1,Q2,Q3\" title=\"Revenue\" unit=\"$\" width=480 height=300 hideGrid /&gt;</code></pre>\n<h2 id=\"the-catalog\">The catalog</h2>\n<p><strong>Comparison</strong> — <code>BarChart</code>, <code>ColumnChart</code>, <code>GroupedColumnChart</code>, <code>StackedColumnChart</code>, <code>LollipopChart</code>, <code>BulletChart</code>, <code>DotPlot</code>, <code>DumbbellChart</code>, <code>RangePlot</code>, <code>SlopeChart</code>, <code>DivergingBarChart</code>, <code>WaterfallChart</code>, <code>PictogramChart</code>, <code>ParallelCoordinates</code>, <code>MatrixChart</code>, <code>SmallMultiples</code>, <code>TableChart</code>.</p>\n<p><strong>Trend &amp; temporal</strong> — <code>LineChart</code>, <code>SplineChart</code>, <code>StepLineChart</code>, <code>AreaChart</code>, <code>StackedAreaChart</code>, <code>Sparkline</code>, <code>CandlestickChart</code>, <code>OHLCChart</code>, <code>GanttChart</code>, <code>StreamGraph</code>, <code>BumpChart</code>, <code>BumpAreaChart</code>, <code>BarcodeChart</code>, <code>HorizonChart</code>.</p>\n<p><strong>Part-to-whole &amp; hierarchical</strong> — <code>PieChart</code>, <code>DonutChart</code>, <code>SemiDonutChart</code>, <code>GaugeChart</code>, <code>RadialBarChart</code>, <code>NightingaleChart</code>, <code>WaffleChart</code>, <code>TreemapChart</code>, <code>CircularTreemap</code>, <code>FunnelChart</code>, <code>PyramidChart</code>, <code>MarimekkoChart</code>, <code>PopulationPyramid</code>, <code>SunburstChart</code>.</p>\n<p><strong>Distribution</strong> — <code>Histogram</code>, <code>RadialHistogram</code>, <code>BoxPlot</code>, <code>ViolinPlot</code>, <code>StripPlot</code>, <code>BeeswarmChart</code>, <code>DensityPlot</code>, <code>RidgelinePlot</code>, <code>WordCloud</code>.</p>\n<p><strong>Correlation</strong> — <code>ScatterChart</code>, <code>BubbleChart</code>, <code>ConnectedScatterChart</code>, <code>Heatmap</code>, <code>HexbinChart</code>, <code>QuadrantChart</code>, <code>RadarChart</code>.</p>\n<p><strong>Flow &amp; relationship</strong> — <code>SankeyDiagram</code>, <code>ChordDiagram</code>, <code>ArcDiagram</code>, <code>Dendrogram</code>, <code>VennDiagram</code>.</p>\n<h2 id=\"what-isn-t-included-and-why\">What isn't included (and why)</h2>\n<p>A few chart types need external data or heavy layout algorithms that don't fit a pure server-side SVG renderer, so they're deliberately left out rather than faked:</p>\n<ul><li><strong>Geographic / choropleth / tile maps</strong> — need TopoJSON geometry + map projections.</li><li><strong>Force-directed network diagrams</strong> — need an iterative physics layout.</li><li><strong>Contour plots</strong> — need grid interpolation over a continuous field.</li><li><strong>Flowcharts</strong> — need manual node placement / routing.</li></ul>\n<h2 id=\"how-it-works\">How it works</h2>\n<p>Each component compiles to one call into <code>mo:motoview/Charts</code> (the <code>runtime/src/Charts.mo</code> module) — e.g. <code>&lt;BarChart …&gt;</code> becomes <code>b.raw(Charts.bar(values, labels, opts))</code>. The module is pure functions returning SVG <code>Text</code>, built on shared helpers (scales, nice ticks, axes, an arc/polar toolkit, a categorical palette, a legend builder). To add your own chart, add a function there and a one-line <code>gen_builtin</code> arm — see <a href=\"/docs/components\">Components</a> and <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a>.</p>"; prevSlug = "svg"; prevTitle = "SVG Interfaces"; nextSlug = "security"; nextTitle = "Security" } };
        case ("security") { ?{ slug = "security"; title = "Security"; section = "Security"; html = "<h1 id=\"security\">Security</h1>\n<p>MotoView is <strong>secure by construction</strong>. Rendering, validation, and authorization all live inside the canister, there is <strong>no application JavaScript</strong> and <strong>no separate API layer</strong>, and the responses the browser receives are <strong>certified by the chain</strong>. There is simply no second place for security rules to drift out of sync: the browser is a renderer, the canister is the source of truth.</p>\n<p>This page walks the whole posture — the attack surface, certified responses, secure forms, authentication, authorization and roles, and zero-trust encryption — and ends with a threat-to-defense table.</p>\n<h2 id=\"the-attack-surface-what-isn-t-there\">The attack surface (what <em>isn't</em> there)</h2>\n<p>A lot of a web app's security problems come from things MotoView doesn't have:</p>\n<ul><li><strong>No application JavaScript.</strong> You don't ship app JS, so there's no app-level XSS sink, no <code>eval</code>, no dynamic code, no place to leak a key. The only JavaScript on the page is MotoView's small, audited framework glue (DOM sync, fetch, focus/scroll preservation) — it carries no business logic and is identical for every app.</li><li><strong>No npm / build-time supply chain.</strong> No <code>node_modules</code>, no transitive dependency tree, no bundler plugins running arbitrary code at build time. The client \"brain\" is Rust → WASM, compiled reproducibly.</li><li><strong>No separate API to guard.</strong> There's no REST/GraphQL layer that can disagree with the UI about who may do what. Handlers are typed Motoko functions in the same actor that renders the page.</li><li><strong>Output is escaped by default.</strong> <code>@value</code> interpolation HTML-escapes; raw HTML requires the explicit, opt-in <code>@raw(...)</code>. So untrusted data can't become markup by accident.</li></ul>\n<h2 id=\"certified-responses-integrity\">Certified responses (integrity)</h2>\n<p>What the browser renders is <strong>provably what the canister produced</strong>:</p>\n<ul><li>Static framework assets and pages you mark <code>@cacheable</code> are served as <strong>certified queries</strong> (HTTP response certification v2). The IC boundary verifies the canister's signature over the response bytes against the subnet's certified state — a boundary node or a man-in-the-middle cannot tamper with them undetected.</li><li>Every dynamic page and every mutation goes through <strong><code>http_request_update</code></strong> — an update call validated by subnet <strong>consensus</strong>, so it is always fresh and agreed-upon, never a stale or forged read.</li></ul>\n<p>In short: certified for the static, consensus for the dynamic. There is no \"trust the CDN\" step.</p>\n<h2 id=\"secure-forms\">Secure forms</h2>\n<p>Mark any form that mutates state with <code>secure</code>. Inputs <code>bind</code> to your model; the handler runs server-side.</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputText  name=\"name\"  label=\"Name\"  bind=\"@model.name\"  required /&gt;\n  &lt;InputEmail name=\"email\" label=\"Email\" bind=\"@model.email\" required /&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;Button appearance=\"primary\"&gt;Send&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<p>When the form renders, MotoView mints a signed token and embeds it. It is <strong>not</strong> a bare CSRF nonce and <strong>not</strong> a session cookie — it is a MAC over the exact request that is allowed to follow.</p>\n<h3 id=\"the-token\">The token</h3>\n<p>An <strong>HMAC-SHA256</strong> value the canister computes over a tuple that binds the submission to a specific context:</p>\n<ul><li><strong>path</strong> — the page the form was rendered on</li><li><strong>handler</strong> — the one event handler permitted to run (<code>send</code>)</li><li><strong>principal</strong> — the caller's IC principal at render time</li><li><strong>nonce</strong> — a single-use random value</li><li><strong>expiry</strong> — an absolute deadline after which the token is dead</li><li><strong>schema hash</strong> — a hash of the form's field schema (names + constraints)</li></ul>\n<p>SHA-256 and HMAC are implemented in Motoko and verified against standard test vectors — there is no JavaScript crypto in the trust path. On submit, the event is delivered as a <code>POST</code> to <code>/_motoview/event</code> (served by <code>http_request_update</code>); the canister re-derives the MAC from the <em>live</em> request and rejects any mismatch before the handler runs.</p>\n<pre class=\"lang-motoko\"><code>let expected = HMAC.sha256(secretKey, encode(path, handler, caller, nonce, expiry, schemaHash));\nif (token != expected)      return reject(\"bad token\");\nif (now() &gt; expiry)         return reject(\"expired\");\nif (nonceStore.seen(nonce)) return reject(\"replay\");</code></pre>\n<p>This single token gives you four protections at once:</p>\n<ul><li><strong>CSRF / cross-origin submits</strong> — it can't be forged without the canister's secret key (minted from <code>raw_rand</code> at first boot; never leaves the canister).</li><li><strong>Replay</strong> — single-use nonce + expiry: a captured submission can't be resent.</li><li><strong>Principal substitution</strong> — bound to the caller principal at render time; a token minted for one caller won't verify for another. Authorization is <em>cryptographic</em>, not advisory.</li><li><strong>Field tampering</strong> — the schema hash binds the allowed field set; added or renamed fields diverge the hash and are rejected.</li></ul>\n<p>And there's no client-side-only validation to bypass — the <code>validate { … }</code> rules run in the handler. See <a href=\"/docs/forms\">Forms &amp; Validation</a>.</p>\n<h2 id=\"authentication-internet-identity\">Authentication — Internet Identity</h2>\n<p>Sign-in uses <strong>Internet Identity</strong> (passkeys / WebAuthn) — <strong>no passwords</strong>, hand-rolled with no npm or <code>agent-js</code>:</p>\n<ul><li>The browser makes <strong>one</strong> authenticated update call the IC has cryptographically verified; the canister records the real signed-in principal.</li><li>The canister then mints a short, HMAC-signed <strong>httpOnly session cookie</strong>. Because it's <code>httpOnly</code>, page JavaScript cannot read or steal it; <code>ctx.caller</code> resolves from it on every later request.</li><li>The login nonce is <strong>server-generated and browser-bound</strong> (an httpOnly <code>mv_login</code> cookie), which prevents login-CSRF and nonce theft, and the II <code>postMessage</code> flow is pinned to the exact II origin.</li><li>Sessions are <strong>revocable per principal</strong> (an epoch bump invalidates outstanding cookies) and survive canister upgrades.</li></ul>\n<p>Drop in a <code>&lt;button data-mv-signin&gt;</code> and it's wired automatically.</p>\n<h2 id=\"authorization-roles-scopes\">Authorization — roles &amp; scopes</h2>\n<p>Gate pages and handlers with <code>@authorize</code>, optionally scoped to a role:</p>\n<pre class=\"lang-razor\"><code>@page \"/admin\"\n@authorize role=\"Admin\"</code></pre>\n<p><code>@authorize</code> (no role) requires an authenticated caller; <code>role=\"Admin\"</code> additionally requires the caller to <strong>hold that role</strong> in the runtime's persisted role store — otherwise the page redirects and its content never renders (server-side; not a hidden div).</p>\n<p>Manage roles from any handler via the request context — <code>hasRole(who, role)</code>, <code>callerRoles()</code>, <code>grantRole</code>, <code>revokeRole</code>, and <code>claimRole(role)</code> (first-come bootstrap for the first admin). The store is a <code>stable var</code>, so grants survive <code>dfx deploy --mode upgrade</code>.</p>\n<pre class=\"lang-motoko\"><code>@code {\n  func claimAdmin(ctx : Context) : async () { ignore ctx.claimRole(\"Admin\"); };\n  func makeEditor(ctx : Context) : async () {\n    if (ctx.hasRole(ctx.caller, \"Admin\")) { ctx.grantRole(somePrincipal, \"Editor\"); };\n  };\n}</code></pre>\n<p>For a fuller picture, apps can build a <strong>scoped</strong> role model on top of this — global tiers (e.g. SuperAdmin / Admin / Moderator) plus per-resource roles (e.g. per-server Owner/Admin/Moderator/Member), with the page combining the two so a global staffer can act in any scope. The flagship Bzzz app ships exactly this, with an Admin Console; the framework primitive (<code>@authorize</code> + the role store) is what backs it.</p>\n<h2 id=\"zero-trust-encryption-vetkeys-the-browser-decrypts-locally\">Zero-trust encryption — vetKeys, the browser decrypts locally</h2>\n<p>For data the canister itself must never read, MotoView ships <strong>end-to-end encryption with vetKeys</strong> — the <strong>browser decrypts locally</strong> and the canister holds <strong>only ciphertext</strong>.</p>\n<ul><li>Every actor exposes the endpoints <code>GET /_motoview/vetkd/public-key</code> (the BLS12-381 master key) and <code>POST /_motoview/vetkd/derive</code> (a vetKey bound to the <strong>session caller's</strong> principal — only they can unwrap it). The threshold key is derived across the subnet; <strong>no single node holds it</strong>, and the canister never sees the plaintext key.</li><li>All the crypto (BLS unwrap + identity-based encrypt/decrypt) runs in a <strong>Rust → WASM</strong> module in the browser (<code>ic-vetkeys</code>). Two declarative attributes make it JS-free for apps: <code>data-mv-encrypt</code> (a form field is IBE-encrypted <em>before</em> it's sent) and <code>data-mv-decrypt=\"&lt;ciphertext&gt;\"</code> (decrypted in place on render). The canister stores ciphertext and audits each access; it can prove <em>who did what, when</em> without ever reading the data.</li></ul>\n<p>So a compromised canister or a curious node operator sees only ciphertext. This is verified end to end on a deployed canister, and there's a worked <code>examples/vault</code> (a per-user encrypted notes app). Full walkthrough: <a href=\"/docs/zero-trust\">Building a Zero-Trust App</a>. (Local dfx uses <code>dfx_test_key</code>; mainnet uses <code>key_1</code>. The crypto is opt-in — it's only fetched when an app uses it — so non-crypto apps keep the lean default brain.)</p>\n<h2 id=\"denial-of-service\">Denial-of-service</h2>\n<p>The update path enforces a request-size cap, and the certified-query path serves static assets without consuming a consensus round. Heavy reads should be <code>@cacheable</code> so they're served as queries rather than updates.</p>\n<h2 id=\"threat-defense\">Threat → defense</h2>\n<table class=\"mv-doc-table\"><thead><tr><th>Threat</th><th>Defense</th></tr></thead><tbody><tr><td>Cross-site request forgery</td><td>secure-form HMAC token (can't forge without the canister secret) + browser-bound login nonce</td></tr><tr><td>Replay / double-submit</td><td>single-use nonce + token expiry</td></tr><tr><td>Acting as another user</td><td>token bound to the caller principal; httpOnly session resolves <code>ctx.caller</code></td></tr><tr><td>Field tampering / over-posting</td><td>schema-hash binding rejects unexpected fields</td></tr><tr><td>Client validation bypass</td><td>validation runs server-side in the handler</td></tr><tr><td>Response tampering / stale reads</td><td>certified queries (static/<code>@cacheable</code>) + consensus updates (dynamic)</td></tr><tr><td>XSS from app code</td><td>no application JavaScript; output escaped by default (<code>@raw</code> is explicit)</td></tr><tr><td>Supply-chain / dependency attacks</td><td>no npm; reproducible Rust→WASM client</td></tr><tr><td>Session theft via JS</td><td>httpOnly session cookie; per-principal revocation</td></tr><tr><td>Privilege escalation</td><td><code>@authorize</code> + persisted role store enforced server-side</td></tr><tr><td>Canister/operator reading private data</td><td>vetKeys E2E encryption — only ciphertext on-chain, browser decrypts locally</td></tr></tbody></table>\n<p>Build on what is verified end to end: secure forms, certified responses, Internet Identity, role-based authorization, and zero-trust vetKeys encryption.</p>"; prevSlug = "charts"; prevTitle = "Charts"; nextSlug = "zero-trust"; nextTitle = "Building a Zero-Trust App" } };
        case ("zero-trust") { ?{ slug = "zero-trust"; title = "Building a Zero-Trust App"; section = "Security"; html = "<h1 id=\"building-a-zero-trust-app-on-motoview\">Building a Zero-Trust App on MotoView</h1>\n<p>Zero trust is a posture, not a product: <em>never trust, always verify</em>. No request is trusted because of where it comes from; every request is authenticated, authorized, and minimized, and sensitive data is encrypted so that even the systems holding it cannot read it.</p>\n<p>MotoView is unusually well-positioned for this because of its shape. There is no application JavaScript and no separate API tier, so authorization and validation live in exactly one place — the canister — and cannot drift out of sync between a client and a server. The browser is a renderer (and, for vetKeys, a local crypto engine); the canister is the source of truth.</p>\n<p>This page maps the seven pillars of a zero-trust architecture to concrete MotoView primitives. It is deliberately honest about the line between <strong>what ships today</strong> and <strong>what the encrypted-storage change adds</strong>, because designing around a feature that does not exist yet is the opposite of zero trust.</p>\n<hr>\n<h2 id=\"the-seven-pillars-mapped\">The seven pillars, mapped</h2>\n<table class=\"mv-doc-table\"><thead><tr><th>Pillar</th><th>MotoView primitive</th><th>Status</th></tr></thead><tbody><tr><td>1. Identity</td><td>Internet Identity login → session cookie → <code>ctx.caller</code></td><td><strong>Ships today</strong></td></tr><tr><td>2. Authorization</td><td><code>@authorize</code> + the persisted per-principal role store</td><td><strong>Ships today</strong></td></tr><tr><td>3. Integrity of served content</td><td>Certified query rendering (response-certification v2)</td><td><strong>Ships today</strong></td></tr><tr><td>4. Request authenticity</td><td><code>secure</code> forms: HMAC token binding path + handler + principal + nonce + expiry + schema</td><td><strong>Ships today</strong></td></tr><tr><td>5. Server-side enforcement</td><td><code>validate model { … }</code> + handlers run in the canister; no client-only rules</td><td><strong>Ships today</strong></td></tr><tr><td>6. Data confidentiality (key custody)</td><td>vetKeys endpoints: <code>mvVetkdPublicKey</code> / <code>mvVetkdDeriveKey</code>, threshold-derived, per-principal</td><td><strong>Ships today</strong> (canister foundation, verified)</td></tr><tr><td>7. Encrypted-only storage + audit</td><td><code>EncStore</code> (ciphertext-only state) + server-side <code>Audit</code> log + <strong>in-browser</strong> vetKeys decrypt</td><td><strong>Added by this change</strong> (opt-in)</td></tr></tbody></table>\n<p>The first six are verified and in the runtime/compiler now. The seventh is what turns \"we <em>could</em> encrypt\" into \"the canister <em>only ever holds ciphertext</em>, and every sensitive transition is logged\" — and it is opt-in, because the in-browser crypto is not free (see <a href=\"#the-opt-in-cost\">the size caveat</a>).</p>\n<hr>\n<h2 id=\"pillar-1-identity-who-is-calling\">Pillar 1 — Identity: who is calling</h2>\n<p>MotoView ships a hand-rolled Internet Identity login with <strong>no npm and no agent-js</strong>. A small browser IC agent (served at <code>/mv-auth.js</code>) makes one authenticated <code>mvEstablish(nonce)</code> update call; the IC itself verifies the caller's signature, the runtime records the principal under a server-issued nonce, and a following <code>GET /mv-session</code> mints a short-lived <strong>httpOnly</strong> session cookie bound to that principal.</p>\n<p>From then on, every handler resolves the real signed-in principal from the cookie via <code>effectiveCaller</code> (<code>runtime/src/App.mo</code>), exposed to your code as <code>ctx.caller</code>. The session token is never readable by JavaScript (XSS-resistant), the nonce is server-chosen and never travels in a URL (login-CSRF-resistant), and sessions are revocable per principal (a logout bumps a stored epoch, invalidating every outstanding token for that user).</p>\n<p>This is the anchor for everything else: roles attach to a principal, vetKeys are derived for a principal, and the secure-form MAC is bound to a principal.</p>\n<pre class=\"lang-razor\"><code>@page \"/vault\"\n@authorize\n&lt;!-- ctx.caller is now the authenticated II principal --&gt;</code></pre>\n<h2 id=\"pillar-2-authorization-least-privilege\">Pillar 2 — Authorization: least privilege</h2>\n<p><code>@authorize</code> (bare) requires an authenticated caller. <code>@authorize role=\"Admin\"</code> additionally requires the caller to <strong>hold that role</strong> in the runtime's role store; otherwise the page redirects and its content never renders — the data is never sent, not merely hidden.</p>\n<p>Roles are per-principal, persisted across upgrades (<code>stable var</code>, see <code>runtime/src/Roles.mo</code>), and managed from any handler via the context: <code>hasRole</code>, <code>callerRoles</code>, <code>grantRole</code>, <code>revokeRole</code>, and <code>claimRole</code> (a first-come bootstrap for seating the first admin safely). Because authorization runs server-side before render, there is no client gate to bypass.</p>\n<h2 id=\"pillar-3-integrity-of-served-content\">Pillar 3 — Integrity of served content</h2>\n<p>Static framework assets and pages you mark <code>@cacheable</code> are served as <strong>certified queries</strong> using HTTP response-certification v2 (<code>runtime/src/CertV2.mo</code>). The boundary node validates the certificate against the subnet's root key, so a single malicious replica or boundary node cannot tamper with the bytes a user receives. Dynamic pages keep the consensus-validated <code>http_request_update</code> path, which is fresh by construction. Either way, the content a user sees is attributable to the canister, not to whatever served it.</p>\n<h2 id=\"pillar-4-request-authenticity-every-mutation-is-a-bound-request\">Pillar 4 — Request authenticity: every mutation is a bound request</h2>\n<p>Mark any state-mutating form <code>secure</code>. On render, the canister mints an <strong>HMAC-SHA256</strong> token over a tuple that pins the submission to one exact request: path, handler, caller principal, single-use nonce, expiry, and a hash of the field schema. On submit, the canister re-derives the MAC from the <em>live</em> request and rejects any mismatch before the handler runs.</p>\n<p>This defeats CSRF (no secret, no forgery), replay (single-use nonce + expiry), principal substitution (the MAC is bound to <code>ctx.caller</code>), and field tampering (the schema hash binds the allowed field set). SHA-256/HMAC are implemented in Motoko and pass standard test vectors — there is no JavaScript crypto in the trust path. See <a href=\"/docs/security\">Security</a> for the full token shape.</p>\n<h2 id=\"pillar-5-server-side-enforcement-no-trusted-client\">Pillar 5 — Server-side enforcement: no trusted client</h2>\n<p>There is no client-side-only validation to bypass, because there is no application JavaScript to hold it. The <code>validate model { … }</code> block and every event handler execute in the canister. A client can lie about anything; the canister re-checks all of it. This is the structural reason MotoView's \"verify\" is cheap: there is exactly one enforcement point.</p>\n<h2 id=\"pillar-6-data-confidentiality-keys-nobody-can-steal\">Pillar 6 — Data confidentiality: keys nobody can steal</h2>\n<p>Every generated MotoView actor exposes vetKeys endpoints for identity-based encryption (IBE), so each user can have state that <strong>only they</strong> can decrypt (<code>compiler/src/project.rs</code> generates these into every app):</p>\n<ul><li><code>mvVetkdContext()</code> → the app's derivation context (<code>\"motoview\"</code>).</li><li><code>mvVetkdPublicKey()</code> → the 96-byte BLS12-381 G2 master public key for that context. Free; reveals no secret.</li><li><code>mvVetkdDeriveKey(transportKey)</code> → a 192-byte vetKey for the <strong>caller's principal</strong> (the derivation <code>input</code> is <code>Principal.toBlob(msg.caller)</code>), encrypted to a client-generated transport key.</li></ul>\n<p>The threshold key is derived across the subnet — <strong>no single node ever holds it</strong>. The canister mediates the derivation (it attaches the cycles and calls the management canister <code>aaaaa-aa</code>) but never sees the plaintext key: it only forwards a 192-byte blob that is already encrypted to the client's transport key. The BLS unwrap and the IBE encrypt/decrypt run in the <strong>client</strong>, via <a href=\"https://crates.io/crates/ic-vetkeys\"><code>ic-vetkeys</code></a>.</p>\n<p>This foundation is verified end to end against a real local replica with the real client crypto — see <a href=\"../tools/vetkeys-roundtrip\"><code>tools/vetkeys-roundtrip</code></a>, which prints <code>ROUND_TRIP_OK</code> after a full derive → unwrap (BLS-signature-checked <code>decrypt_and_verify</code>) → IBE encrypt → IBE decrypt that recovers the exact plaintext. It also confirms <code>ic-vetkeys</code> compiles to <code>wasm32-unknown-unknown</code>, which is the prerequisite for putting that crypto in the browser brain.</p>\n<hr>\n<h2 id=\"what-this-change-adds-pillar-7\">What this change adds (Pillar 7)</h2>\n<p>The six pillars above mean a user is authenticated, authorized, served verified content, and <em>can</em> obtain a per-user key the infrastructure cannot read. This change closes the loop so that the canister holds <strong>only ciphertext</strong> and every sensitive transition is <strong>audited</strong> — and it wires the vetKeys decrypt into the browser brain so an app does this with no external tool.</p>\n<p>Three pieces, all opt-in:</p>\n<ol><li><strong><code>EncStore</code> — ciphertext-only storage.</strong> A store whose values are IBE ciphertexts keyed by principal. It deliberately offers no \"read plaintext\" method: the canister cannot decrypt, by design. It stores what the browser hands it (already encrypted) and returns it on request. The threat surface of a compromised canister or a curious node operator collapses to \"they see ciphertext.\"</li></ol>\n<ol><li><strong><code>Audit</code> — a server-side append-only log.</strong> Every sensitive transition (store, fetch, key derive, role change) appends an immutable record — principal, action, timestamp, and a content hash (never the plaintext). This is the \"always verify\" half of zero trust: even though the canister cannot read the data, it can prove <em>who did what, when</em>. The log is <code>stable</code>, so it survives upgrades, and there is no method to mutate or delete past entries.</li></ol>\n<ol><li><strong>In-browser vetKeys decryption.</strong> The same <code>ic-vetkeys</code> crypto that <code>tools/vetkeys-roundtrip</code> proves on the host, shipped inside the MotoView WASM brain. A page generates a transport key, calls <code>mvVetkdDeriveKey</code>, unwraps the vetKey, and does the IBE encrypt/decrypt <strong>locally</strong> — the plaintext exists only in the browser tab. The JS glue stays dumb hands: it loads a wasm, calls an export with bytes, gets randomness from <code>window.crypto</code>, and <code>fetch</code>es. All crypto logic is in Rust → WASM. (This is the framework's hard rule: no application JavaScript; crypto is brain, not hands.)</li></ol>\n<p>See the worked example under <a href=\"../examples/vault\"><code>examples/vault</code></a> — a per-user secret store where the browser encrypts, the canister stores only ciphertext via <code>EncStore</code>, every access is recorded in <code>Audit</code>, and the browser decrypts on read.</p>\n<h3 id=\"the-client-api-what-you-actually-write\">The client API (what you actually write)</h3>\n<p>The whole client side is two declarative attributes — no JavaScript:</p>\n<pre class=\"lang-mview\"><code>@* encrypt a field in the browser before the secure form is sent *@\n&lt;form @submit=\"addNote\" secure&gt;\n    &lt;textarea name=\"note\" data-mv-encrypt required&gt;&lt;/textarea&gt;\n    &lt;Button appearance=\"primary\" type=\"submit\"&gt;Encrypt &amp;amp; save&lt;/Button&gt;\n&lt;/form&gt;\n\n@* render stored ciphertext; the browser decrypts it in place on load *@\n@for n in notes {\n    &lt;span data-mv-decrypt=\"@n.ciphertext\"&gt;🔒 decrypting…&lt;/span&gt;\n}</code></pre>\n<p>On submit, the glue IBE-encrypts every <code>[data-mv-encrypt]</code> field (so the canister only ever receives ciphertext); on render, it decrypts every <code>[data-mv-decrypt]</code> element in place. Both go through <code>window.mvCrypto</code>, which loads <code>/motoview-crypto.wasm</code> on first use. The canister auto-exposes two endpoints the brain calls — <code>GET /_motoview/vetkd/public-key</code> (the master key) and <code>POST /_motoview/vetkd/derive</code> (the vetKey, bound to the session caller via the II session cookie). Your Motoko handler reads <code>ctx.form</code> (already ciphertext) and stores it; it never sees plaintext.</p>\n<blockquote><strong>Verified end to end</strong> in a real browser on a deployed canister: a note typed into the vault is encrypted before submit (the served HTML contains only the <code>IbeCiphertext</code>, never the plaintext), stored on-chain, then decrypted back to the exact text locally on render — <code>ok: true</code>.</blockquote>\n<hr>\n<h2 id=\"end-to-end-data-flow\">End-to-end data flow</h2>\n<p>A single sensitive value, from login to plaintext-in-tab and back:</p>\n<ol><li><strong>Login.</strong> The user signs in with Internet Identity; the runtime mints an httpOnly session cookie. <code>ctx.caller</code> now resolves to their principal (Pillar 1).</li><li><strong>Derive.</strong> The browser brain generates a 48-byte transport key (<code>window.crypto</code> RNG → <code>ic-vetkeys</code>), calls <code>mvVetkdPublicKey()</code> for the 96-byte master key, and calls <code>mvVetkdDeriveKey(transportKey)</code>. The canister attaches cycles, calls the management canister, and returns the 192-byte encrypted vetKey — still encrypted to the transport key.</li><li><strong>Unwrap.</strong> In the browser, <code>decrypt_and_verify</code> checks the BLS signature and yields the user's vetKey. The plaintext key never leaves the tab and never touches the canister.</li><li><strong>Encrypt + store.</strong> The browser IBE-encrypts the secret to the user's principal and submits the <strong>ciphertext</strong> through a <code>secure</code> form (Pillar 4). The handler writes it to <code>EncStore</code> and appends an <code>Audit</code> record. The canister stores ciphertext only.</li><li><strong>Fetch.</strong> Later, <code>ctx.caller</code> (Pillar 1) is checked, the ciphertext is read from <code>EncStore</code>, an <code>Audit</code> \"fetch\" record is appended, and the ciphertext is rendered into the page.</li><li><strong>Decrypt.</strong> The browser brain re-derives/uses the vetKey and IBE-decrypts locally, recovering the plaintext for display. The canister never saw it.</li></ol>\n<p>The only point at which the plaintext exists is inside the authenticated user's browser. Everything in transit and at rest is ciphertext; everything is attributable; nothing is trusted on the client's word.</p>\n<hr>\n<h2 id=\"threat-model-what-the-canister-and-node-operator-can-and-cannot-see\">Threat model: what the canister and node operator can and cannot see</h2>\n<p><strong>Cannot see:</strong></p>\n<ul><li>The <strong>plaintext</strong> of any value stored through <code>EncStore</code>. The canister holds IBE ciphertext only and has no decrypt path.</li><li>The <strong>vetKey</strong> itself. It is threshold-derived (no single node holds it) and returned encrypted to a transport key the browser generated; the canister forwards an opaque blob.</li><li>Anything by <strong>impersonating a user</strong>: derivation <code>input</code> is the caller's principal, so a node cannot ask for someone else's key, and the secure-form MAC is bound to the caller.</li></ul>\n<p><strong>Can see (and you must account for):</strong></p>\n<ul><li><strong>Metadata.</strong> Which principal stored or fetched which key, when, and how large the ciphertext is. The <code>Audit</code> log makes this explicit on purpose. If access patterns or sizes are themselves sensitive, pad and/or partition.</li><li><strong>Ciphertext at rest</strong>, available to anyone who can read canister state (including a malicious replica). Confidentiality rests entirely on the IBE encryption, not on access control.</li><li><strong>A coerced subnet.</strong> vetKeys' guarantee is <em>threshold</em> — it assumes an honest majority of the subnet's nodes. A subnet-level compromise that breaks that threshold is outside what any vetKeys-based scheme defends against.</li></ul>\n<p><strong>Not solved here:</strong> transport security (handled by the IC boundary and <code>http_request_update</code>), denial of service, and traffic analysis beyond the metadata note above. Zero trust reduces the blast radius of a compromise; it does not make the system invulnerable.</p>\n<hr>\n<h2 id=\"the-dfx-test-key-vs-key-1-cycle-caveat\">The dfx_test_key vs key_1 + cycle caveat</h2>\n<p>vetKeys derivation is keyed by name and costs cycles:</p>\n<ul><li><strong>Local dfx</strong> ships the test key <code>dfx_test_key</code> (the generated endpoints use it). The local replica charges roughly <strong>26.2 billion cycles per derive</strong>; <code>runtime/src/VetKeys.mo</code> attaches a margin (30 billion) on every <code>mvVetkdDeriveKey</code> call.</li><li><strong>Mainnet</strong> uses the production key <code>key_1</code>. Switch the key name in the generated <code>mvVetkdPublicKey</code> / <code>mvVetkdDeriveKey</code> (the codegen in <code>compiler/src/project.rs</code>, backed by <code>runtime/src/VetKeys.mo</code>) before you deploy.</li></ul>\n<p>Two practical consequences: budget cycles for derivation (it is not free, and it is an <strong>update</strong> call, not a query), and cache the unwrapped vetKey in the browser for the session rather than deriving per operation. <code>mvVetkdPublicKey</code> is free and safe to call anywhere; only the derive costs.</p>\n<hr>\n<h2 id=\"the-opt-in-cost\">The opt-in cost</h2>\n<p>The in-browser vetKeys crypto is <strong>opt-in</strong>, and the reason is honest: it is not small. The default MotoView brain is roughly <strong>76 KB</strong> of WASM. Bundling the <code>ic-vetkeys</code> BLS12-381 + IBE machinery adds on the order of <strong>~300 KB</strong>. Apps that don't need client-side encryption keep the lean default brain and pay nothing; apps that opt in accept the larger download in exchange for plaintext that never leaves the browser.</p>\n<p>This is the right default. Zero trust is a cost-benefit decision per app, not a tax on every page.</p>\n<hr>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li><strong>Proof it works:</strong> <a href=\"../tools/vetkeys-roundtrip\"><code>tools/vetkeys-roundtrip</code></a> — the verified derive → unwrap → IBE round-trip against a real replica with the real <code>ic-vetkeys</code> crypto.</li><li><strong>Worked example:</strong> <a href=\"../examples/vault\"><code>examples/vault</code></a> — encrypt-in-browser, store-ciphertext, audit-every-access, decrypt-in-browser.</li><li><strong>Foundations:</strong> <a href=\"/docs/security\">Security</a> (secure forms, HMAC binding, roles, the vetKeys endpoints) and the <a href=\"/docs/roadmap\">Roadmap</a> (the honest status line).</li></ul>\n<p>Build on the verified pillars; opt into the encrypted-storage pillar where the data warrants it. That is zero trust on MotoView — not a slogan, but a small set of primitives that each refuse to trust the layer below them.</p>"; prevSlug = "security"; prevTitle = "Security"; nextSlug = "protocol"; nextTitle = "The Protocol" } };
        case ("protocol") { ?{ slug = "protocol"; title = "The Protocol"; section = "Architecture"; html = "<h1 id=\"the-protocol\">The Protocol</h1>\n<p>MotoView speaks a small, deliberate wire protocol called <code>motoview/1</code>. It rests on one idea that keeps the whole framework simple:</p>\n<blockquote>Rendering is a query, events are updates, and the browser synchronizes through versioned UI batches.</blockquote>\n<p>Once you internalize that sentence, the rest of the protocol falls out naturally. Let's walk through it.</p>\n<h2 id=\"render-as-query\">Render-as-query</h2>\n<p>The first time a visitor hits a route, the canister renders the page server-side and returns plain HTML. The interactive content lives inside a single mount point:</p>\n<pre class=\"lang-html\"><code>&lt;div id=\"mv-root\"&gt;\n  &lt;!-- server-rendered page content --&gt;\n&lt;/div&gt;</code></pre>\n<p>That's the SEO-friendly, no-JavaScript-required baseline. A crawler, a <code>curl</code>, or a browser with scripting disabled all see real HTML.</p>\n<p>After the page loads, the WASM client keeps the view fresh by <em>asking</em> for it. Rendering a page is a read, so synchronization is modeled as a query:</p>\n<pre class=\"lang-bash\"><code>GET /_motoview/render?path=/products/42&amp;lastBatchId=ab12cd34</code></pre>\n<p>The server re-renders the route for the current caller and compares the result to the <code>lastBatchId</code> the client already has. Nothing about the UI changes unless the underlying state changed.</p>\n<h2 id=\"event-as-update\">Event-as-update</h2>\n<p>When the user does something — <code>@click=\"save\"</code>, <code>@submit=\"send\"</code>, <code>@input=\"search\"</code> — the client posts the handler identity plus its baked-in arguments:</p>\n<pre class=\"lang-bash\"><code>POST /_motoview/event\nContent-Type: application/x-www-form-urlencoded</code></pre>\n<p>Events are state transitions, so they are served by <code>http_request_update</code>. The handler runs, mutates state, and the server immediately renders and returns the <strong>new batch</strong> in the same response. There is no waiting for the next poll tick after an interaction — the round trip carries the fresh UI back with it. See <a href=\"/docs/events\">Events</a> for how handler arguments are evaluated server-side and dispatched to typed Motoko functions.</p>\n<h2 id=\"the-batch-json\">The batch JSON</h2>\n<p>Every sync and every event resolves to a <em>batch</em>: a versioned snapshot of the UI with a status that tells the client what to do.</p>\n<p>Every batch carries a <code>\"protocol\": \"motoview/1\"</code> field. The fields below are exactly what the runtime emits (<code>runtime/src/Json.mo</code>).</p>\n<p><strong><code>changed</code></strong> — state moved; here is the new markup to swap into <code>target</code> (always <code>\"mv-root\"</code> in the MVP full-container-replace mode). Includes the document <code>head</code> and any client <code>effects</code>.</p>\n<pre class=\"lang-json\"><code>{\n  \"protocol\": \"motoview/1\",\n  \"status\": \"changed\",\n  \"batchId\": \"9f3c1a07\",\n  \"mode\": \"replace\",\n  \"target\": \"mv-root\",\n  \"html\": \"&lt;section&gt;…&lt;/section&gt;\",\n  \"head\": { \"title\": \"Dashboard\", \"description\": \"\", \"canonical\": \"\" },\n  \"effects\": []\n}</code></pre>\n<p><strong><code>unchanged</code></strong> — the rendered state hashes to the <code>lastBatchId</code> the client sent, so <code>html</code> is omitted entirely. This is the common case while polling and it keeps the wire quiet.</p>\n<pre class=\"lang-json\"><code>{ \"protocol\": \"motoview/1\", \"status\": \"unchanged\", \"batchId\": \"9f3c1a07\" }</code></pre>\n<p><strong><code>redirect</code></strong> — the handler or guard wants the client somewhere else.</p>\n<pre class=\"lang-json\"><code>{ \"protocol\": \"motoview/1\", \"status\": \"redirect\", \"location\": \"/login\" }</code></pre>\n<p><strong><code>validation-error</code></strong> — a <code>validate model { … }</code> block failed; the batch carries the re-rendered form (with <code>&lt;ValidationSummary /&gt;</code> and per-field errors populated) plus the <code>errors</code> map, so the user sees exactly what to fix.</p>\n<pre class=\"lang-json\"><code>{\n  \"protocol\": \"motoview/1\",\n  \"status\": \"validation-error\",\n  \"batchId\": \"44ed90b2\",\n  \"target\": \"mv-root\",\n  \"html\": \"&lt;form&gt;…&lt;/form&gt;\",\n  \"errors\": { \"email\": \"That email address looks invalid.\" },\n  \"effects\": []\n}</code></pre>\n<blockquote>The <code>mode</code> field on a <code>changed</code> batch is <code>\"replace\"</code> in the MVP (full-container replace). Keyed-region and patch modes are on the <a href=\"/docs/roadmap\">roadmap</a>.</blockquote>\n<h2 id=\"batchid\">batchId</h2>\n<p>The <code>batchId</code> is a hash of the rendered state. It is the version stamp that makes the protocol idempotent and cheap:</p>\n<ul><li>The client sends its current <code>batchId</code> as <code>lastBatchId</code> on every render query.</li><li>If the freshly rendered output hashes to the same value, the server answers <code>unchanged</code> and skips sending markup.</li><li>Only a genuine state change produces a new <code>batchId</code>, and only then does the DOM get swapped.</li></ul>\n<p>This is what lets MotoView poll frequently without thrashing the page.</p>\n<h2 id=\"adaptive-polling-cadence\">Adaptive polling cadence</h2>\n<p>The WASM client runs a small state machine that dials its sync frequency up and down based on what the user is doing:</p>\n<table class=\"mv-doc-table\"><thead><tr><th>State</th><th>Cadence</th><th>When</th></tr></thead><tbody><tr><td>Hot</td><td>~350ms</td><td>for ~3s right after an interaction</td></tr><tr><td>Warm</td><td>~2.5s</td><td>tab visible, no recent interaction</td></tr><tr><td>Cold</td><td>~15s</td><td>visible but idle</td></tr><tr><td>Hidden</td><td>~45s</td><td>tab backgrounded</td></tr><tr><td>Offline</td><td>backoff</td><td>exponential backoff while disconnected</td></tr></tbody></table>\n<p>Because the event response already returns the new batch, \"hot\" mode is mostly about catching <em>other</em> callers' changes immediately after you act, then relaxing as activity dies down.</p>\n<h2 id=\"why-polling-on-icp\">Why polling on ICP</h2>\n<p>ICP canisters cannot hold open server-push connections, so MotoView synchronizes by polling — but cheaply, thanks to <code>batchId</code> and adaptive cadence.</p>\n<p>There is one transport detail worth knowing. In the MVP, <code>http_request</code> returns <code>upgrade = true</code>, so every request — including renders — is promoted to <code>http_request_update</code>. This sidesteps query response-certification while the protocol stabilizes.</p>\n<blockquote><strong>Roadmap:</strong> Certified query rendering for cacheable public pages, so reads can be served as true (certified) queries without the upgrade.</blockquote>"; prevSlug = "zero-trust"; prevTitle = "Building a Zero-Trust App"; nextSlug = "client-bridge"; nextTitle = "The Client Bridge" } };
        case ("client-bridge") { ?{ slug = "client-bridge"; title = "The Client Bridge"; section = "Architecture"; html = "<h1 id=\"the-client-bridge\">The Client Bridge</h1>\n<p>Every MotoView app ships with the same small client. It is the only code that runs in the browser, and it is deliberately split into two parts: a <strong>brain</strong> written in Rust and compiled to WebAssembly, and a thin pair of <strong>hands</strong> written by hand in JavaScript. The canister serves both, plus a stylesheet, from three fixed URLs:</p>\n<pre class=\"lang-text\"><code>/motoview.wasm   the brain  (Rust -&gt; WASM)\n/motoview.js     the hands  (DOM/fetch/timer glue)\n/motoview.css    base styles</code></pre>\n<p>You never write any of this. Your <code>.mview</code> pages compile to Motoko, the runtime serves the bridge automatically, and the bridge keeps the page in sync with the canister.</p>\n<h2 id=\"the-brain-rust-compiled-to-wasm\">The brain: Rust compiled to WASM</h2>\n<p>The WASM module owns everything that needs real logic. It runs the <a href=\"/docs/protocol\">protocol</a> (<code>motoview/1</code>), interprets the batch JSON the server returns, sequences events, and drives the adaptive polling state machine that decides <em>when</em> to ask the server for a fresh render.</p>\n<p>That cadence is the brain's most visible job. After you interact with the page it runs <strong>hot</strong> (~350ms for about 3 seconds), settles to <strong>warm</strong> (~2.5s while the tab is visible), drops to <strong>cold</strong> (~15s) when idle, slows to <strong>hidden</strong> (~45s) when the tab is backgrounded, and backs off exponentially when offline. When a batch arrives whose <code>batchId</code> matches what is already on screen, the server reports <code>unchanged</code> and the brain skips the work entirely.</p>\n<p>Keeping this in Rust/WASM means the protocol logic is one compiled artifact, the same in every app, fast, and not something you can accidentally fork or monkey-patch.</p>\n<h2 id=\"the-hands-the-js-glue\">The hands: the JS glue</h2>\n<p>WASM cannot touch the DOM, open a <code>fetch</code>, or set a timer on its own. Those capabilities only exist on the JavaScript side of the boundary, so the brain reaches them through a small, hand-written glue file. No bundler, no npm, no framework — just the primitives the brain asks for:</p>\n<ul><li>swap the <code>#mv-root</code> content when a batch says <code>changed</code></li><li>issue <code>GET /_motoview/render</code> and <code>POST /_motoview/event</code></li><li>start and stop timers for the polling cadence</li><li>preserve focus, scroll position, and in-flight input across a DOM swap</li></ul>\n<p>The glue is intentionally dumb. It holds no application state and makes no decisions; it does what the brain tells it and reports DOM events back.</p>\n<h2 id=\"preserving-focus-scroll-and-input\">Preserving focus, scroll, and input</h2>\n<p>A naive server-driven framework feels broken because every refresh blows away what the user was doing. MotoView's glue handles the three things people notice immediately. Before a swap it records which element is focused, the caret/selection position in text inputs, the current scroll offsets, and any values the user has typed but not yet submitted. After the brain applies the new HTML into <code>#mv-root</code>, the glue restores them.</p>\n<p>This is why a form stays usable while the page is polling behind it:</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputEmail name=\"email\" label=\"Email\" bind=\"@model.email\" required /&gt;\n  &lt;InputText name=\"name\" label=\"Name\" bind=\"@model.name\" required /&gt;\n  &lt;Button kind=\"primary\"&gt;Send&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<p>You type into <code>email</code>, a warm poll lands, the page re-renders — and your text, caret, and focus are exactly where you left them. See <a href=\"/docs/forms\">Forms &amp; Validation</a> for how <code>bind</code> and <code>secure</code> work, and <a href=\"/docs/events\">Events</a> for how the submit becomes an update.</p>\n<h2 id=\"why-wasm-and-the-honest-tradeoffs\">Why WASM, and the honest tradeoffs</h2>\n<p>Putting the brain in WebAssembly buys real things: one shared, compiled implementation of the protocol and polling logic; no JavaScript application code to write, audit, or keep in sync with your Motoko; and a clean line between <em>logic</em> (portable, testable Rust) and <em>side effects</em> (the unavoidable browser primitives).</p>\n<p>The honest cost is the boundary itself. WASM has no direct access to the DOM, network, or timers — every one of those calls has to cross into the JS glue and back. So the glue is not optional, and it is the one piece of the client that is plain JavaScript. We keep it as small and side-effect-driven as we can, but it exists because the browser gives WASM no other door to the page.</p>\n<p>The current build also makes a transport tradeoff worth knowing: on IC, <code>http_request</code> returns <code>upgrade = true</code>, so every request is served by <code>http_request_update</code>. That sidesteps query response-certification and keeps the bridge simple. Certified query rendering for cacheable public pages is <strong>Roadmap</strong>, as are keyed-region DOM patches that would let the glue update a sub-tree instead of swapping all of <code>#mv-root</code>.</p>\n<p>Next: read the <a href=\"/docs/protocol\">Protocol</a> for the exact batch shapes the brain consumes.</p>"; prevSlug = "protocol"; prevTitle = "The Protocol"; nextSlug = "runtime"; nextTitle = "The Motoko Runtime" } };
        case ("runtime") { ?{ slug = "runtime"; title = "The Motoko Runtime"; section = "Architecture"; html = "<h1 id=\"the-motoko-runtime\">The Motoko Runtime</h1>\n<p>When you run <code>motoview build</code>, your <code>.mview</code> files are compiled to Motoko and wired into a canister actor. That actor never works alone — it leans on the <strong>MotoView runtime</strong>, a Motoko library shipped in <code>runtime/</code> (the mops package <code>motoview</code>). The runtime is what turns an HTTP request from the browser into rendered HTML, dispatched events, and versioned UI batches.</p>\n<p>This page walks through the modules in <code>runtime/src</code> and shows how a generated actor stitches pages, layouts, and assets together behind <code>http_request</code> / <code>http_request_update</code>.</p>\n<h2 id=\"what-lives-in-runtime-src\">What lives in <code>runtime/src</code></h2>\n<p>The runtime is deliberately small and dependency-free. Each module owns one concern:</p>\n<ul><li><strong>App</strong> — the orchestrator class. Holds registered pages and layouts, routes incoming requests, renders the matching page into its layout, dispatches events to typed handlers, and produces the batch JSON described in the <a href=\"/docs/protocol\">protocol</a>.</li><li><strong>Types</strong> — the shared types: requests, responses, batches, route entries, render context, handler signatures.</li><li><strong>Router</strong> — matches a request path against registered routes, including parameters like <code>/products/{id}</code> and typed <code>/orders/{id:Nat}</code>.</li><li><strong>Url</strong> — parses paths, query strings, and form-encoded bodies (used to read <code>path</code>, <code>lastBatchId</code>, and event payloads).</li><li><strong>Json</strong> — minimal JSON building/escaping for batch responses.</li><li><strong>Hash</strong> — hashing helpers, including the <code>batchId</code> hash that lets the client skip unchanged renders.</li><li><strong>Security / Sha256</strong> — SHA-256 and HMAC-SHA256 in pure Motoko, verified against standard test vectors. This is what mints and re-derives the signed <code>secure</code> form token (path + handler + caller principal + nonce + expiry + field-schema hash).</li><li><strong>Html.Builder</strong> — a small typed builder for emitting escaped HTML, used by generated page code.</li><li><strong>Base64</strong> — encode/decode for tokens and binary asset payloads.</li><li><strong>ClientAssets</strong> — serves the only browser code: <code>/motoview.wasm</code> (the WASM brain), <code>/motoview.js</code> (the JS glue hands), and <code>/motoview.css</code>.</li></ul>\n<h2 id=\"how-a-generated-actor-wires-everything\">How a generated actor wires everything</h2>\n<p>The compiler emits a Motoko actor that constructs an <code>App</code>, registers each compiled page and layout, and forwards the IC HTTP entry points to it.</p>\n<pre class=\"lang-motoko\"><code>import App \"mo:motoview/App\";\nimport Types \"mo:motoview/Types\";\n\nimport HomePage \"Generated/HomePage\";\nimport MainLayout \"Generated/MainLayout\";\n\nactor {\n  let app = App.App();\n\n  // Pages and layouts compiled from your .mview files\n  app.layout(\"main\", MainLayout.render);\n  app.page(\"/\", HomePage.render, HomePage.handlers);\n\n  public query func http_request(req : Types.Request) : async Types.Response {\n    // MVP transport: upgrade so every request is served by update.\n    app.httpRequest(req);\n  };\n\n  public func http_request_update(req : Types.Request) : async Types.Response {\n    app.httpRequestUpdate(req);\n  };\n};</code></pre>\n<p>Two entry points, two jobs:</p>\n<ul><li><strong><code>http_request</code></strong> — the IC query entry point. In the MVP it returns <code>upgrade = true</code>, which tells the replica to re-issue the call as an update. This sidesteps query response-certification while the protocol matures. (Certified query rendering for cacheable public pages is <strong>Roadmap</strong>.)</li><li><strong><code>http_request_update</code></strong> — does the real work. The <code>App</code> routes the path through the <code>Router</code>, then:</li><li>For a first load (<code>GET /page</code>), it renders the page into its layout and returns server-rendered HTML with content inside <code>&lt;div id=\"mv-root\"&gt;</code>.</li><li>For a sync poll (<code>GET /_motoview/render</code>), it compares the freshly computed <code>batchId</code> against <code>lastBatchId</code> and returns <code>\"changed\"</code> or <code>\"unchanged\"</code>.</li><li>For an event (<code>POST /_motoview/event</code>), it verifies the <code>secure</code> token via <code>Security</code>, dispatches <code>handlerId</code> + args to the typed Motoko handler, re-renders, and returns the new batch immediately.</li><li>For <code>/motoview.wasm</code>, <code>/motoview.js</code>, <code>/motoview.css</code>, it serves bytes from <code>ClientAssets</code>.</li></ul>\n<h2 id=\"the-request-lifecycle\">The request lifecycle</h2>\n<pre class=\"lang-bash\"><code># First load: full HTML document\nGET /                         -&gt; &lt;html&gt; ... &lt;div id=\"mv-root\"&gt; ... &lt;/div&gt;\n\n# Background sync (adaptive polling)\nGET /_motoview/render?path=/&amp;lastBatchId=ab12  -&gt; { \"status\": \"unchanged\" }\n\n# An interaction\nPOST /_motoview/event         -&gt; { \"status\": \"changed\", \"batchId\": \"cd34\", \"html\": \"...\" }</code></pre>\n<p>Because every response carries a <code>batchId</code> (a hash of rendered state from <code>Hash</code>), the WASM client only swaps the DOM when something actually changed.</p>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li>See <a href=\"/docs/events\">Events</a> for how <code>@click</code> handlers are baked into <code>data-mv-arg*</code> attributes and dispatched server-side.</li><li>See <a href=\"/docs/security\">Forms &amp; Security</a> for the full <code>secure</code> token derivation.</li><li>See <a href=\"/docs/protocol\">The Protocol</a> for batch statuses and the adaptive polling cadence.</li></ul>"; prevSlug = "client-bridge"; prevTitle = "The Client Bridge"; nextSlug = "deployment"; nextTitle = "Deployment" } };
        case ("deployment") { ?{ slug = "deployment"; title = "Deployment"; section = "Deployment"; html = "<h1 id=\"deployment\">Deployment</h1>\n<p>MotoView apps deploy the same way any Internet Computer canister does: with <code>dfx</code>. There is no Node build step, no bundler, and no JavaScript pipeline to maintain. You compile your <code>.mview</code> files to Motoko, then ship the canister. This page covers building, deploying locally and to mainnet, the transport model, asset serving, and SEO.</p>\n<h2 id=\"build-then-deploy\">Build, then deploy</h2>\n<p>The two-step rhythm is <strong>compile</strong> and <strong>deploy</strong>. <code>motoview build</code> turns your <code>.mview</code> files into Motoko; <code>dfx</code> installs the resulting canister.</p>\n<pre class=\"lang-bash\"><code># Compile .mview -&gt; Motoko\nmotoview build\n\n# Deploy to a local replica\ndfx start --background\ndfx deploy</code></pre>\n<p>During development, <code>motoview dev</code> wraps both steps and watches for changes:</p>\n<pre class=\"lang-bash\"><code>motoview dev</code></pre>\n<p>This builds, runs <code>dfx deploy</code> against your local replica, and recompiles on save so you can iterate without restarting anything.</p>\n<h2 id=\"deploying-to-mainnet\">Deploying to mainnet</h2>\n<p>Mainnet deployment is the same <code>dfx deploy</code> with the <code>ic</code> network selected. The difference is that mainnet runs on <strong>cycles</strong>, the IC's compute and storage fuel.</p>\n<pre class=\"lang-bash\"><code>motoview build\ndfx deploy --network ic</code></pre>\n<p>Your identity needs a cycles balance to create and top up the canister. Convert ICP to cycles or use a faucet, then check and top up as needed:</p>\n<pre class=\"lang-bash\"><code># How many cycles does the canister hold?\ndfx canister --network ic status YOUR_CANISTER\n\n# Top up from your wallet\ndfx canister --network ic deposit-cycles 2_000_000_000_000 YOUR_CANISTER</code></pre>\n<p>A MotoView canister keeps UI state in memory across calls, so it consumes cycles for both compute and the storage backing that state. Watch the balance after launch and budget top-ups accordingly.</p>\n<h2 id=\"the-transport-model-upgrade-true\">The transport model (upgrade=true)</h2>\n<p>MotoView serves every request through the canister's HTTP interface. In the MVP, <code>http_request</code> returns <code>upgrade = true</code>, which tells the replica to re-run the request as <code>http_request_update</code>. This sidesteps query response-certification entirely: every page render and every event is handled as an update call.</p>\n<pre class=\"lang-motoko\"><code>public query func http_request(req : HttpRequest) : async HttpResponse {\n  // MVP: defer everything to the update path\n  { status_code = 200; headers = []; body = \"\"; upgrade = ?true };\n};\n\npublic func http_request_update(req : HttpRequest) : async HttpResponse {\n  // Real rendering and event dispatch happen here\n};</code></pre>\n<p>The practical consequences: first loads, sync polls (<code>/_motoview/render</code>), and events (<code>/_motoview/event</code>) all flow through the update path, so responses reflect the latest state immediately. The tradeoff is that requests pay update-call latency rather than query speed. <strong>Certified query rendering for cacheable public pages is Roadmap.</strong> See <a href=\"/docs/protocol\">Protocol</a> for the full request/batch lifecycle.</p>\n<h2 id=\"serving-assets\">Serving assets</h2>\n<p>The browser client ships as three files served by the canister itself:</p>\n<ul><li><code>/motoview.wasm</code> — the Rust client compiled to WebAssembly (the \"brain\")</li><li><code>/motoview.js</code> — the small hand-written glue that loads the wasm and provides DOM, fetch, and timer primitives</li><li><code>/motoview.css</code> — base styles for the built-in components</li></ul>\n<p>These are emitted by the build and served from your canister's HTTP handler. There is no separate asset canister to wire up and no application JavaScript beyond the glue loader. Your layout references them in <code>@head</code>:</p>\n<pre class=\"lang-razor\"><code>@head {\n  &lt;link rel=\"stylesheet\" href=\"/motoview.css\" /&gt;\n  &lt;script src=\"/motoview.js\" defer&gt;&lt;/script&gt;\n}</code></pre>\n<h2 id=\"seo-sitemap-xml-and-robots-txt\">SEO: sitemap.xml and robots.txt</h2>\n<p>Because the first load is fully server-rendered HTML with page content inside <code>&lt;div id=\"mv-root\"&gt;</code>, crawlers see real markup. Set per-page metadata with the directives in <a href=\"/docs/pages\">Pages</a>:</p>\n<pre class=\"lang-razor\"><code>@page \"/products/{id:Nat}\"\n@title \"Product \" # product.name\n@description product.summary\n@canonical \"https://example.com/products/\" # Nat.toText(id)</code></pre>\n<p>Serve <code>robots.txt</code> and <code>sitemap.xml</code> as routes from your canister so crawlers can discover and index your pages. Point the sitemap at your canonical mainnet origin and list the URLs you want indexed.</p>\n<pre class=\"lang-text\"><code>User-agent: *\nAllow: /\nSitemap: https://example.com/sitemap.xml</code></pre>\n<h2 id=\"mops-packaging\">mops packaging</h2>\n<p>The MotoView runtime is distributed as the mops package <strong><code>motoview</code></strong>. Add it to your project's <code>mops.toml</code>:</p>\n<pre class=\"lang-toml\"><code>[dependencies]\nmotoview = \"...\"</code></pre>\n<p>For local development against a checkout of the runtime, you can instead pass it directly to the Motoko compiler through your <code>dfx.json</code> build args:</p>\n<pre class=\"lang-json\"><code>{\n  \"canisters\": {\n    \"app\": {\n      \"type\": \"motoko\",\n      \"main\": \".mvbuild/main.mo\",\n      \"args\": \"--package motoview ../../runtime/src\"\n    }\n  }\n}</code></pre>\n<p>Either way, the runtime provides the HTTP serving, batch protocol, and the SHA-256/HMAC primitives used by <code>secure</code> forms. With the package resolved, <code>motoview build</code> followed by <code>dfx deploy</code> is all it takes to ship.</p>"; prevSlug = "runtime"; prevTitle = "The Motoko Runtime"; nextSlug = "cli"; nextTitle = "CLI Reference" } };
        case ("cli") { ?{ slug = "cli"; title = "CLI Reference"; section = "Deployment"; html = "<h1 id=\"cli-reference\">CLI Reference</h1>\n<p>The <code>motoview</code> binary is your single entry point for scaffolding, compiling, and deploying a MotoView app. It is a Rust crate you install with <code>cargo</code>, and it speaks to <code>dfx</code> under the hood for the deploy step. There is no Node, no npm, and no JavaScript build tooling anywhere in the pipeline — that is the point.</p>\n<pre class=\"lang-bash\"><code>motoview &lt;command&gt; [options]</code></pre>\n<p>All commands run from your project root, where <code>motoview.json</code>, <code>dfx.json</code>, and <code>mops.toml</code> live.</p>\n<h2 id=\"motoview-new\">motoview new</h2>\n<p>Scaffold a new project.</p>\n<pre class=\"lang-bash\"><code>motoview new my-shop</code></pre>\n<p>This creates the standard <a href=\"/docs/quickstart\">project layout</a>: <code>src/Pages/</code>, <code>src/Layouts/</code>, <code>src/Components/</code>, <code>src/Services/</code>, <code>src/Models/</code>, plus <code>motoview.json</code>, <code>dfx.json</code>, and <code>mops.toml</code>. It wires the runtime (the <code>motoview</code> mops package) so the generated actor can serve HTTP, and it drops in a working starter page so your first <code>motoview dev</code> shows something real in the browser.</p>\n<h2 id=\"motoview-build\">motoview build</h2>\n<p>Compile every <code>.mview</code> file in <code>src/</code> to Motoko.</p>\n<pre class=\"lang-bash\"><code>motoview build</code></pre>\n<p><code>build</code> is the heart of the toolchain. It parses your templates and <code>@code { ... }</code> blocks and emits a Motoko actor to <code>.mvbuild/main.mo</code> (a gitignored build artifact). It does <strong>not</strong> deploy — use it in CI or when you want to inspect generated Motoko before shipping.</p>\n<h3 id=\"the-build-pipeline\">The build pipeline</h3>\n<p>Each <code>motoview build</code> runs these steps in order:</p>\n<ol><li><strong>Discover</strong> all <code>.mview</code> files in <code>src/Pages</code>, <code>src/Layouts</code>, and <code>src/Components</code>.</li><li><strong>Parse</strong> template markup and directives — <code>@page</code>, <code>@layout</code>, <code>@code</code>, <code>@if</code>, <code>@for</code>, <code>@switch</code>, components, and the inline <code>@expr</code> outputs.</li><li><strong>Resolve routes</strong> from <code>@page \"/path\"</code> directives, including parameterized routes like <code>@page \"/orders/{id:Nat}\"</code>.</li><li><strong>Type-bind handlers</strong>: each <code>@click=\"save\"</code> / <code>@submit=\"send\"</code> is matched to a typed Motoko function, and handler arguments are lowered into <code>data-mv-arg*</code> attributes so the WASM client can forward <code>handlerId + args</code>.</li><li><strong>Generate Motoko</strong>: render functions, the event dispatcher, and the <a href=\"/docs/protocol\"><code>motoview/1</code> protocol</a> wiring (<code>http_request</code> / <code>http_request_update</code>, the <code>/_motoview/render</code> and <code>/_motoview/event</code> endpoints).</li><li><strong>Write output</strong> to the <code>.mvbuild/main.mo</code> actor (override with <code>--out</code>).</li></ol>\n<p>If a template references a handler that does not exist, or a <code>bind=\"@model.field\"</code> points at an unknown field, the build fails here with a clear error — before you ever reach the replica.</p>\n<h2 id=\"motoview-dev\">motoview dev</h2>\n<p>Build, deploy locally, and watch.</p>\n<pre class=\"lang-bash\"><code>motoview dev</code></pre>\n<p>This is your day-to-day loop. It runs <code>motoview build</code>, hands the generated Motoko to <code>dfx</code> for a local deploy, and then watches your <code>.mview</code> files. Edit a page, save, and the project recompiles and redeploys so you can refresh the browser. The adaptive polling client (hot ~350ms after an interaction, warm ~2.5s while visible) means external state changes show up without a manual reload — see <a href=\"/docs/protocol\">Protocol</a> for the cadence details.</p>\n<h2 id=\"motoview-shell\">motoview shell</h2>\n<p>Scaffold native desktop + mobile wrappers around your deployed app.</p>\n<pre class=\"lang-bash\"><code>motoview shell --url https://&lt;canister-id&gt;.icp0.io --name \"My App\"</code></pre>\n<p>This writes <code>clients/desktop-tauri/</code> (a <a href=\"https://tauri.app\">Tauri</a> v2 config whose window loads your canister URL) and <code>clients/mobile-capacitor/</code> (a <a href=\"https://capacitorjs.com\">Capacitor</a> config whose <code>server.url</code> is your canister), plus a README. Your app's logic stays on-chain; the shells are just native windows pointed at it.</p>\n<p><code>motoview shell</code> generates the <strong>configs</strong> — building the actual binaries needs the platform toolchains you supply (<code>cargo tauri build</code> for desktop; <code>npx cap add ios|android</code> + Xcode/Android Studio for mobile). Before reaching for a native binary, remember every MotoView app is already an <strong>installable PWA</strong> (offline-capable, standalone window) with no build step.</p>\n<h2 id=\"motoview-compile\">motoview compile</h2>\n<p>Compile a single file. Useful for debugging the compiler or inspecting one template's output.</p>\n<pre class=\"lang-bash\"><code>motoview compile src/Pages/Products.mview</code></pre>\n<p>Where <code>motoview build</code> walks the whole project, <code>compile</code> targets one <code>.mview</code> and emits its Motoko so you can read exactly what a given template produces. Reach for this when a page misbehaves and you want to see the generated render and dispatch code in isolation.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<p>Before any of the above, install the toolchain:</p>\n<pre class=\"lang-bash\"><code># DFINITY SDK\nsh -ci \"$(curl -fsSL https://internetcomputer.org/install.sh)\"\n\n# Rust + the wasm target used by the browser client\nrustup target add wasm32-unknown-unknown\n\n# wasm-opt (binaryen)\nbrew install binaryen\n\n# The MotoView compiler\ncargo install motoview</code></pre>\n<p>The runtime is added per-project as the <code>motoview</code> mops package, or as a local path dependency in <code>dfx.json</code> build args:</p>\n<pre class=\"lang-json\"><code>\"args\": \"--package motoview ../../runtime/src\"</code></pre>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul><li>New to the framework? Start with <a href=\"/docs/quickstart\">Getting Started</a>.</li><li>Wiring up handlers? See <a href=\"/docs/events\">Events</a>.</li><li>Curious how the browser stays in sync? Read the <a href=\"/docs/protocol\">Protocol</a> page.</li></ul>"; prevSlug = "deployment"; prevTitle = "Deployment"; nextSlug = "ai-tools"; nextTitle = "Using AI (Claude, Copilot, Cursor)" } };
        case ("ai-tools") { ?{ slug = "ai-tools"; title = "Using AI (Claude, Copilot, Cursor)"; section = "Tooling"; html = "<h1 id=\"using-ai-claude-copilot-cursor\">Using AI (Claude, Copilot, Cursor)</h1>\n<p>MotoView is a small framework, but it is an unusual one. You write <code>.mview</code> files, events run server-side, and there is no application JavaScript anywhere. General-purpose AI assistants do not know this. Left to their own training data, they will reach for React, sprinkle in <code>fetch</code>, invent directives, and reconstruct validation in the browser — exactly the patterns MotoView exists to remove.</p>\n<p>To fix that, MotoView ships <strong>assistant rule files</strong> in every scaffolded project. They teach Claude, GitHub Copilot, and Cursor the real shape of the framework so their suggestions match the facts instead of guessing.</p>\n<h2 id=\"where-the-rule-files-live\">Where the rule files live</h2>\n<p><code>motoview new</code> writes three files, one per assistant, all sourced from the same canonical reference:</p>\n<pre class=\"lang-text\"><code>skills/motoview/SKILL.md          # Claude / Claude Code skill\n.github/copilot-instructions.md   # GitHub Copilot\n.cursor/rules/motoview.mdc        # Cursor</code></pre>\n<p>They cover the same ground in each tool's expected format: the <code>.mview</code> file model, the directive list, the event flow, secure forms, the built-in components, and the protocol. Keeping them next to your code means they travel with the repository — clone it on another machine and the guidance comes along.</p>\n<h2 id=\"how-to-use-them\">How to use them</h2>\n<p>In most cases you do nothing. Each assistant picks its file up automatically.</p>\n<ul><li><strong>Claude / Claude Code</strong> — discovers <code>skills/motoview/SKILL.md</code> and loads it as a skill when you work in a MotoView project.</li><li><strong>GitHub Copilot</strong> — reads <code>.github/copilot-instructions.md</code> and applies it to completions and Copilot Chat across the workspace.</li><li><strong>Cursor</strong> — applies <code>.cursor/rules/motoview.mdc</code> as a project rule for inline edits and chat.</li></ul>\n<p>If your assistant supports manual context, point it at the relevant file directly, for example \"follow <code>skills/motoview/SKILL.md</code>\". When you upgrade the compiler, re-run the scaffold (or copy the latest files from a fresh <code>motoview new</code>) so the rules track the current directive set.</p>\n<h2 id=\"what-the-rules-prevent\">What the rules prevent</h2>\n<p>The single most useful thing these files do is keep an assistant from \"helpfully\" adding a frontend. With the rules loaded, you get suggestions like this:</p>\n<pre class=\"lang-razor\"><code>@page \"/counter\"\n\n@code {\n  var count : Nat = 0;\n  func increment() { count += 1; };\n}\n\n&lt;p&gt;Count: @count&lt;/p&gt;\n&lt;Button kind=\"primary\" @click=\"increment\"&gt;Increment&lt;/Button&gt;</code></pre>\n<p>instead of a <code>useState</code> hook and an <code>onClick</code> that POSTs JSON. Handler arguments are evaluated server-side, so the assistant learns to write <code>@click=\"remove(item.id)\"</code> rather than wiring up a client listener. For the full event flow see <a href=\"/docs/events\">Events</a>; for signed forms and <code>validate</code>, see <a href=\"/docs/forms\">Forms</a> and <a href=\"/docs/validation\">Validation</a>.</p>\n<h2 id=\"directive-cheat-sheet\">Directive cheat-sheet</h2>\n<p>This is the table the rule files lean on. Keep it nearby — it is also the fastest way to sanity-check an AI suggestion.</p>\n<pre class=\"lang-text\"><code>Page &amp; metadata\n  @page \"/path\"            route (\"/products/{id}\", typed \"/orders/{id:Nat}\")\n  @layout NAME             choose a layout\n  @title / @description    head metadata\n  @canonical / @meta       canonical URL / extra meta tags\n  @head                    inject into &lt;head&gt;\n\nStructure\n  @code { ...Motoko... }   state and handlers\n  @style { ...css... }     scoped styles\n  @theme { tokens }        design tokens\n  @section \"name\" { }      named content for a layout\n  @yield                   render the page body in a layout\n  @slot \"name\"             component slot\n\nControl flow\n  @if EXPR { } else { }    conditional\n  @for X in EXPR { }       loop\n  @switch EXPR {           variant match\n    case #Variant { } }\n\nOutput &amp; effects\n  @count  @user.name  @(expr)   inline output\n  @effect Focus(\"#x\")           also ScrollTo, Toast\n  @animate                      transition hint\n\nEvents\n  @click=\"save\"            @click=\"save(arg)\"\n  @submit=\"send\"           @input=\"onType\"   @change\n\nAccess control\n  @authorize               @authorize role=\"Admin\"</code></pre>\n<p>For secure forms, the rules also remind the assistant to add the <code>secure</code> attribute and <code>bind=\"@model.field\"</code>:</p>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputText name=\"name\" label=\"Name\" bind=\"@model.name\" required /&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;Button kind=\"primary\"&gt;Save&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<pre class=\"lang-motoko\"><code>func send() {\n  validate model {\n    name required \"Name is required\";\n  };\n  // persist...\n};</code></pre>\n<h2 id=\"a-note-on-honesty\">A note on honesty</h2>\n<p>The rule files describe what is built and verified, and they mark planned work — keyed-region DOM patches, full Internet Identity login, certified query rendering — as Roadmap. If an assistant suggests one of those as if it exists today, that is a hallucination, not a feature. Trust the cheat-sheet and the rules over the model's instincts.</p>"; prevSlug = "cli"; prevTitle = "CLI Reference"; nextSlug = "directives-reference"; nextTitle = "Directive Reference" } };
        case ("directives-reference") { ?{ slug = "directives-reference"; title = "Directive Reference"; section = "Reference"; html = "<h1 id=\"directive-reference\">Directive Reference</h1>\n<p>Every MotoView feature is reachable through a small set of <code>@</code> directives and component attributes. This page is the quick lookup: each row gives the syntax and a tiny, copy-paste example. For deeper walkthroughs see <a href=\"/docs/events\">Events</a>, <a href=\"/docs/forms\">Forms &amp; Validation</a>, and <a href=\"/docs/components\">Layouts &amp; Components</a>.</p>\n<h2 id=\"page-metadata-directives\">Page &amp; metadata directives</h2>\n<p>These declare what a <code>.mview</code> file <em>is</em> and what the browser sees in <code>&lt;head&gt;</code>.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@page</code></td><td><code>@page \"/path\"</code></td><td><code>@page \"/about\"</code></td></tr><tr><td><code>@page</code> (param)</td><td><code>@page \"/products/{id}\"</code></td><td><code>@page \"/products/{id}\"</code></td></tr><tr><td><code>@page</code> (typed)</td><td><code>@page \"/orders/{id:Nat}\"</code></td><td><code>@page \"/orders/{id:Nat}\"</code></td></tr><tr><td><code>@layout</code></td><td><code>@layout NAME</code></td><td><code>@layout Main</code></td></tr><tr><td><code>@cacheable</code></td><td><code>@cacheable</code></td><td>serve a public page as a fast certified query (parameterized routes via a wildcard)</td></tr><tr><td><code>@title</code></td><td><code>@title EXPR</code></td><td><code>@title \"Product \" # product.name</code></td></tr><tr><td><code>@description</code></td><td><code>@description EXPR</code></td><td><code>@description product.summary</code></td></tr><tr><td><code>@canonical</code></td><td><code>@canonical EXPR</code></td><td><code>@canonical \"https://shop.ic0.app/p/\" # id</code></td></tr><tr><td><code>@meta</code></td><td><code>@meta name=... content=...</code></td><td><code>@meta name=\"robots\" content=\"index\"</code></td></tr><tr><td><code>@head</code></td><td><code>@head { ... }</code></td><td>extra tags injected into <code>&lt;head&gt;</code></td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@page \"/orders/{id:Nat}\"\n@layout Main\n@title \"Order #\" # Nat.toText(id)\n@description \"Details for order \" # Nat.toText(id)</code></pre>\n<h2 id=\"code-style-theming\">Code, style &amp; theming</h2>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@code</code></td><td><code>@code { ...Motoko... }</code></td><td>handlers, state, route params</td></tr><tr><td><code>@style</code></td><td><code>@style { ...css... }</code></td><td>page-scoped CSS</td></tr><tr><td><code>@theme</code></td><td><code>@theme \"name\"</code> / <code>@theme { tokens }</code></td><td>apply a theme package and/or set design tokens</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@code {\n  var count : Nat = 0;\n  func increment() : async () { count += 1 };\n}\n\n@style { .total { font-weight: 600 } }\n@theme \"ocean\" { --mv-primary: #0d9488 }</code></pre>\n<p>Theme packages: <code>midnight</code> (dark), <code>ocean</code>, <code>forest</code>, <code>sunset</code>, <code>slate</code>. Tokens include <code>--mv-primary</code>, <code>--mv-bg</code>, <code>--mv-surface</code>, <code>--mv-text</code>, <code>--mv-border</code>, <code>--mv-radius</code>, <code>--mv-font</code>, … (see <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a>).</p>\n<h2 id=\"layout-composition\">Layout composition</h2>\n<p>Layouts define shared chrome; pages fill named regions.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@yield</code></td><td><code>@yield</code></td><td>renders the page body (in a layout)</td></tr><tr><td><code>@section</code></td><td><code>@section \"name\" { ... }</code></td><td>page-side content for a named region</td></tr><tr><td><code>@slot</code></td><td><code>@slot \"name\"</code></td><td>layout-side placeholder for a section</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@section \"sidebar\" {\n  &lt;nav&gt;Account menu&lt;/nav&gt;\n}</code></pre>\n<pre class=\"lang-razor\"><code>@slot \"sidebar\"\n@yield</code></pre>\n<h2 id=\"control-flow\">Control flow</h2>\n<p>Standard branching and looping, evaluated server-side at render time.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@if</code> / <code>else</code></td><td><code>@if EXPR { } else { }</code></td><td>conditional markup</td></tr><tr><td><code>@for</code></td><td><code>@for X in EXPR { }</code></td><td>iterate a collection</td></tr><tr><td><code>@switch</code></td><td><code>@switch EXPR { case #Variant { } }</code></td><td>match a Motoko variant</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@if cart.isEmpty() {\n  &lt;Alert type=\"info\"&gt;Your cart is empty.&lt;/Alert&gt;\n} else {\n  @for item in cart.items {\n    &lt;Card title=item.name&gt;@item.price&lt;/Card&gt;\n  }\n}\n\n@switch order.status {\n  case #Pending { &lt;Badge type=\"warning\"&gt;Pending&lt;/Badge&gt; }\n  case #Shipped { &lt;Badge type=\"success\"&gt;Shipped&lt;/Badge&gt; }\n}</code></pre>\n<h2 id=\"output-expressions\">Output expressions</h2>\n<p>Print a value into the rendered HTML.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Form</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td>Field</td><td><code>@user.name</code></td><td><code>Hello, @user.name</code></td></tr><tr><td>Count</td><td><code>@count</code></td><td><code>Total: @count</code></td></tr><tr><td>Expression</td><td><code>@(expr)</code></td><td><code>@(price * qty)</code></td></tr><tr><td>Raw HTML</td><td><code>@raw(expr)</code></td><td><code>@raw(doc.html)</code></td></tr></tbody></table>\n<p><code>@user.name</code>, <code>@count</code> and <code>@(expr)</code> are <strong>HTML-escaped</strong> — safe by default. <code>@raw(expr)</code> emits the expression <strong>without escaping</strong>, for trusted server-side HTML (e.g. markdown you rendered yourself). The expression must already be <code>Text</code>. Never pass user input to <code>@raw</code> — it bypasses escaping. To print a literal <code>@</code>, write <code>@@</code>.</p>\n<h2 id=\"keyed-regions\">Keyed regions</h2>\n<p>Give the elements of a list a <code>key</code> so the client can patch only what changed instead of replacing the whole region. The key should be a stable identifier — usually the item's id:</p>\n<pre class=\"lang-mview\"><code>&lt;ul&gt;\n@for item in items {\n    &lt;li key=\"@item.id\"&gt;@item.name &lt;input&gt;&lt;/li&gt;\n}\n&lt;/ul&gt;</code></pre>\n<p><code>key=\"@item.id\"</code> compiles to <code>data-mv-key</code>. On a re-render, the brain (the Rust→WASM client) diffs the keyed regions and touches only what changed: content-only changes <strong>replace</strong> just those items; added, removed and reordered items are <strong>inserted, removed and moved</strong> (a reorder moves the minimum number of nodes). Every node that doesn't change keeps its identity — so its focus, selection, scroll and media state survive an update to its siblings, and even a reorder <em>moves</em> a node rather than recreating it. A change to the surrounding markup safely falls back to a full re-render. All of this logic runs in WASM; there is no application JavaScript.</p>\n<h2 id=\"events\">Events</h2>\n<p>Bind DOM events to typed Motoko handlers. Handler arguments are evaluated <strong>server-side</strong> at render time and baked into <code>data-mv-arg*</code> attributes; the WASM client forwards them on dispatch. See <a href=\"/docs/events\">Events</a>.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Attribute</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@click</code></td><td><code>@click=\"handler\"</code></td><td><code>&lt;Button @click=\"save\"&gt;Save&lt;/Button&gt;</code></td></tr><tr><td><code>@click</code> (arg)</td><td><code>@click=\"handler(arg)\"</code></td><td><code>@click=\"remove(item.id)\"</code></td></tr><tr><td><code>@submit</code></td><td><code>@submit=\"handler\"</code></td><td><code>&lt;form @submit=\"send\"&gt;</code></td></tr><tr><td><code>@input</code></td><td><code>@input=\"handler\"</code></td><td>live field handling</td></tr><tr><td><code>@change</code></td><td><code>@change=\"handler\"</code></td><td>select / checkbox changes</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>&lt;Button @click=\"increment\"&gt;+1&lt;/Button&gt;\n&lt;Button @click=\"remove(item.id)\" kind=\"danger\"&gt;Delete&lt;/Button&gt;</code></pre>\n<h2 id=\"forms-validation-security\">Forms, validation &amp; security</h2>\n<p>The <code>secure</code> attribute mints a signed HMAC-SHA256 token binding path, handler, caller principal, nonce, expiry, and field-schema hash; the server re-derives the MAC and rejects mismatches, expired tokens, and replays. See <a href=\"/docs/forms\">Forms &amp; Validation</a>.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Feature</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td>Secure form</td><td><code>&lt;form @submit=\"h\" secure&gt;</code></td><td>hardened submit</td></tr><tr><td>Two-way bind</td><td><code>bind=\"@model.field\"</code></td><td><code>&lt;InputText bind=\"@model.name\" /&gt;</code></td></tr><tr><td>Validation</td><td><code>validate model { rules }</code></td><td>in the handler</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>&lt;form @submit=\"send\" secure&gt;\n  &lt;InputText label=\"Name\" bind=\"@model.name\" required /&gt;\n  &lt;InputEmail label=\"Email\" bind=\"@model.email\" /&gt;\n  &lt;ValidationSummary /&gt;\n  &lt;Button kind=\"primary\"&gt;Send&lt;/Button&gt;\n&lt;/form&gt;</code></pre>\n<pre class=\"lang-motoko\"><code>func send() {\n  validate model {\n    name required \"Name is required\";\n    email required email;\n    price min 1 \"Price must be at least 1\";\n  };\n}</code></pre>\n<h2 id=\"effects-animation\">Effects &amp; animation</h2>\n<p>Imperative client effects returned in a batch and applied by the WASM client.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@effect Focus</code></td><td><code>@effect Focus(\"#x\")</code></td><td>move focus after render</td></tr><tr><td><code>@effect ScrollTo</code></td><td><code>@effect ScrollTo(\"#id\")</code></td><td>scroll to an element</td></tr><tr><td><code>@effect Toast</code></td><td><code>@effect Toast(\"msg\")</code></td><td>show a transient toast</td></tr><tr><td><code>@animate</code></td><td><code>@animate</code></td><td>mark a region for transition</td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@effect Focus(\"#name\")\n@effect Toast(\"Saved!\")</code></pre>\n<h2 id=\"authorization\">Authorization</h2>\n<table class=\"mv-doc-table\"><thead><tr><th>Directive</th><th>Syntax</th><th>Example</th></tr></thead><tbody><tr><td><code>@authorize</code></td><td><code>@authorize</code></td><td>require an authenticated caller</td></tr><tr><td><code>@authorize</code> (role)</td><td><code>@authorize role=\"Admin\"</code></td><td>require a role <em>(role stores: Roadmap)</em></td></tr></tbody></table>\n<pre class=\"lang-razor\"><code>@authorize role=\"Admin\"\n@page \"/admin/orders\"</code></pre>\n<p>Built-in semantic components (<code>Button</code>, <code>Card</code>, <code>Alert</code>, <code>Badge</code>, <code>InputText</code>, <code>Table</code>, <code>Grid</code>, and more) are documented in <a href=\"/docs/components\">Layouts &amp; Components</a>.</p>"; prevSlug = "ai-tools"; prevTitle = "Using AI (Claude, Copilot, Cursor)"; nextSlug = "roadmap"; nextTitle = "Roadmap & Status" } };
        case ("roadmap") { ?{ slug = "roadmap"; title = "Roadmap & Status"; section = "Reference"; html = "<h1 id=\"roadmap-status\">Roadmap &amp; Status</h1>\n<p>MotoView is a real framework, built and verified end to end — but it is young. This page is the honest line between what works today and what is still ahead. We would rather you ship on solid ground than discover a \"Roadmap\" feature mid-project.</p>\n<p>If a capability is listed under <strong>Verified</strong> or <strong>Implemented</strong>, you can use it now. If it is listed under <strong>Roadmap</strong>, treat it as planned and design around its absence.</p>\n<h2 id=\"verified-end-to-end\">Verified end to end</h2>\n<p>The core loop is not a demo slide — it has been deployed to a local replica and exercised in a real browser:</p>\n<ul><li>The counter example was deployed with <code>dfx</code> and clicked through a browser. Each click flows through the full pipeline: event to <code>http_request_update</code>, a fresh batch is returned, and the DOM is swapped.</li><li><strong>Adaptive polling</strong> picks up external state changes (a value mutated by another caller appears without a user interaction).</li><li><strong>State persists</strong> across calls — the canister is the source of truth.</li><li><strong>SHA-256 and HMAC-SHA256</strong> are implemented in Motoko and pass standard test vectors. This is what backs the <code>secure</code> form token (path + handler + caller principal + nonce + expiry + field-schema hash).</li></ul>\n<pre class=\"lang-bash\"><code>motoview new counter\ncd counter\nmotoview dev          # build .mview -&gt; Motoko, dfx deploy, watch</code></pre>\n<pre class=\"lang-razor\"><code>@page \"/\"\n@code {\n  var count = 0;\n  func increment() { count += 1 };\n}\n\n&lt;Button @click=\"increment\"&gt;Count: @count&lt;/Button&gt;</code></pre>\n<h2 id=\"implemented\">Implemented</h2>\n<p>These ship in the current compiler and runtime:</p>\n<ul><li><strong>The <code>.mview</code> pipeline</strong> — the <code>motoview</code> Rust compiler parses templates plus <code>@code</code> and generates a Motoko actor. The runtime (mops package <code>motoview</code>) serves HTTP from the canister.</li><li><strong>Directives</strong> — <code>@page</code> (including <code>/products/{id}</code> and typed <code>/orders/{id:Nat}</code>), <code>@layout</code>, <code>@title</code>, <code>@description</code>, <code>@canonical</code>, <code>@meta</code>, <code>@section</code> / <code>@yield</code>, <code>@head</code>, <code>@slot</code>, <code>@code</code>, <code>@style</code>, <code>@theme</code>, control flow (<code>@if</code>/<code>else</code>, <code>@for</code>, <code>@switch</code>), inline output, <code>@effect</code>, <code>@animate</code>.</li><li><strong>Events</strong> — <code>@click</code>, <code>@submit</code>, <code>@input</code>, <code>@change</code>, with server-evaluated arguments baked into <code>data-mv-arg*</code>. See <a href=\"/docs/events\">Events</a>.</li><li><strong>Forms, validation, security</strong> — <code>secure</code> forms, <code>bind=\"@model.field\"</code>, the <code>validate model { ... }</code> block, <code>&lt;ValidationSummary /&gt;</code>, and per-field errors. See <a href=\"/docs/forms\">Forms &amp; Validation</a> and <a href=\"/docs/security\">Security</a>.</li><li><strong>Components</strong> — capitalized app components in <code>src/Components/*.mview</code> plus built-ins (<code>Button</code>, <code>Card</code>, <code>Alert</code>, <code>Badge</code>, <code>InputText</code>, <code>InputEmail</code>, <code>InputNumber</code>, <code>TextArea</code>, <code>ValidationSummary</code>, <code>Table</code>, <code>PageHeader</code>, <code>Grid</code>). See <a href=\"/docs/components\">Components</a>.</li><li><strong>The WASM client</strong> — the browser \"brain\" (the <code>motoview/1</code> protocol, adaptive polling state machine, batch interpretation, event sequencing) is Rust compiled to WebAssembly, with a tiny hand-written JS glue for DOM, fetch, timers, and focus/scroll/input preservation. No npm, no bundler.</li><li><strong>Output</strong> — escaped interpolation (<code>@count</code>, <code>@(expr)</code>) plus <code>@raw(expr)</code> for trusted, unescaped server-rendered HTML, and <code>@@</code> for a literal <code>@</code>. The compiler scans <code>Model</code>/<code>Service</code> record types so <code>@item.name</code> interpolates with the field's real type (<code>Text</code> directly, <code>Nat</code>/<code>Int</code>/<code>Float</code>/<code>Bool</code> converted) instead of a <code>debug_show</code> fallback.</li><li><strong>Tooling</strong> — <code>motoview check</code> builds and type-checks the generated actor, mapping any <code>moc</code> error back to the originating <code>.mview</code>. The generated actor is a <strong>build artifact</strong> in <code>.mvbuild/</code> (gitignored, like Blazor's <code>obj/</code>) — you never read or commit it. A parser/codegen regression suite runs with <code>make test</code>.</li><li><strong>Examples &amp; apps</strong> — <code>counter</code>, <code>contact</code> (secure form), <code>crm</code> (Kanban), <code>products</code> (CRUD), <code>blog</code> (SEO) and <code>svg-network</code> (interactive SVG) under <code>examples/</code>; two full apps under <code>apps/</code> — <strong>bzzz</strong> (a Discord × X × forum × WhatsApp super-app) and <strong>this site</strong> (the docs + marketing site, itself a MotoView canister).</li></ul>\n<h2 id=\"production-features\">Production features</h2>\n<p>Built and verified end to end — locally and, where it matters, against the IC mainnet boundary (the playground):</p>\n<ul><li><strong>Upgrade-stable persistence</strong> — a service exposing <code>mvStableSave()</code>/<code>mvStableLoad(Blob)</code> (a Candid round-trip) gets an auto-generated <code>stable var</code> plus <code>preupgrade</code>/<code>postupgrade</code> hooks, so its state survives <code>dfx deploy --mode upgrade</code>. See <a href=\"/docs/persistence\">Persistence</a>.</li><li><strong>Internet Identity login</strong> — hand-rolled, no npm and no agent-js: a browser IC agent makes one authenticated call, the runtime mints an httpOnly session cookie, and <code>ctx.caller</code> resolves from it. Served at <code>/mv-auth.js</code>; add <code>&lt;button data-mv-signin&gt;</code>. Includes per-principal session revocation.</li><li><strong>Role stores</strong> — <code>@authorize role=\"Admin\"</code> is enforced against a persisted, per-principal role store. Manage roles from any handler via <code>ctx.hasRole / grantRole / revokeRole / claimRole / callerRoles</code>; <code>claimRole</code> is a first-come bootstrap for the first admin. Survives <code>--mode upgrade</code>. See <a href=\"/docs/security\">Security</a>.</li><li><strong>Certified query rendering</strong> — static framework assets and pages marked <code>@cacheable</code> are served as fast <strong>certified queries</strong> (HTTP response-certification v2) instead of upgrading to an update call. Parameterized cacheable routes are covered by a single wildcard certificate (<code>/u/{handle}</code> → <code>/u/&lt;*&gt;</code>). Dynamic pages keep the consensus-validated update path.</li><li><strong>Animation engine</strong> — a built-in CSS animation library (entrances, exits, attention-seekers; transform/opacity only, with a <code>prefers-reduced-motion</code> guard). Play one from a handler with <code>animate(\"#sel\",\"name\")</code>, or declaratively on keyed list items via <code>enter=\"…\"</code> / <code>exit=\"…\"</code> — the WASM client plays the entrance on insert and the exit before removal. No application JavaScript. See <a href=\"/docs/drag-drop-and-effects\">Effects &amp; Animations</a>.</li><li><strong>Fluent UI 2 design system</strong> — MotoView's design foundation is the authentic Microsoft Fluent 2 token system (the real <code>--colorBrandBackground</code> / <code>--colorNeutralForeground1</code> / type ramp / spacing / radii / shadows / motion). <code>@theme brand=\"#hex\"</code> generates a complete Fluent theme — the 16-step brand ramp + every brand alias for light <strong>and</strong> dark — from one color; dark mode is automatic (<code>prefers-color-scheme</code>) or forced with <code>data-theme</code>. The full Fluent component set ships as built-ins — buttons, inputs (text/select/combobox/search/checkbox/radio/switch), cards, badges, tags, avatars/personas/groups, nav rail, app bar, tablist, breadcrumb, menu, message bar, accordion, tooltip, popover, and Dialog/Drawer modals — all token-driven and CSS-only (the modals use a checkbox-hack, no JS). The flagship <a href=\"apps/bzzz\">Bzzz</a> app is Fluent-designed. See <a href=\"/docs/styling-and-themes\">Styling &amp; Themes</a> and the live <code>/components</code> gallery. Legacy presets (<code>@theme \"midnight\"</code> / <code>ocean</code> / …) and <code>@theme { token overrides }</code> still work.</li><li><strong>PWA, offline-first &amp; native shells</strong> — every app is an installable PWA: the runtime links a web manifest + iOS/standalone meta into every page and serves an offline-first service worker (auto-registered by the client) that precaches the app shell, serves immutable assets cache-first, and serves navigations network-first with a cache fallback (a visited page still opens offline). Verified in Chrome: a deployed app meets the install criteria. For true native packaging, <code>motoview shell --url &lt;canister-url&gt;</code> scaffolds desktop (Tauri) and mobile (Capacitor) wrappers that load the live canister — you build the binaries with the platform toolchains (Tauri CLI / Xcode / Android SDK).</li><li><strong>Keyed-region DOM patches</strong> — give list items a <code>key=\"...\"</code> and the WASM brain diffs the keyed regions, patching precisely: content changes replace just those items; added/removed/reordered items are inserted/removed/moved (reorder moves the minimum number of nodes). Untouched and merely-moved nodes keep their live state (focus, selection, scroll, media). All the diffing runs in WASM — no application JavaScript. See <a href=\"/docs/directives-reference\">Keyed regions</a>.</li><li><strong>Zero-trust E2E encryption — the browser decrypts locally with vetKeys.</strong> A MotoView app can hold <strong>only ciphertext</strong> on-chain while the plaintext exists only in the user's browser tab. An opt-in Rust crypto module (<code>/motoview-crypto.wasm</code>, <code>ic-vetkeys</code>/BLS12-381 + IBE, fetched on demand) does the crypto; the canister auto-exposes <code>GET /_motoview/vetkd/public-key</code> + <code>POST /_motoview/vetkd/derive</code> (the vetKey bound to the session caller); and two declarative attributes — <code>data-mv-encrypt</code> on a form field, <code>data-mv-decrypt=\"&lt;ciphertext&gt;\"</code> on an element — make the glue encrypt before submit and decrypt on render. All crypto is in WASM; no application JavaScript. With <code>EncStore</code> (ciphertext-only storage) and <code>Audit</code> (append-only server-side log) this is the full zero-trust stack. <strong>Verified in a real browser</strong> on a deployed canister (the served HTML carries only the <code>IbeCiphertext</code>; the note round-trips to the exact plaintext locally). See <a href=\"/docs/zero-trust\">Zero-Trust</a> and <a href=\"https://github.com/miadey/MotoView\"><code>examples/vault</code></a>.</li></ul>\n<h3 id=\"transport-note\">Transport note</h3>\n<p>By default <code>http_request</code> returns <code>upgrade = true</code>, so a request is served by <code>http_request_update</code> — consensus-validated, always fresh. Static assets and pages you mark <code>@cacheable</code> are the exception: they're served as certified queries (no consensus round-trip).</p>\n<h2 id=\"roadmap\">Roadmap</h2>\n<p>Not yet built. Do not design around these — they are labeled honestly as planned.</p>\n<p><strong>Next</strong></p>\n<ul><li><strong>Certifying the root <code>/</code> and exact-vs-wildcard prefix collisions</strong> — <code>@cacheable</code> works for most routes; the root path and an exact route that collides with a wildcard prefix (e.g. <code>/docs</code> alongside <code>/docs/{slug}</code>) are rejected by the boundary today and safely fall back to the update path.</li></ul>\n<p><strong>Later</strong></p>\n<ul><li><strong>A visual page designer &amp; component marketplace.</strong> A live <a href=\"https://github.com/miadey/MotoView\">component gallery</a> ships today (the <code>/components</code> page on this site renders every built-in component server-side with its <code>.mview</code> snippet); the interactive drag-and-drop designer and a shareable component marketplace are the remaining, larger pieces.</li></ul>\n<blockquote><strong>Realtime is not on this list.</strong> The adaptive-polling render/event protocol <em>is</em> MotoView's communication layer; a canister cannot open a WebSocket without an external gateway. Polling is the design, not a placeholder.</blockquote>\n<h2 id=\"how-to-read-a-feature-s-status\">How to read a feature's status</h2>\n<p>When in doubt, the directive and CLI references describe only implemented behavior — if you find it documented there with a working snippet, it works. Anything on this page under <strong>Roadmap</strong> is the single source of truth for what is intentionally not done yet.</p>\n<table class=\"mv-doc-table\"><thead><tr><th>Status</th><th>Meaning</th></tr></thead><tbody><tr><td>Verified</td><td>Deployed and exercised in a real browser (or on the mainnet boundary)</td></tr><tr><td>Implemented / Production</td><td>In the compiler/runtime; documented with working snippets</td></tr><tr><td>Roadmap</td><td>Planned; not available</td></tr></tbody></table>\n<p>If something you need is on the Roadmap, that is a great place to contribute. The protocol is intentionally small and the brain/hands split keeps the surface area honest.</p>"; prevSlug = "directives-reference"; prevTitle = "Directive Reference"; nextSlug = ""; nextTitle = "" } };
        case (_) { null };
      };
    };

    func navLink(active : Text, slug : Text, title : Text) : Text {
      "<li><a" # (if (active == slug) " class=\"active\"" else "") # " href=\"/docs/" # slug # "\">" # title # "</a></li>";
    };

    public func sidebar(active : Text) : Text {
      var s = "";
      s #= "<div class=\"mv-nav-section\">Prologue</div><ul>";
      s #= navLink(active, "introduction", "Introduction");
      s #= navLink(active, "why-motoview", "Why MotoView");
      s #= navLink(active, "whats-new", "What's New");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Getting Started</div><ul>";
      s #= navLink(active, "installation", "Installation");
      s #= navLink(active, "quickstart", "Quickstart");
      s #= navLink(active, "project-structure", "Project Structure");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">The .mview Format</div><ul>";
      s #= navLink(active, "mview-files", "The .mview File");
      s #= navLink(active, "pages-and-routing", "Pages & Routing");
      s #= navLink(active, "layouts", "Layouts");
      s #= navLink(active, "components", "Components");
      s #= navLink(active, "control-flow", "Control Flow");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Interactivity</div><ul>";
      s #= navLink(active, "events", "Events");
      s #= navLink(active, "forms", "Forms");
      s #= navLink(active, "validation", "Validation");
      s #= navLink(active, "state", "State");
      s #= navLink(active, "persistence", "Upgrade-Stable Persistence");
      s #= navLink(active, "drag-drop-and-effects", "Drag & Drop, Effects & Services");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Styling</div><ul>";
      s #= navLink(active, "styling-and-themes", "Styling & Themes");
      s #= navLink(active, "svg", "SVG Interfaces");
      s #= navLink(active, "charts", "Charts");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Security</div><ul>";
      s #= navLink(active, "security", "Security");
      s #= navLink(active, "zero-trust", "Building a Zero-Trust App");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Architecture</div><ul>";
      s #= navLink(active, "protocol", "The Protocol");
      s #= navLink(active, "client-bridge", "The Client Bridge");
      s #= navLink(active, "runtime", "The Motoko Runtime");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Deployment</div><ul>";
      s #= navLink(active, "deployment", "Deployment");
      s #= navLink(active, "cli", "CLI Reference");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Tooling</div><ul>";
      s #= navLink(active, "ai-tools", "Using AI (Claude, Copilot, Cursor)");
      s #= "</ul>";
      s #= "<div class=\"mv-nav-section\">Reference</div><ul>";
      s #= navLink(active, "directives-reference", "Directive Reference");
      s #= navLink(active, "roadmap", "Roadmap & Status");
      s #= "</ul>";
      s;
    };

    public func sidebarForPath(path : Text) : Text {
      let active = switch (Text.stripStart(path, #text "/docs/")) { case (?x) x; case null "" };
      sidebar(active);
    };
  };
}
