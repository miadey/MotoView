// MotoView Android — root build file (scaffold). Plugin versions are declared on
// each module (:motoview library, :app application) to keep this minimal;
// nothing is applied at the root.
//
// SCAFFOLD ONLY — NOT BUILT HERE (no Android SDK/NDK). The :app module carries
// the release/signing/Play Publisher config (Slice 11). See CARGO_NDK.md for the
// Rust .so cross-compile and ../../RELEASE.md for the full pipeline.
